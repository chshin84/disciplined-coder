# 개발 대상(도메인) 참고서 — 인덱스

이 파일은 목차일 뿐이고 상세는 각 스킬에 있다(온디맨드). 개발 대상에 따라 **반드시 지켜야 할 요건과 갖추면 좋은 요건**이 있으니, 아래 표에서 지금 만들 대상의 행을 찾아 그 스킬을 연다. 표에 지금 만들 대상의 행이 없으면 그 대상에는 참고서가 없는 것이니 더 찾지 말고 그대로 설계를 이어 간다.

| 도메인 | 여는 시점 | 참조 스킬 |
|---|---|---|
| 문서 관리 | 문서를 쓰거나 문서의 구조를 잡을 때 연다(Claude Code에서 하는 일은 대개 문서를 읽어 문서를 쓰는 일이다). | `domain-docs` |
| 플러그인 관리 | Claude Code 플러그인이나 마켓플레이스를 만들 때 연다. | `domain-plugin` |
| LLM 런타임 | 제품이 돌아가는 중에 LLM을 부를 때 연다. | `domain-llm-runtime` · `reviewer-*` · `meta-aggregate` |

LLM 런타임 행만은 설계가 아니라 **런타임**에 도는 검증으로 반영하고, `domain-llm-runtime`을 열 때 그것이
부르는 `reviewer-*`와 `meta-aggregate`를 함께 본다.

## 등재 기준

- **등재 조건** — 실제로 겪은 문제가 있는 도메인만 올린다. 새 도메인은 필요할 때 신설한다(YAGNI·측정 먼저). 비어 있는 스텁을 두지 않는다 — 스텁이 있으면 세션이 그 행을 믿고 스킬을 열었다가 명세에 반영할 요건을 하나도 얻지 못한다.
