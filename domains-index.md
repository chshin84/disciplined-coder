# 개발 대상(도메인) 참고서 — 인덱스

이 파일은 목차일 뿐이고 상세는 각 스킬에 있다(온디맨드). 개발 대상에 따라 **마땅히 그래야 하는 것 / 그랬으면 하는 것**이 있으니, 아래 표에서 지금 만들 대상의 행을 찾아 그 스킬을 연다.

| 도메인 | 무엇을 만들 때 여나 | 어느 단계에서 반영하나 | 참조 스킬 |
|---|---|---|---|
| 문서 관리 | 문서를 쓰거나 문서의 구조를 잡을 때 연다(Claude Code에서 하는 일은 대개 문서를 읽어 문서를 쓰는 일이다). | 설계와 개발 단계에서 반영한다. | `domain-docs` |
| 플러그인 관리 | Claude Code 플러그인이나 마켓플레이스를 만들 때 연다. | 설계와 개발 단계에서 반영한다. | `domain-plugin` |
| LLM 런타임 | 제품이 돌아가는 중에 LLM을 부를 때 연다. | **런타임**에 도는 검증으로 반영한다. | `domain-llm-runtime`을 열고, 그것이 부르는 `reviewer-*`와 `meta-aggregate`를 함께 본다. |

## 사용
- **설계와 계획 단계에서는** 만들 대상이 위 도메인이면 해당 스킬을 열어 "마땅히 그래야 하는 것"을 명세에 반영한다. 이것이 1순위다.
- **개발 단계에서는** 명세에 못 담은 것을 그때 참조한다. 이것이 폴백이다.
- **실제로 겪은 문제가 있는 도메인만 올린다.** 새 도메인은 필요할 때 신설한다(YAGNI·측정 먼저). 비어 있는 스텁을 두지 않는다 — 없는 것이 "다뤄진 척"보다 정직하다.
