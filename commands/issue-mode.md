---
description: 오답노트 미해결 처분 모드를 토글한다 — surface(기본·메모리+surface) ↔ issues(must-keep을 GitHub Issues에 위임). 인자 없으면 현재 모드 표시. PC 전역, 다음 세션부터 적용.
---
`bash "${CLAUDE_PLUGIN_ROOT}/scripts/issue-mode.sh" $ARGUMENTS`를 실행하고 결과를 한 줄로 보고하라. 인자는 `surface` 또는 `issues`(없으면 현재 모드를 표시한다).
