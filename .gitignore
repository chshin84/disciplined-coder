# session-specific Claude Code local settings (not plugin content)
.claude/settings.local.json

# git worktrees (isolated workspaces — not plugin content)
.claude/worktrees/

# SDD 작업 폴더와 조각 회차 보관 (플러그인 내용이 아니다 — 커밋하면 감사의 tree_clean 이 거짓이 된다)
.superpowers/
