# 핸드오프 — 새 절차로 첫 회차를 돌고 미룬 것을 닫는다

읽고 담긴 것을 영속처(메모리·장부·문서)에 옮긴 뒤 이 파일을 지운다. 이 파일은 살아 있는 문서가 아니다.

## 이 세션이 맡는 것

감사 절차가 2026-09-05에 통째로 바뀌어 main(`f7ae303`)에 들어갔다. 발견이 짚은 곳과 상대편과 원칙과 결과 넷을 지고, 인용 확인과 지문과 회차 대조와 측정이 스크립트 둘로 옮겨졌고, 세션이 몰고 렌즈만 서브에이전트로 띄운다. 그 절차로 **회차를 한 번도 돌지 않았다.** 이 세션이 첫 회차를 돌려 설계의 성공 기준을 실제로 확인하고, 그 앞뒤로 미뤄 둔 것을 닫는다.

같은 시각에 다른 워크트리에서 자기개선 고리를 다른 프로젝트에 이식하는 설계가 돈다. 그쪽은 새 파일만 만들고 이쪽이 손대는 파일을 건드리지 않기로 했으므로, 이쪽이 먼저 main에 병합된다.

## 시작하기 전에

- 워크트리를 연다. 이름은 `audit-first-round`가 좋다. 바탕은 `git pull`을 마친 main이어야 한다.
- 플러그인이 `f7ae303`인지 확인한다. `/plugin`에서 `disciplined-coder`의 커밋을 보면 된다. 아니면 갱신하고 세션을 다시 연다. 옛 판으로 돌면 옛 절차가 실린다.
- 계약 테스트 다섯을 돌려 전부 FAIL=0인지 확인한다. `CLAUDE.md`의 「변경 뒤 실행」이 명령의 정본이다.

## 할 일과 순서

1. **판정 상태의 닫힌 집합을 살아 있는 문서에 적는다.** `scripts/test_audit.sh`가 `confirmed`·`rejected`·`undetermined`·`derived` 넷을 계획 문서(`docs/superpowers/plans/2026-09-05-findings-bar-and-deterministic-first.md`)에서 뽑아 쓴다. 계획 문서는 소비하고 지우는 문서라 정본이 못 된다. `skills/project-doc-audit/SKILL.md`의 「판정」 절에 넷을 적고 테스트가 거기서 뽑게 바꾼다. 회차보다 먼저 하는 이유는 회차 기록의 `status`가 그 넷을 쓰기 때문이다.
2. **첫 회차를 돈다.** 절차는 `skills/project-doc-audit/SKILL.md`의 걸음 여덟이다. 스크립트는 `scripts/audit_targets.sh`(대상), `scripts/audit_topics.sh`(이름표), `scripts/audit_evidence.sh`(인용 확인과 지문), `scripts/audit_rounds.sh diff|metrics`(대조와 측정)다. 앞선 회차 `docs/superpowers/reviews/2026-09-05-self-audit/`의 `findings.json`은 옛 형태라 `counterpart`와 `fingerprint`가 없으므로 첫 대조는 `no_prior_round`로 시작하는 것이 맞다. 회차가 끝나면 요약문에 뿌리와 물음과 실제 호출 수를 붙이고 `scripts/seal_reviews.sh`로 봉인한다.
3. **성공 기준 다섯을 확인해 적는다.** 서브에이전트 스물다섯 이하, 인용이 없는 발견이 판단 없이 탈락, 같은 기록으로 두 번 계산하면 같은 대조 결과, 계약 테스트가 픽스처만으로 FAIL=0, `run.json`에서 렌즈별 확정 수와 확정 하나당 값과 해소율을 읽을 수 있음이다. 어느 것이 안 맞으면 그것이 다음 설계의 입력이다.
4. **함정 목록을 실측으로 다시 세운다.** 지금 목록은 한 세션의 표본이다. 세션 기록(`~/.claude/projects/<프로젝트>/*.jsonl`)에서 오류와 재시도를 문자열로 세어 빈도 순으로 세우고, 세는 법을 함께 적는다. 결과로 메모리 `windows-env-pitfalls`와 계획의 「이 PC 에서 되풀이되는 함정」 절을 다시 쓴다.
5. **함정 가운데 코드로 막을 수 있는 것을 한 곳에서 막는다.** 목록으로 남기지 않는다. `json_run`이 인코딩을 막은 것과 같은 꼴로, 검사 헬퍼나 계약 테스트로 만든다. 코드로 못 막는 환경 성질(워크트리 격리, autocrlf, 백슬래시 전달)만 정본의 "이 환경의 관례" 줄 옆에 한두 줄로 남긴다.

## 알아 둘 것

- 회차를 돌리기 전에 작업 트리가 깨끗해야 한다. 회차 자신이 쓰는 파일은 지문에서 빼도록 돼 있으나, 그 밖의 미커밋 변경은 `tree_clean`을 거짓으로 만든다.
- 단언을 새로 쓸 때 고치기 전에 이미 통과하면 아무것도 검사하지 않는 것이다. 형태가 셋이다 — 원래 없던 문자열의 부재를 보는 것, 자기가 쓴 픽스처를 되읽는 것, 리터럴 하나만 겨눠 다른 표현을 놓치는 것. 2026-09-05에 이 형태가 다섯 번 리뷰까지 갔다.
- 파일을 지울 때는 함께 사라지는 검사가 살아 있는 대상을 보던 것인지 먼저 센다. 같은 날 두 번 그것을 놓쳤다.
- 다른 워크트리(`self-improvement-loop`)가 도는 동안 `skills/`의 기존 파일은 이쪽만 손댄다. 그쪽은 새 파일만 만든다.

## 끝나면

회차 기록과 고친 것을 커밋해 main에 병합하고, 이 파일을 지운다. 담긴 것 가운데 다음에도 필요한 것은 메모리 `pending-next-session`을 갱신해 남긴다.
