# 핸드오프 — 자기개선 고리를 다른 프로젝트에 이식하는 설계를 연다

읽고 담긴 것을 영속처(메모리·장부·문서)에 옮긴 뒤 이 파일을 지운다. 이 파일은 살아 있는 문서가 아니다.

## 이 세션이 맡는 것

이 저장소는 자기 원칙과 자기 렌즈로 자기를 감사해 고치는 고리를 갖고 있다. 2026-09-05에 그 고리의 발견 문턱과 검증 구조를 선행연구에 비추어 다시 짰고 main(`f7ae303`)에 들어갔다. 사용자의 구상은 **선행연구 렌즈(`lens-prior-art`)를 잘 끼워 넣으면 이 고리를 다른 프로젝트에도 옮길 수 있다**는 것이다. 이 세션이 그 이식의 설계를 연다. 구현까지 갈지 설계에서 멈출지는 설계가 정한다.

**구상의 뜻은 사용자가 2026-09-05에 직접 정정했다. 아래를 먼저 읽고 다른 해석으로 가지 않는다.** 첫 회차가 이 구상을 "spec을 쓴 뒤 리뷰에 선행연구를 얹고 발견을 계획의 검사로 잇는 고리"로 읽고 설계 초안을 냈다가 사용자가 "완전히 다르다"고 철회했다. 사용자의 말은 이렇다.

- 설계 스킬(`brainstorming`)이 **사용자에게 질문만 던지지 말고, 그 앞에 아이디어를 놓고 웹에서 기술을 적극적으로 찾아보고 그 결과를 들고 설계 대화에 들어오라**는 것이다. 선행연구 렌즈든 그에 맞는 기술 검색 렌즈든, 설계 직전에 넣거나 설계 스킬에 넣어 준다.
- 탐색은 둘이다. 첫 질문 앞에서는 **참고를 찾는 것**에 가깝다. 중간에는 한 번으로 끝나지 않고, **기술적 또는 의미론적 구성 요소 하나가 사용자와 협의해 충분히 파악되면 그 건마다 더 나은 방법이 없는지 찾는다.**
- 들고 올 것은 셋이다. 남들이 같은 문제를 푸는 방법과 그 결말, 이미 있는 도구와 라이브러리와 표준, 알려진 함정과 실패 원인.
- 값이 많이 드니 **"조사를 해볼까요"라고 그때마다 묻는다.** 조사는 조사대로 뒤에서 돌고, brainstorming과 계획과 구현은 그것을 기다리지 않고 병렬로 이어 간다. 건별 조사 자체는 설계 대화 안에서만 발동한다.
- 결과가 그 구성 요소의 결정이 끝난 뒤 도착하면 다음 검문소(접근안 제안, 설계 절 승인, spec 쓰기 앞)에서 보이고 되돌릴지 사용자에게 묻는다.
- 경로는 spike·bounded·architectural 셋 다에 건다.
- 끼우는 방법은 A안(brainstorming이 열릴 때 훅이 지시를 끼움)과 B안(우리 스킬이 먼저 탐색하고 brainstorming을 부름)의 하이브리드를 사용자가 물었다. 첫 회차의 답은 "탐색의 내용은 새 스킬 하나가 정본으로 갖고, 훅은 그 스킬대로 하라고만 끼우며, 같은 스킬을 사용자가 직접 부를 수도 있다"였고, 그 설계 초안이 아티팩트로 남아 있다(https://claude.ai/code/artifact/5daf3a48-a27b-404d-b772-e9edf5b7926a). 사용자는 절별 승인 전에 회차를 철회했으므로 그 초안은 승인된 것이 아니다.

훅이 Skill 도구 호출을 잡으면 입력은 `{"skill":"superpowers:brainstorming"}` 꼴이다. 세션 기록에서 확인했다.

첫 감사 회차(`audit-first-round`)는 main에 병합됐다(`04e2ef1`). 이제 그 파일들을 피할 이유는 없다.

## 시작하기 전에

- 워크트리를 연다. 이름은 `self-improvement-loop`가 좋다. 바탕은 `git pull`을 마친 main이어야 한다. 이 파일은 git 추적에서 뺐으므로 워크트리에는 없다. main 폴더의 이 파일을 읽는다.
- 플러그인이 `04e2ef1` 이후인지 확인한다. `/plugin`에서 `disciplined-coder`의 커밋을 보면 된다.
- `brainstorming`부터 연다. 이 저장소의 절차가 설계를 요구하고, 이식은 새 하위 체계라 architectural이다.

## 설계의 입력

- **선행연구 대조 기록** — `docs/superpowers/reviews/2026-09-05-lens-design-prior-art.md`. 렌즈 둘이 독립으로 같은 곳 셋을 짚었다. 경험적 게이트가 없으면 관점을 늘려도 그럴듯한 오탐이 안 걸러진다(적대적 검토자 열 명이 없는 취약점을 만장일치로 승인한 사례). 측정 축이 비어 있으면 배정을 바꿀 근거가 안 생긴다(사람의 검토는 결함 제거 효율과 결함당 비용으로 판단한다). 그리고 견주지 않은 기준선이 넷이다 — 같은 계산량의 단일 호출, 기계 검사 넓게 돌리고 LLM은 선별만, 검토자 한 명, 사용 기반 읽기. 이식 설계는 이 셋에 답을 갖고 있어야 한다.
- **상황별 렌즈 배치도** — 상황 다섯(문서 검진, spec 리뷰, 저장소 감사, 런타임 검증, 3층 diff 리뷰)마다 어느 렌즈를 걸고 누가 몰고 서브에이전트가 몇인지 정리한 것이다. 다른 프로젝트에 옮길 때 무엇이 상황에 묶여 있고 무엇이 저장소에 묶여 있는지 가르는 데 쓴다.
- **이 저장소가 세운 규칙** — 판단 앞에 기계를 세운다(`skills/domain-docs/SKILL.md` 「판단 앞에 기계를 세운다」), 렌즈는 판단만 하고 값의 경계는 새 프로젝트나 새 모델이 필요하면 제안하지 않는 것, 발견의 문턱은 짚을 상대편이 있는 어긋남이다. 이식할 때 이 규칙들이 그대로 가는지가 첫 물음이다.

## 설계가 답해야 할 물음

- brainstorming은 superpowers의 스킬이라 본문을 못 고친다. 훅이 지시를 끼우는 것과 우리 스킬을 직접 부르는 것을 어떻게 합치나. 훅이 무시돼도 남는 길이 있어야 한다.
- `lens-prior-art`는 웹에 나가는 유일한 렌즈라 상시 허용에서 빠져 있다. 사용자는 조사마다 묻는 쪽을 택했다. 새 탐색 스킬이 그 렌즈의 가드(인용 열어 확인, 못 찾은 것은 `not_found`, 질의 경계, `search_status`)를 베끼지 않고 가리키게 한다.
- 뒤에서 도는 조사의 결과를 어느 검문소에서 어떻게 보이고, 되돌릴지 어떻게 묻나. 웹에서 온 것이 설계를 저절로 고치지 않아야 한다.
- 기록을 어디에 남기나. 기록 이름 규칙의 소유자는 `skills/domain-docs/SKILL.md`의 문서 타입 표다. spec 리뷰의 선행연구 렌즈가 그 기록을 배경으로 받아 같은 것을 다시 찾지 않게 한다.
- 새 스킬이 렌즈인지(심사) 탐색인지에 따라 이름에 `lens-` 접두사를 붙일지 갈린다. 다른 회차가 렌즈 이름 형태를 사용자 결정으로 올려 두었다.

## 알아 둘 것

- 선행연구를 다시 돌릴 값이 있으면 `lens-prior-art`를 제안하고 승인받아 띄운다. 상한은 통상 둘까지다. 실험을 짜기 전에 먼저 찾아본다 — 모델 성능에 관한 것은 실측보다 조사가 앞이다.
- 서브에이전트 수를 셀 때 문서 하나에 호출 하나가 단위이고, 서브에이전트끼리는 캐시가 공유되지 않는다. 본문을 미리 적재해 팬아웃하는 방식은 실측에서 토큰 2.65배로 더 비쌌다.
- 이 저장소의 계약 테스트 다섯이 전부 FAIL=0인 상태를 유지한다. 새 스킬을 만들면 `scripts/test_docs_drift.sh`가 그 문서의 금지 낱말과 계약을 검사한다.

## 끝나면

설계 문서를 `docs/superpowers/specs/`에 두고 리뷰를 지난 뒤(사용자가 건너뛰기로 하면 그 사실을 마커 위에 적는다) 계획으로 넘긴다. 이 파일은 지운다. 담긴 것 가운데 다음에도 필요한 것은 메모리에 남긴다.
