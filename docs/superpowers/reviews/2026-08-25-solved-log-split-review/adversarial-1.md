# reviewer-adversarial 1회차 원본 (2026-08-25)

검토 대상: `docs/superpowers/specs/2026-08-25-solved-log-split-design.md`
띄운 방식: 읽기 전용 서브에이전트 1회. 전사본(`tasks/<id>.output`)은 완료 후 0바이트가 되므로
완료 통지에 실려 온 JSON을 그대로 옮겨 둔다.

```json
{
  "lens": "adversarial",
  "angles_scanned": [
    "지시사항 색인이 예방으로 걸리는가 — 실제 전역 52개·레포 27개를 항목별로 대조",
    "색인이 자랄 때의 한계 — spec에 상한이나 다음 단계 조건이 있는가",
    "세션 시작 색인 재생성의 비용과 실패 모드 — 동시 실행·권한·부분 실패·폴더 내용 필터",
    "비가역성 — 79개 재작성의 롤백 경로, 백업 하나로 충분한가, 잘못 쓴 지시사항의 탐지 수단",
    "옛 형식 로그에 매 세션 묻는 것 — 창 여럿·워크트리·거절 반복의 성가심",
    "과설계 — 색인을 도출로 만드는 것의 대가 대비 이득(SIMPLE·YAGNI)",
    "이득이 실제로 나는가 — 캐시 성질, 본문을 여는 회차, 프로젝트 층 적용",
    "정본 recall 규칙 교체의 부작용 — 증상으로 찾는 경로가 어디로 가는가",
    "기존 스캐폴드 계약과의 충돌 — 관리 디렉터리 화이트리스트, Codex 쌍둥이, 머리말 갱신",
    "검증 절의 커버리지 — 이 레포 자신의 오답노트가 이미 적어 둔 실패 경로가 계약에 있는가"
  ],
  "findings": [
    {
      "kind": "failure_mode",
      "where": "「색인은 손으로 유지하지 않고 본문에서 도출한다」 — \"세션 시작 때 스캐폴드가 본문 폴더를 훑어 각 파일의 첫 줄을 모아 색인을 다시 쓴다\", 그리고 검증 절의 \"**두 번 돌려도 같다** — 연속 두 번 실행해 색인과 본문이 모두 같은지 본다\"",
      "attack": "창을 여럿 열면 SessionStart가 동시에 같은 색인 파일에 진입한다. 이 레포는 이미 그 사고를 실측으로 겪었고, 그때의 교훈이 \"순차 멱등성 테스트는 이 경로를 구조적으로 못 밟으므로 동시 실행 회귀 테스트를 따로 세워야 한다\"였다. spec의 검증 계약은 정확히 그 못 밟는 순차 멱등성 하나뿐이고 잠금·원자적 교체를 한 줄도 요구하지 않는다. 더 나쁜 것은 경합 창의 크기다 — 지금 로그에 쓰는 유일한 경로인 `scaffold_fix_solved_header`는 머리말이 낡았을 때만 도는 드문 쓰기라 창이 좁았는데(`_scaffold_common.sh:184-186`이 `solved_rules_stale`이 1일 때만 부른다), 색인 재생성은 매 세션 무조건 쓰는 작업이라 경합 창이 상시가 된다.",
      "evidence": "D:\\projects\\disciplined-coder\\docs\\solved_problems.md:49-51 — \"**세션 시작 훅이 사용자 전역 `CLAUDE.md`를 통째로 날릴 수 있었다.** … 원인: 대상 경로에서 파생한 고정 이름 임시 파일을 쓰고 잠금이 없었다. 창을 여럿 열면 SessionStart가 같은 하나의 파일에 동시 진입한다. 실측으로 20개 동시 실행을 걸자 사용자 본문 두 줄이 0줄이 되고 관리블록이 6~11개로 늘었다.\" / 대비: `scripts\\_managed_block.sh:71,94`는 `mkdir` 락을 갖췄으나 `scripts\\_scaffold_common.sh:165`의 로그 쓰기 경로는 `mktemp`만 쓰고 락이 없다 / spec 179",
      "why_it_matters": "색인은 새 recall 규칙에서 유일한 진입로다. 반쯤 쓰이고 죽거나 두 세션이 겹쳐 쓰면 남은 항목은 영영 안 걸리는데, 본문 파일은 멀쩡히 있고 색인은 '도출물이라 원래 매번 바뀌는 파일'로 보이므로 아무도 사라진 것을 눈치채지 못한다. 조용한 실패다."
    },
    {
      "kind": "failure_mode",
      "where": "「색인은 손으로 유지하지 않고 본문에서 도출한다」 — \"정렬은 색인이 맡는다. 파일 이름에 기대지 않고, 본문 파일의 등록 순서를 색인이 시간순으로 유지한다.\"",
      "attack": "이 문장은 재료 없이 성립을 주장한다. 색인은 매 세션 본문에서 다시 쓰이고 손편집은 덮인다고 같은 절이 못박았으므로, 등록 순서라는 사실은 색인에 남을 수 없다. 파일 이름에도 기대지 않는다고 명시했고, 실제로 45개 이상은 날짜가 아예 안 붙는다. 남는 후보는 파일 시스템 mtime뿐인데 이관 당일 79개가 전부 같은 시각이 되고 백업 복원·다른 PC 복사에서 무너진다. 반대로 '기존 색인의 순서를 읽어 유지하고 새 파일만 뒤에 붙인다'로 구현하면 색인은 더 이상 순수 생성물이 아니라 상태를 지닌 파일이 되어 \"색인은 생성물이다\"·\"색인과 본문이 어긋날 수가 없다\"는 이 절의 근거가 통째로 무너진다.",
      "evidence": "spec 59-68 — \"색인은 생성물이다\" / \"사람이 색인을 손댈 일이 없으니 색인과 본문이 어긋날 수가 없다\" / \"색인을 손으로 고쳐도 덮인다.\" / \"파일 이름에 기대지 않고\" / spec 76-77 — \"45개가 한 달 구간에 뭉친다. 그 45개에 이관일을 박으면 … 적지 않는다\"",
      "why_it_matters": "\"순서는 시간순이고 아래에 추가한다\"는 현행 머리말 계약(`_scaffold_common.sh:23`)을 새 구조가 어떻게 지키는지가 미정인 채로 구현에 넘어간다. 구현자가 어느 쪽을 고르든 spec이 내세운 SSOT 논거 또는 시간순 계약 하나가 깨지고, 어느 쪽이 깨졌는지는 코드를 열어야만 안다."
    },
    {
      "kind": "yagni",
      "where": "「파일 이름은 아는 것에만 날짜를 붙인다」 절 전체 — \"이름은 `<날짜>-<짧은 영문>.md`이되, **날짜를 모르는 항목에는 날짜를 붙이지 않는다.**\"",
      "attack": "이 규칙이 하는 일이 없다. 색인 도출은 각 파일의 첫 줄만 읽고, 정렬은 파일 이름에 기대지 않는다고 바로 앞 절이 못박았다. 그러면 이름의 날짜를 읽는 소비자가 아무도 없다. 그런데 이 장식을 위해 \"이 레포의 오답노트 27개는 git 이력 스무 커밋으로 각 항목이 언제 들어왔는지 정확히 복원된다\"는 복원 작업이 이관 범위에 들어오고, 이름 규약이 세 갈래(날짜 없음·본문에 있음·앞으로 적을 것)로 갈려 새 항목을 적을 때마다 어느 갈래인지 판단해야 한다.",
      "evidence": "spec 61 — \"각 파일의 첫 줄을 모아 색인을 다시 쓴다\" / spec 68 — \"파일 이름에 기대지 않고\" / spec 74-75 — \"git 이력 스무 커밋으로 각 항목이 언제 들어왔는지 정확히 복원된다(2026-06-30부터)\"",
      "why_it_matters": "지금 필요 없는 규약 때문에 이관 비용과 앞으로의 판단 부담이 늘어난다. 날짜를 쓰는 소비자가 생겼을 때 붙여도 늦지 않다(`SIMPLE`). 게다가 날짜가 있는 파일과 없는 파일이 섞이면 폴더를 눈으로 볼 때의 정렬조차 뒤죽박죽이라, 이름의 날짜가 사람에게 주려던 이득마저 사라진다."
    },
    {
      "kind": "failure_mode",
      "where": "「인지한 대가와 한계」 — \"**지시사항 한 줄로 고를 수 있어야 성립한다.** 색인 줄만 보고 열지 말지 정하지 못하면 결국 여러 파일을 열게 되어 이득이 사라진다.\"",
      "attack": "실제 79개를 지시사항으로 올리면 상당수가 같은 한 줄로 수렴한다. 뮤테이션 검증 계열만 세어도 전역 일곱(빈 줄 개수 항진, 가로채이는 호스트 음성대조, `@($null).Count`, `'10' -eq 10`, AND 조건 픽스처, 알파 기준 단언, `IndexOf` -1)에 레포 둘(`grep -F` 여러 줄, 같은 줄 grep 두 번)이다. 이들을 서로 다르게 만드는 것은 전부 '원인' 문장이고 spec은 그것을 본문으로 내린다. 그러면 색인에는 \"단언을 넣었으면 회귀를 심어 FAIL이 뜨는지 확인하라\"에 해당하는 줄이 아홉 벌 남고, 파워셸 테스트를 쓰기 시작한 회차에 그 아홉 중 어느 것을 열지 색인 줄만으로는 못 고른다.",
      "evidence": "C:\\Users\\CHSHIN\\.claude\\disciplined-coder\\solved_problems.md:22, 50-52, 53-55, 127-129, 131-133, 143-145, 160-162 / D:\\projects\\disciplined-coder\\docs\\solved_problems.md:40-42, 46-48 — 아홉 항목 모두 해결 문장이 \"회귀를 심어 확인하라\"·\"뮤테이션 검증에서만 드러났다\"로 끝난다",
      "why_it_matters": "spec 자신이 성립 조건으로 세운 전제가 실제 데이터에서 깨진다. 아홉 파일을 다 열면 옮겨 간 본문을 회차마다 다시 싣게 되어 절감이 사라지고, 안 열면 예방이 안 걸린다. 이 위험은 다시 쓰기를 다 끝낸 뒤에야 드러나므로 그때는 79개가 이미 흩어진 뒤다."
    },
    {
      "kind": "failure_mode",
      "where": "「무엇을 왜 바꾸는가」의 발동 시점 논거와 「정본의 recall 규칙과 머리말 형식 규칙을 고친다」 — \"정본 오답노트 절의 recall 규칙을 … **\"지금 하려는 작업에 걸리는 줄이 있는지 본다\"**로 고친다\"",
      "attack": "실제 52개 가운데 지시사항으로 바꿔도 '시작할 때' 걸리지 않는 항목이 있다. 트리거가 사고 그 자체인 항목들이다. 사용량 제한 항목의 처방은 \"제한 리셋 후 `resumeFromRunId`로 재개하면\"이라 제한에 걸린 뒤에만 발동하고, Firestore 항목은 \"0건이 나오면 조건을 다 떼고\"라 0건을 본 뒤에만 발동하며, O(N²) 항목은 \"`cumtime`이 아니라 그 함수 자신의 `tottime`을 먼저 보라\"라 이미 느려진 뒤에만 발동한다. python3 항목은 아예 \"**2026-08-25 해결됨**\"이 붙어 지시사항으로 옮길 내용이 환경 사실뿐이고, pptx 렌더 경로 항목은 본문이 \"(이후 설치했다)\"로 스스로를 무효화해 두었다. 이런 항목의 첫 줄을 억지로 지시사항형으로 고치면 트리거가 지나치게 넓어져(\"쿼리를 짤 때\"·\"에이전트를 여럿 띄울 때\") 상시 발동이 되고, 상시 발동은 안 걸리는 것과 같다.",
      "evidence": "C:\\Users\\CHSHIN\\.claude\\disciplined-coder\\solved_problems.md:16(세션 리밋), :17(Firestore 0건), :38-40(tottime), :15(python3 — \"**2026-08-25 해결됨**\"), :93-95(soffice — \"그때 이 머신에는 파워포인트만 있었다(이후 설치했다)\")",
      "why_it_matters": "spec의 핵심 전제는 \"지시사항으로 바꾸면 시작할 때 걸린다\"인데, 그 전환이 되는 항목과 안 되는 항목이 섞여 있다는 사실이 문서 어디에도 없다. 안 되는 항목까지 전부 다시 쓰면 79개 재작성 비용을 다 치르고 그 몫은 예방 효과가 0이며, 그러면서 원래 걸리던 증상 검색만 잃는다."
    },
    {
      "kind": "failure_mode",
      "where": "「색인과 본문을 가른다」 — \"예방에 실패한 회차에는 여전히 증상으로 찾아야 하고\" 와 「정본의 recall 규칙과 머리말 형식 규칙을 고친다」의 규칙 교체",
      "attack": "증상을 본문에 남기는 이유로 '예방에 실패한 회차'를 들면서, 그 회차에 본문을 **어떻게 찾는지**는 아무 데도 정하지 않았다. 정본의 recall 규칙은 한 자리뿐이고 spec은 그것을 '고친다'(대체한다)고 적었으므로, 교체 후 상시 로드에는 증상으로 들어가는 진입로가 없다. 색인에는 증상이 없고 본문은 상시 로드가 아니며, 폴더를 grep하라는 절차도 없다. 즉 오답노트가 가장 필요한 회차 — 이미 증상이 난 회차 — 의 경로가 통째로 빈다.",
      "evidence": "spec 52-55 / spec 119-120 / D:\\projects\\disciplined-coder\\agent-principles.md:122 — \"**꺼내 쓰기(recall)**: 디버깅·구현을 시작하기 전에 **PC solved와 프로젝트 solved 둘 다**에서 비슷한 증상을 먼저 찾는다\"(이 한 문장이 유일한 recall 규칙이다)",
      "why_it_matters": "증상 줄을 본문에 보존한 것이 헛수고가 된다. 보존은 했는데 도달 수단이 없으니, 예방이 빗나간 회차에는 오답노트가 있어도 없는 것과 같아진다. 이 문서가 여는 절에서 지적한 것과 정확히 같은 종류의 앞뒤 안 맞음이 반대 방향으로 다시 생긴다."
    },
    {
      "kind": "overdesign",
      "where": "「색인은 손으로 유지하지 않고 본문에서 도출한다」 — \"같은 사실이 두 곳에 손유지로 놓이는 `SSOT` 위반을 구조로 없앤다\"",
      "attack": "이 SSOT 위반은 구조로 없앨 만큼 크지 않다. 오답노트를 쓰는 주체는 이미 메인 세션 하나로 좁혀져 있고, 새 항목 등록은 한 턴 안에서 본문 파일과 색인 줄을 함께 쓰는 한 동작이다. `SSOT`가 막으려는 것은 \"사람이 두 곳을 손으로 맞추게 만드는\" 상황인데 여기서는 두 곳이 같은 쓰기 안에 있다. 반면 도출로 만드는 대가는 위에 적은 동시 진입·순서 재료 부재에 더해, 매 세션 도는 새 쓰기 경로, 관리 디렉터리 위생과의 충돌, Codex 쌍둥이 동기화, 그리고 spec 자신이 센 \"`test_scaffold.sh`가 오답노트를 서른두 곳에서 잡고 있어 그만큼 함께 손본다\"까지다. 값이 나오는 두 이득(상시 로드 축소·예방 발동)은 색인이 손유지든 도출이든 똑같이 난다.",
      "evidence": "D:\\projects\\disciplined-coder\\agent-principles.md:138-139 — \"**작성자**: solved는 메인 세션만 쓴다(동시 쓰기 손상 방지). 서브에이전트는 직접 쓰지 말고 …\" / agent-principles.md:63 `SSOT` 정의 — \"사람이 두 곳을 손으로 맞추게 만드는 순간 둘은 반드시 어긋나기 때문이다\" / spec 185-186",
      "why_it_matters": "설계의 가장 큰 위험 덩어리(매 세션 자동 재작성)가 가장 작은 이득(손유지 두 줄의 정합)에 붙어 있다. 도출을 빼면 이 spec의 실패 모드 목록 절반이 함께 사라지고 이관 자체는 그대로 굴러간다."
    },
    {
      "kind": "failure_mode",
      "where": "「색인과 본문을 가른다」 — \"**두 층에 같은 구조를 둔다.** 전역은 `~/.claude/disciplined-coder/` 아래, 프로젝트는 `docs/` 아래다.\"",
      "attack": "전역 본문 폴더가 관리 디렉터리 화이트리스트에 없다. 스캐폴드 위생은 화이트리스트 밖 디렉터리를 발견하면 매 세션 \"비관리 디렉터리 '…' 잔존(자동삭제 안 함, 확인 요)\"을 stderr로 낸다. spec은 화이트리스트 갱신을 한 줄도 적지 않았다. 그리고 Codex 층이 통째로 빠져 있다 — `codex-scaffold.sh`는 매 세션 `cat \"$KDIR/solved_problems.md\"`로 전역 로그 전문을 주입하고 홈이 `~/.codex/disciplined-coder/`인데, spec은 전역 자리를 `~/.claude/…` 하나로만 적는다. 정본은 두 자리를 함께 적으라고 명시해 두었다.",
      "evidence": "D:\\projects\\disciplined-coder\\scripts\\_scaffold_common.sh:6 — `SCAFFOLD_WHITELIST=\"agent-principles.md domains-index.md solved_problems.md backups\"` / 같은 파일:45-47 — 비관리 디렉터리 경고 / scripts\\codex-scaffold.sh:74 — `if [ -f \"$KDIR/solved_problems.md\" ]; then cat \"$KDIR/solved_problems.md\"; fi` / agent-principles.md:162-163 — \"그 사본이 놓이는 자리는 런타임마다 다르다 — Claude에서는 `~/.claude/disciplined-coder/`이고, Codex에서는 `~/.codex/disciplined-coder/`이며\"",
      "why_it_matters": "화이트리스트를 안 고치면 배포 첫 세션부터 모든 PC가 매 세션 오탐 경고를 받고, 그 경고는 진짜 고아를 알리라고 세운 `FAIL-LOUD` 신호라 오탐이 상시가 되면 무뎌진다. Codex 쪽은 이 레포가 이미 \"쌍둥이 스크립트는 한쪽을 고칠 때 반드시 같이 본다\"를 오답노트에 적어 둔 자리인데 spec이 그 절반만 설계했다."
    },
    {
      "kind": "irreversible",
      "where": "「기존 79개를 옮기고 지시사항 줄을 새로 쓴다」 — \"이관 전에 두 로그의 사본을 백업 디렉터리에 뜬다. 사본을 못 뜨면 이관하지 않는다.\" 및 「인지한 대가와 한계」 — \"**79개를 다시 쓰다 뜻이 상할 수 있다.**\"",
      "attack": "백업 하나로는 부족한 경로가 셋이다. 첫째, 롤백 절차가 없다 — 이관 뒤 며칠 지나 되돌리려면 백업 사본으로 덮어야 하는데 그 사이 메인 세션이 적은 새 항목이 함께 지워진다. 둘째, 잘못 쓴 지시사항을 잡아낼 수단이 0이다. 검증 절의 계약은 \"본문은 한 줄도 안 지워진다\"뿐이고, 지시사항이 원문 증상과 맞는지는 어느 계약도 보지 않는다. 색인만 읽는 것이 새 recall 규칙이므로 어긋난 한 줄은 색인에 영구히 남고 원문은 그 파일을 여는 회차에만 보인다 — 즉 열지 않으면 영영 안 드러난다. 셋째, 한 덩어리 파일일 때는 항목 소실이 파일 diff로 드러났는데 79개 파일로 흩어지면 파일 하나가 사라져도 색인이 그냥 한 줄 짧게 재생성되고 정상으로 보인다. 전역 로그는 git 밖이다.",
      "evidence": "spec 99 / spec 168-169 — \"원문 증상을 본문에 보존하는 것이 이 위험에 대한 대비이지 제거가 아니다\" / spec 177-178(검증 계약 목록) / D:\\projects\\disciplined-coder\\scripts\\_scaffold_common.sh:159 — \"사본이 유일한 복구 수단이다(이 로그들은 git 밖일 수 있다)\" / _scaffold_common.sh:25 — 머리말 계약 \"안 쓰이는 항목도 지우지 않는다\"",
      "why_it_matters": "append-only 계약의 집행이 파일 시스템에 위임되는데, 파일 시스템은 그 계약을 모른다. 그리고 뜻이 상한 지시사항은 잘못된 예방을 시키는 쪽이라 침묵보다 나쁘다 — 색인만 읽는 회차에는 그 줄이 곧 정답으로 쓰인다."
    },
    {
      "kind": "failure_mode",
      "where": "「옛 형식 로그를 만나면 개편을 권하며 묻는다」 — \"**거절을 기록하지 않는다.** 넘어가면 다음 세션에 다시 묻는다. … 한 번 승낙하면 끝나는 일이다.\"",
      "attack": "\"한 번 승낙하면 끝\"이 성립하지 않는 경로가 있다. 첫째, 승낙 자체가 무겁다 — 개편은 앞 절이 \"기계가 못 한다\"고 인정한 그 다시 쓰기이므로, 여든 개짜리 로그에 '지금 개편한다'를 누르면 사용자가 하려던 일 앞에서 세션 하나가 통째로 재작성에 먹힌다. 그런데 그것이 권장값이고, 함께 보이는 것은 항목 수뿐이라 그 값이 몇 분인지 한 세션인지 판단할 근거가 없다. 둘째, 사본을 못 뜨거나 쓰기가 막힌 PC에서는 승낙해도 안 고쳐지므로(같은 절의 규율) 물음이 영구 반복된다. 셋째, 세션은 창마다 열리고 프로젝트마다 열린다 — 3층 오케스트레이션은 스펙별 독립 워크트리를 쓰므로 워크트리마다 `docs/solved_problems.md`가 있는 세션이 저마다 질문 도구를 띄우려 든다. 자율로 돌아야 하는 층이 사람 입력을 기다린다.",
      "evidence": "spec 95 — \"**사람이 하는 다시 쓰기.** 79개의 첫 줄을 … 기계가 못 한다\" / spec 105-108 / spec 114 — \"못 뜨면 아예 고치지 않고 사유를 알린다\" / D:\\projects\\disciplined-coder\\agent-principles.md:147-153(병렬 오케스트레이션 — 스펙별 독립 워크트리) / scripts\\scaffold.sh:54-61(프로젝트 로그는 `CLAUDE_PROJECT_DIR` 기준이라 세션마다 다시 판정한다)",
      "why_it_matters": "\"고칠 때까지 세션마다 반복한다\"는 관례를 따라 왔다고 적었지만, 따라온 그 관례는 사용자를 막지 않는 stderr 한 줄이었다. 질문 도구는 답해야 진행되는 차단이라 성질이 다르다. 반복되는 차단은 사용자가 반사적으로 넘기게 만들고, 그러면 개편은 영영 안 되면서 세션마다 값만 든다."
    },
    {
      "kind": "failure_mode",
      "where": "「인지한 대가와 한계」 — \"상시 로드는 프롬프트 캐시에 올라가므로 회차당 금액으로 치면 원래 싸다. … 본문은 사라지는 것이 아니라 옮겨 가는 것이라, 그 항목을 실제로 여는 회차에는 값이 다시 든다. 이득은 안 여는 회차에서 난다.\"",
      "attack": "이 설계는 캐시에 올라가는 비용을 캐시에 안 올라가는 비용으로 바꾼다. 그리고 설계가 목표대로 작동할수록 손해가 커진다 — 새 recall 규칙은 \"지금 하려는 작업에 걸리는 줄이 있는지 본다\"이고, 지시사항 색인의 존재 이유는 증상과 달리 **작업을 시작할 때마다** 걸린다는 것이다. 예방이 잘 걸릴수록 본문 파일을 여는 회차가 늘고, 그 Read는 회차마다 새로 드는 값이다. 반대로 이득이 나는 \"안 여는 회차\"는 예방이 안 걸린 회차, 즉 이 설계가 실패한 회차다. 그런데 두 회차의 비율은 측정되지 않았고 이관 후에 재겠다는 계획도 절감폭에만 걸려 있다.",
      "evidence": "spec 161-163 / spec 119 / spec 156-159 — \"이관을 마친 뒤 실제로 세어 기록한다\"(세겠다고 적은 것은 색인 자수뿐이다)",
      "why_it_matters": "성공 지표와 이득 지표가 반대 방향을 가리키는데 spec이 그것을 짚지 않았다. 이관 뒤 자수만 세면 \"3할 줄었다\"가 나오고 실제로 늘어난 회차별 Read 비용은 아무도 안 센다. `MEASURE-FIRST`가 요구하는 실측이 절감 쪽에만 걸려 있다."
    },
    {
      "kind": "yagni",
      "where": "「무엇을 왜 바꾸는가」 — \"프로젝트 오답노트(이 레포는 항목 27개에 9,816자)는 자동으로 실리지는 않지만 꺼내 쓸 때 통째로 읽히므로 같은 성질이다\" 및 두 층 동시 적용",
      "attack": "프로젝트 층에는 이 설계의 첫째 이득이 없다. 상시 로드가 아니라고 spec 자신이 인정하므로 남는 이득은 \"꺼내 쓸 때\" 한 번의 Read를 줄이는 것인데, 새 구조에서는 색인 Read 한 번에 본문 Read 여러 번이 더해진다. 27개 9,816자짜리 파일 하나를 통째로 읽는 것보다 비싸지는 회차가 흔하다. 그런데 이관 위험(79개 재작성 중 27개)과 이관 비용의 3분의 1이 이 층에서 발생하고, 남의 프로젝트 로그에 개편을 묻는 매 세션 물음도 전부 이 층에서 나온다.",
      "evidence": "spec 11 / spec 57 / spec 165-166(여러 파일을 열면 이득이 사라진다는 자기 조건)",
      "why_it_matters": "이득이 불확실한 층까지 한 번에 밀면서 위험과 성가심의 큰 몫을 거기서 얻는다. 전역 층만 먼저 하고 프로젝트 층은 값이 확인된 뒤에 넓혀도 설계는 그대로 성립한다(`SIMPLE`)."
    },
    {
      "kind": "failure_mode",
      "where": "「정본의 recall 규칙과 머리말 형식 규칙을 고친다」 — \"오답노트 머리말의 형식 규칙 블록도 새 구조에 맞게 전면 개정한다. 개정하면 기존 로그들의 머리말은 배포 후 첫 세션에 스캐폴드가 저절로 갱신한다\"",
      "attack": "머리말 갱신은 승인 없이 자동으로 돌고 개편은 물어서 거절될 수 있으므로, 둘 사이에 어긋난 상태가 남는다. 개편을 거절한 로그에는 쪼개진 구조를 규정하는 새 머리말이 이미 박혀 있고 항목은 옛 한 덩어리 형식이다. 게다가 낡음 판정은 머리말 문자열 포함 검사이므로, 갱신이 끝나면 그 로그는 \"형식 최신\"으로 판정되고 옛 형식이라는 사실은 본문 폴더 존재 여부로만 남는다 — 사용자 눈에는 최신 형식 규칙을 선포하면서 그 규칙을 안 지키는 파일이 된다.",
      "evidence": "spec 122-124 / spec 110-112(거절해도 그대로 둔다) / spec 180 — \"본문 폴더가 없는 로그에는 색인 재생성을 걸지 않고\" / D:\\projects\\disciplined-coder\\scripts\\_scaffold_common.sh:112-115 — `case \"$body\" in *\"$SCAFFOLD_SOLVED_RULES\"*)`로 포함 여부만 본다",
      "why_it_matters": "한 파일이 자기 형식과 다른 규칙을 규정하게 되고, 그 상태는 `scaffold_check_solved_rules`가 최신으로 보므로 어떤 자동 신호에도 안 걸린다. 다음에 그 로그에 항목을 적는 사람은 머리말을 읽고 본문 파일을 만들려다 폴더가 없다는 것을 발견한다."
    },
    {
      "kind": "overdesign",
      "where": "「무엇을 왜 바꾸는가」 — \"`2026-06-15-agent-principles-redesign-design.md`의 비목표 절이 \\\"검색형 이슈 로그는 만들지 않는다. 평면 리스트가 실제로 커져서 아플 때 별도로 다룬다(측정 먼저)\\\"로 조건을 걸어 두었고, 위 숫자가 그 조건을 채웠다.\"",
      "attack": "같은 조건이 이번 설계에는 걸려 있지 않다. 색인은 항목당 한 줄이므로 52개가 150개가 되면 색인 자체가 다시 20,000자대로 자라 원점으로 돌아가는데, 그때는 이미 한 겹을 쪼갠 뒤라 같은 수법을 두 번 쓸 수 없다. spec의 「인지한 대가와 한계」 네 항목 어디에도 이 한계가 없고, \"이관을 마친 뒤 실제로 세어 기록한다\"는 한 번의 측정만 약속한다. 참고로 현행 머리말 계약은 \"항목이 스무 개를 넘으면 그때 영역별로 묶는다\"인데 79개인 지금 이미 안 지켜지고 있다 — 성장 조건을 적어만 두고 발동하지 않은 전례가 바로 이 로그에 있다.",
      "evidence": "spec 19-21 / spec 154-169(한계 네 항목에 성장 관련 서술이 없다) / D:\\projects\\disciplined-coder\\scripts\\_scaffold_common.sh:24 — \"- 항목이 스무 개를 넘으면 그때 영역별로 묶는다.\" (전역 로그는 52개, 레포 로그는 27개다)",
      "why_it_matters": "이 설계의 절감이 한시적이라는 사실이 문서에 안 남는다. 다음 사람은 색인이 다시 커졌을 때 왜 커졌는지, 이때 무엇을 하기로 했었는지를 처음부터 다시 측정해야 하고, \"조건을 적어 두면 그때 발동한다\"는 가정이 같은 파일에서 이미 한 번 빗나갔다."
    }
  ],
  "principles_applied": ["SIMPLE", "SSOT", "IDEMPOTENT", "REVERSIBLE", "FAIL-LOUD", "MEASURE-FIRST", "EXPLICIT", "ASK-FORK", "NAME-ITEMS"],
  "notes": "안 본 것을 밝힌다. 첫째, `scripts/test_scaffold.sh`를 열지 않았다 — spec이 \"오답노트를 서른두 곳에서 잡고 있다\"고 적었고 `grep -c solved`로는 48줄이 잡히는데, 그 차이가 세는 단위 차이인지 spec의 수치 오류인지는 그 파일을 읽어야 갈린다(이 레포 오답노트에 \"서브에이전트가 준 수치를 그대로 spec에 옮겨 실측이라고 못박았는데 세어 보니 틀렸다\"는 전례가 있어 확인할 값어치가 있다). 둘째, 옛 한 줄 형식 7개가 실제로 손으로 갈리는 모양인지 대조하지 않았다 — spec이 \"평균 337자\"라는 실측을 들었으나 어느 항목인지 지목하지 않아 대조 대상을 특정할 수 없었다. 셋째, 색인 재생성의 실행 시간은 재지 않았다(구현이 없어 잴 대상이 없다). 셋 다 확인하려면 spec이 그 대상을 이름으로 지목해야 한다."
}
```
