# reviewer-consistency 1회차 원본 (2026-08-25)

검토 대상: `docs/superpowers/specs/2026-08-25-solved-log-split-design.md`
띄운 방식: 읽기 전용 서브에이전트 1회. 전사본(`tasks/<id>.output`)은 완료 후 0바이트가 되므로
완료 통지에 실려 온 JSON을 그대로 옮겨 둔다.

```json
{
  "lens": "consistency",
  "angles_scanned": [
    "색인은 생성물이라는 규칙과 본문 보존 규칙이 부딪히는 자리",
    "색인 도출 규칙의 입력 형식 이탈(첫 줄 없음·여러 줄·헤딩 기호·중복) 처리",
    "정렬과 등록 순서를 무엇이 기억하는가",
    "파일 이름 규칙과 정렬·중복·충돌의 정합",
    "전역과 프로젝트 두 층의 대칭, 그리고 scaffold.sh와 codex-scaffold.sh 쌍둥이 반영",
    "Codex의 stdout 주입 경로와 포인터 경로 해석",
    "agent-principles 오답노트 절(작성자·recall·스코프 라우팅·append-only)과의 어긋남",
    "domain-docs 문서 타입·수정 규율 표와 새 문서들의 분류",
    "_scaffold_common.sh의 머리말 템플릿·형식 규칙 상수·머리말 갱신 절차와의 충돌",
    "test_scaffold.sh에 이미 걸려 있는 계약과의 충돌",
    "README·CLAUDE.md가 소유한 정본 문장과의 충돌",
    "spec이 이미 반영했다고 적은 둘의 실제 레포 상태 대조",
    "spec 안에 적힌 실측 수치의 재현",
    "spec 안의 이름 드리프트",
    "한 구현 계획에 맞는 스코프인가"
  ],
  "findings": [
    {
      "kind": "contradiction",
      "where": "「색인은 손으로 유지하지 않고 본문에서 도출한다」 — \"색인을 손으로 고쳐도 덮인다.\" 및 검증 절 \"색인을 손으로 고쳐 두면 덮인다\"",
      "problem": "색인 파일(`solved_problems.md`)에 사람이 적어 둔 절이 있으면 재생성이 그것을 지운다. 그런데 지금 그 파일에 사람이 만든 절이 남아 있는 것은 이미 기계로 강제된 계약이다 — 머리말 갱신은 '머리말 뒤에 사람이 만들어 둔 절'을 살려 두게 되어 있고, 실측된 로그(newsstore)의 다이제스트 절이 그 사례이며 다른 문서가 그 절을 서브에이전트 주입 재료로 참조한다. spec은 색인 파일에 사람이 적어 둔 것이 있을 때 어떻게 되는지를 한 문장도 정하지 않는다.",
      "evidence": "spec 66·176행 / D:\\projects\\disciplined-coder\\scripts\\test_scaffold.sh:613-632 (\"a hand-written section after the header survives\", `check \"절 보존: gotchas 제목\"` 등) / D:\\projects\\disciplined-coder\\scripts\\_scaffold_common.sh:120-122",
      "why_it_matters": "배포 첫 세션에 색인이 재생성되는 순간 사용자가 그 로그에 만들어 둔 절이 사본에만 남고 사라진다. test_scaffold.sh의 그 계약은 통과할 수 없게 되는데, spec은 그 계약을 지운다고도 고친다고도 적지 않았다."
    },
    {
      "kind": "contradiction",
      "where": "검증 절 \"**본문은 한 줄도 안 지워진다** — 이관과 색인 재생성 어느 쪽도 본문 파일의 내용을 건드리지 않는다\"",
      "problem": "같은 문서가 바로 앞에서 이관이 본문의 첫 줄을 다시 쓴다고 적었다 — \"79개의 첫 줄을 증상에서 지시사항으로 바꾸는 일은 기계가 못 한다\". 이관은 본문 파일의 첫 줄을 새로 쓰는 작업이므로 '이관도 본문을 안 건드린다'는 단언과 부딪힌다.",
      "evidence": "spec 95행 대 177행",
      "why_it_matters": "이 문장이 그대로 테스트 계약이 되면 무엇을 단언해야 하는지가 갈린다. 첫 줄을 바꾸는 이관을 통과시키려고 계약을 느슨하게 쓰면, 정작 막으려던 '본문 줄이 조용히 사라지는 회귀'를 못 잡는다."
    },
    {
      "kind": "gap",
      "where": "「색인은 손으로 유지하지 않고 본문에서 도출한다」 — \"정렬은 색인이 맡는다. 파일 이름에 기대지 않고, 본문 파일의 등록 순서를 색인이 시간순으로 유지한다.\"",
      "problem": "색인은 매 세션 폴더를 훑어 통째로 다시 쓰는 생성물이라 이전 상태를 기억하지 않는다. 그러면 등록 순서를 무엇이 기억하는지가 비어 있다. 이름은 안 쓰겠다고 못박았고, 날짜 없는 항목이 전역 52개 중 45개 이상이며, 전역 로그는 git 밖이라 이력이 없고, 이관하면 파일 수정시각은 모두 같은 날로 뭉친다 — spec이 그 세 사실을 스스로 적어 두었다.",
      "evidence": "spec 61-63행·68행·74-77행. 순서 요구는 머리말 형식 규칙에도 있다(D:\\projects\\disciplined-coder\\scripts\\_scaffold_common.sh:22 \"순서는 시간순이고 아래에 추가한다\")",
      "why_it_matters": "구현이 어쩔 수 없이 이름순이나 수정시각순으로 떨어지고, 그러면 '시간순'이라 적힌 색인이 실제로는 임의 순서가 된다. 순서를 지키려고 이전 색인을 읽어 유지하면 색인이 다시 손유지 상태를 품게 되어 이 설계가 없애려던 `SSOT` 위반이 되돌아온다."
    },
    {
      "kind": "gap",
      "where": "「색인과 본문을 가른다」 — \"본문 파일은 **첫 줄이 그 지시사항**이고 그 아래에 나머지가 붙는다.\"",
      "problem": "첫 줄이 없는 파일, 빈 파일, 지시사항이 두 줄로 접힌 파일, `#`가 없는 파일, `.md`가 아닌 파일, 하위 디렉터리, 첫 줄이 같은 두 파일을 어떻게 처리하는지가 없다. 도출 규칙 자체도 예시끼리 어긋난다 — 본문 예시의 첫 줄은 `# ...와일드카드로 읽힌다`인데 색인 예시 줄은 `- ...와일드카드로 읽힌다.`로 `#`가 `-`로 바뀌고 마침표가 붙어 있다. 무엇을 떼고 무엇을 붙이는지가 명시되지 않았다.",
      "evidence": "spec 38행 대 48행, 175-176행(검증 절은 정상 경로만 단언한다)",
      "why_it_matters": "형식이 어긋난 파일 하나가 색인에서 조용히 빠지면 그 교훈은 상시 로드에서 사라지는데 아무도 모른다(`FAIL-LOUD` 위반). 도출 규칙이 예시 수준에서만 정해져 있으면 구현자마다 다르게 만들고, 색인을 두 번 돌려 같은지 보는 검증은 그 차이를 못 잡는다."
    },
    {
      "kind": "contradiction",
      "where": "「정본의 recall 규칙과 머리말 형식 규칙을 고친다」 — \"오답노트 머리말의 형식 규칙 블록도 새 구조에 맞게 전면 개정한다. 개정하면 기존 로그들의 머리말은 배포 후 첫 세션에 스캐폴드가 저절로 갱신한다\" 대 검증 절 \"**옛 형식 로그를 건드리지 않는다**\"",
      "problem": "형식 규칙 블록은 스코프별로 갈리지 않는 상수 하나이고, 머리말 동기화는 본문 폴더가 있든 없든 모든 로그에 걸린다. 그래서 개편을 아직 안 한(또는 거절한) 옛 형식 로그의 머리말에도 '색인에는 지시사항만 둔다'는 새 규칙이 갈아끼워진다. 색인 재생성만 안 걸릴 뿐 머리말은 건드려진다.",
      "evidence": "D:\\projects\\disciplined-coder\\scripts\\_scaffold_common.sh:18-25(`SCAFFOLD_SOLVED_RULES` 단일 상수)·86(스코프와 무관하게 같은 블록을 붙인다) / scaffold.sh:48·59, codex-scaffold.sh:37·47(폴더 유무를 보지 않고 호출) / spec 122-124행 대 180-181행",
      "why_it_matters": "쪼개지지 않은 로그가 지킬 수 없는 형식을 자기 머리말로 선언하게 된다. 그 로그에 새 항목을 적는 세션은 머리말을 정본으로 읽으라고 지시받으므로, 있지도 않은 본문 폴더를 전제한 형식으로 적거나 규칙을 무시한다."
    },
    {
      "kind": "gap",
      "where": "「색인은 손으로 유지하지 않고 본문에서 도출한다」 — \"전역은 `~/.claude/disciplined-coder/` 아래, 프로젝트는 `docs/` 아래다.\"",
      "problem": "관리 디렉터리의 화이트리스트에 `solved_problems` 디렉터리가 없다. 위생 절차는 화이트리스트 밖 디렉터리를 지우지 않는 대신 매 세션 \"비관리 디렉터리 '…' 잔존(자동삭제 안 함, 확인 요)\" 경고를 낸다. spec은 화이트리스트를 고친다는 말을 하지 않는다.",
      "evidence": "D:\\projects\\disciplined-coder\\scripts\\_scaffold_common.sh:6(`SCAFFOLD_WHITELIST=\"agent-principles.md domains-index.md solved_problems.md backups\"`)·40-47",
      "why_it_matters": "설계가 만드는 정상 산출물을 자기 위생 검사가 이상 신호로 매 세션 떠든다. 끄는 수단이 없는 경고라 사용자가 신호 전체를 무시하게 되고, 그러면 진짜 잔존 파일 경고도 함께 묻힌다."
    },
    {
      "kind": "contradiction",
      "where": "「색인은 손으로 유지하지 않고 본문에서 도출한다」 — \"세션 시작 때 스캐폴드가 본문 폴더를 훑어 각 파일의 첫 줄을 모아 색인을 다시 쓴다\"(두 층 모두)",
      "problem": "프로젝트 층에 이것을 걸면 자동 계층이 프로젝트 파일에 손대는 경우가 셋이 된다. 그런데 README가 \"자동 계층이 프로젝트 파일에 손대는 예외는 둘뿐이고, 둘 다 이미 있는 파일을 고치는 것이다(여기가 그 정본이다)\"로 못박고 있고, 정본과 스캐폴드 머리 주석도 같은 말을 한다. spec에는 README를 고친다는 항목이 없다.",
      "evidence": "D:\\projects\\disciplined-coder\\README.md:11 / D:\\projects\\disciplined-coder\\agent-principles.md:158-159 / scripts\\scaffold.sh:2-4 / spec 57행·61-63행",
      "why_it_matters": "사용자에게 약속한 발자국 정본이 조용히 거짓이 된다. 게다가 색인이 아직 없는 프로젝트에서는 '고치는 것'이 아니라 '새로 만드는 것'이 되어, 설치만 해 둔 레포에 파일이 생기지 않는다는 약속까지 깨진다."
    },
    {
      "kind": "gap",
      "where": "「색인은 손으로 유지하지 않고 본문에서 도출한다」와 「옛 형식 로그를 만나면 개편을 권하며 묻는다」 전반",
      "problem": "spec이 '스캐폴드'를 단수로만 부르고 쌍둥이가 둘이라는 사실을 다루지 않는다. 색인 재생성·개편 물음·형식 규칙 개정이 `codex-scaffold.sh`에도 들어가야 하는지, 공통 로직을 `_scaffold_common.sh`에 두는지가 없다. 더구나 Codex 쪽은 색인을 stdout으로 주입하는데, 본문 파일은 주입 대상이 아니고 개편 물음을 띄울 질문 도구도 없다(Codex 스캐폴드의 stdout은 주입 본문 전용이다).",
      "evidence": "D:\\projects\\disciplined-coder\\scripts\\codex-scaffold.sh:31-49·64-79(주석 \"쌍둥이 스크립트는 한쪽만 고치면 반드시 어긋나므로 같이 둔다\") / scripts\\_scaffold_common.sh:2-3 / spec 61행·105-108행",
      "why_it_matters": "한쪽만 고치면 두 런타임의 오답노트 형식이 갈리고, 그것이 바로 머리말 자동 갱신을 도입한 이유였다. 개편 물음을 Codex에서 어떻게 낼지 안 정하면 Codex 사용자는 영영 옛 형식에 남거나, 주입 본문에 질문 문구가 섞여 들어간다."
    },
    {
      "kind": "gap",
      "where": "「색인과 본문을 가른다」 — 색인 줄 예시 \"→ solved_problems/test-path-literalpath.md\"",
      "problem": "포인터가 상대 경로 한 조각이고, 두 층이 같은 문자열을 쓴다. 전역 색인은 `~/.claude/CLAUDE.md`의 `@import`로, Codex에서는 stdout 주입으로 실려 오므로 읽는 쪽에는 그 줄이 어느 뿌리에서 왔는지가 남지 않는다. 프로젝트 색인의 같은 줄과 구별할 방법도 없다. spec은 읽는 쪽이 경로를 어떻게 도출하는지 정하지 않는다.",
      "evidence": "spec 47-50행 / scripts\\scaffold.sh:93-97(`@disciplined-coder/solved_problems.md` 주입) / scripts\\codex-scaffold.sh:74(색인을 stdout으로 그대로 cat)",
      "why_it_matters": "색인 줄에 걸린 세션이 본문을 열려다 프로젝트 `docs/solved_problems/` 아래에서 찾아 실패하거나, 반대로 전역 본문을 프로젝트 것으로 착각한다. 상시 로드 절감이라는 이득은 '걸리면 그 파일 하나만 연다'가 성립할 때만 나온다."
    },
    {
      "kind": "contradiction",
      "where": "「색인은 손으로 유지하지 않고 본문에서 도출한다」 — 스캐폴드가 색인을 다시 쓴다",
      "problem": "정본과 머리말 템플릿은 오답노트를 쓰는 주체를 메인 세션 하나로 못박고 있다(\"solved는 메인 세션만 쓴다(동시 쓰기 손상 방지)\", 머리말의 \"메인 세션만 기록\"·\"메인 세션만 append\"). 이제 훅이 그 파일을 쓴다. spec은 정본에서 recall 규칙만 고친다고 했지 작성자 규칙은 다루지 않는다. 동시에 열리는 세션이 같은 전역 색인을 함께 다시 쓸 때의 잠금도 정하지 않았다(관리블록 주입에는 잠금이 있다).",
      "evidence": "D:\\projects\\disciplined-coder\\agent-principles.md:138-139 / scripts\\_scaffold_common.sh:69·77 / README.md:13(관리블록 쪽 잠금 언급) / spec 117-124행",
      "why_it_matters": "정본이 금지한 동시 쓰기를 설계가 스스로 도입한다. 워크트리를 여러 개 띄우는 3층 오케스트레이션이 이 레포의 상시 관례이므로, 여러 세션이 같은 시각에 시작해 전역 색인을 반쯤 쓴 상태로 덮을 수 있다."
    },
    {
      "kind": "gap",
      "where": "「색인은 손으로 유지하지 않고 본문에서 도출한다」 — \"본문 파일을 하나 만드는 것이 곧 등록이고, 색인은 다음 세션 시작에 따라온다.\"",
      "problem": "프로젝트 층에서는 이 문장이 성립하지 않는다. 스캐폴드는 `docs/solved_problems.md`가 파일로 있을 때만 그 프로젝트를 본다. 색인이 아직 없는 레포에서 본문 파일만 만들면 다음 세션에도 아무 일이 일어나지 않는다. 정본의 recall도 \"레포에 `docs/solved_problems.md`가 있으면 있는 것으로 치되… 파일 존재로 도출한다\"로 파일 하나만 보므로, 폴더만 있고 색인이 없는 상태를 어떻게 도출할지가 비어 있다.",
      "evidence": "D:\\projects\\disciplined-coder\\scripts\\scaffold.sh:55-61 / scripts\\codex-scaffold.sh:43-49 / agent-principles.md:122 / spec 65-66행",
      "why_it_matters": "프로젝트 고유 교훈을 처음 적는 회차에 등록이 조용히 실패한다. 적은 사람은 등록했다고 믿고, 색인에는 영영 안 올라오며, recall은 그 교훈을 못 꺼낸다."
    },
    {
      "kind": "contradiction",
      "where": "「옛 형식 로그를 만나면 개편을 권하며 묻는다」 — \"첫 항목이 \\\"지금 개편한다\\\"이고 그것이 권장값이다… 한 번 승낙하면 끝나는 일이다.\"",
      "problem": "같은 spec이 개편의 핵심 작업을 기계가 못 한다고 적었다 — \"79개의 첫 줄을 증상에서 지시사항으로 바꾸는 일은 기계가 못 한다. 뜻을 옮기는 작업이라 위험이 있으므로\". 그렇다면 승낙 한 번으로 끝나는 것은 파일 쪼개기까지이고, 남의 프로젝트 로그에서 지시사항 줄을 누가 언제 쓰는지가 정해지지 않았다. 승낙 뒤 상태(쪼개졌으나 첫 줄이 아직 증상인 본문)를 어떻게 부르고 어떻게 다루는지도 없다.",
      "evidence": "spec 95-97행 대 106-107행·112행",
      "why_it_matters": "권장값을 누르면 세션이 그 자리에서 79개(혹은 그 프로젝트의 항목 수)를 다시 쓰려 들거나, 반쯤 개편된 로그가 남는다. 반쯤 개편된 로그는 색인이 증상으로 채워져 이 설계의 목적인 '시작할 때 걸리는 지시'가 안 만들어진다."
    },
    {
      "kind": "gap",
      "where": "「색인은 손으로 유지하지 않고 본문에서 도출한다」와 「기존 79개를 옮기고…」 — \"이관 전에 두 로그의 사본을 백업 디렉터리에 뜬다. 사본을 못 뜨면 이관하지 않는다.\"",
      "problem": "이관과 개편의 사본은 정했으나, 매 세션 도는 색인 재생성이 사본을 뜨는지는 정하지 않았다. 머리말 갱신의 규율은 '고치기 전에 타임스탬프 사본을 먼저 뜨고, 회전이나 상한을 두지 않는다'이다. 그 규율을 매 세션 재생성에 그대로 적용하면 백업이 무한히 쌓이고, 적용하지 않으면 소유자 문서(domain-docs)의 규율에서 예외가 하나 생기는데 그 예외를 어디에 적을지가 없다.",
      "evidence": "D:\\projects\\disciplined-coder\\skills\\domain-docs\\SKILL.md:125-127 / scripts\\_scaffold_common.sh:159-166 / spec 99행·114-115행·175-179행",
      "why_it_matters": "세션마다 색인 사본이 하나씩 쌓여 관리 디렉터리가 커지거나, 반대로 사본 없이 덮어써서 사람이 색인에 적어 둔 줄을 되돌릴 수단이 사라진다(첫째 발견과 겹쳐 손실이 확정된다)."
    },
    {
      "kind": "gap",
      "where": "「정본의 recall 규칙과 머리말 형식 규칙을 고친다」 — \"새 구조에 맞게 전면 개정한다\"",
      "problem": "새 색인과 새 본문 파일이 `domain-docs`의 수정 규율 표에서 어느 칸에 드는지를 정하지 않았다. 지금 표는 오답노트를 통째로 append-only로 분류하고, 머리말 템플릿도 제목 줄에서 스스로를 \"append-only 오답노트\"로 선언한다. 새 구조에서는 색인이 generated이고 본문이 append-only에 가까워 한 파일 이름 아래 두 규율이 섞인다. spec이 고친다고 적은 것은 '형식 규칙 블록'뿐이라 제목 줄과 스코프 문단, 그리고 domain-docs 표는 손대는 목록에도 안 하는 목록에도 없다.",
      "evidence": "D:\\projects\\disciplined-coder\\skills\\domain-docs\\SKILL.md:83·97-102·111 / scripts\\_scaffold_common.sh:64-79 / spec 122-124행",
      "why_it_matters": "문서가 자기 수정 규율을 선언하면 그 선언이 곧 기계 강제의 계약이라는 것이 이 레포의 규칙이다. 색인이 스스로 append-only라고 선언한 채 매 세션 덮어써지면, 그 선언을 믿고 색인에 줄을 더한 사람이나 세션이 손실을 겪는다."
    },
    {
      "kind": "drift",
      "where": "spec 전반 — \"색인에는 지시사항만 두고\"(27행), \"지시사항 줄을 새로 쓴다\"(87행), \"색인 줄만 보고 열지 말지 정하지 못하면\"(165행), \"다음 실행의 색인 줄이 따라 바뀌고\"(175행)",
      "problem": "같은 대상을 지시사항·지시사항 줄·색인 줄 세 이름으로 부른다. 본문 쪽도 본문·본문 파일·\"한 항목 한 파일\"의 항목이 섞이는데, 머리말 형식 규칙에서 '항목'은 로그 안의 한 덩어리를 가리키던 말이라 검증 절의 \"머리말 형식 규칙 갱신이 항목을 보존한다\"가 색인 줄을 말하는지 본문 파일을 말하는지 갈린다. 작업 이름도 쪼개기·이관·다시 쓰기·개편이 겹쳐 쓰인다.",
      "evidence": "spec 27·32·35·87·95·165·175·183행 / scripts\\_scaffold_common.sh:22-25(같은 '항목'을 다른 뜻으로 쓰는 정본 문구)",
      "why_it_matters": "정본은 한 개념에 한 용어만 쓰라고 못박고 있고, 이 문서는 곧 구현자와 테스트 이름의 입력이 된다. 이름이 갈린 채로 계약을 쓰면 '항목 보존' 테스트가 무엇을 지키는 것인지 서로 다르게 구현된다."
    },
    {
      "kind": "gap",
      "where": "「검증」 마지막 문단 — \"지금 `test_scaffold.sh`가 오답노트를 서른두 곳에서 잡고 있어 그만큼 함께 손본다.\"",
      "problem": "그 수치를 재현할 수 없다. `solved`를 담은 줄은 48개, `solved`를 담은 `check` 줄은 12개, 파일 전체 `check`는 197개, 오답노트 구역(514행 이후)의 `check`는 59개다. 무엇을 한 곳으로 세는지 spec이 적지 않았다.",
      "evidence": "D:\\projects\\disciplined-coder\\scripts\\test_scaffold.sh(전체 743행) — `grep -c solved`=48, `grep '^check' | grep -c solved`=12, `grep -c '^check'`=197, 514행 이후 `check`=59",
      "why_it_matters": "손볼 범위를 이 숫자로 잡으면 구현자가 자기 나름의 세는 법을 정하고, 세지 않은 계약이 남는다. 이 레포는 테스트 기대치에 숫자를 박는 것을 이미 금지하고 있으므로 세는 법 없는 개수는 그대로 부채가 된다."
    },
    {
      "kind": "gap",
      "where": "「파일 이름은 아는 것에만 날짜를 붙인다」 — \"이름은 `<날짜>-<짧은 영문>.md`이되\"",
      "problem": "핸드오프는 본문 파일 이름을 아직 못 정한 사항으로 남기며 증상에서 뽑은 짧은 한글(`파워셸-테스트가-회귀를-심어도-초록.md`)을 제안해 두었고 그 이유(폴더만 열어도 무엇이 있는지 보인다)까지 적었다. spec은 영문으로 정하면서 그 안을 왜 기각했는지, 사용자가 답했는지를 적지 않는다. 또한 이름에 날짜를 붙이면서 정렬에는 이름을 쓰지 않겠다고 해, 이름 속 날짜가 무엇에 쓰이는지가 남지 않는다.",
      "evidence": "D:\\projects\\disciplined-coder\\docs\\HANDOFF.md:35-38 / spec 68행 대 72행·80-85행(예시 목록은 날짜 없는 파일이 먼저 오는 이름순 배열로 보인다)",
      "why_it_matters": "기각한 대안을 남기지 않으면 다음 세션이 같은 논의를 다시 열거나, 설계 근거를 남기라는 이 문서 유형의 몫이 비어 버린다. 날짜가 정렬에 안 쓰인다면 이름 규칙 자체가 무엇을 위한 것인지 다시 정해야 한다."
    },
    {
      "kind": "scope",
      "where": "spec 전체 — 「기존 79개를 옮기고 지시사항 줄을 새로 쓴다」·「옛 형식 로그를 만나면 개편을 권하며 묻는다」·「정본의 recall 규칙과 머리말 형식 규칙을 고친다」·「검증」",
      "problem": "한 문서에 성격이 다른 다섯 덩어리가 묶여 있다. 사람이 79개를 손으로 다시 쓰는 이관, 쌍둥이 스캐폴드에 색인 재생성 붙이기, 남의 프로젝트에 개편을 묻는 대화 흐름, 정본과 머리말 정본 개정, 그리고 테스트 대개편이다. 되돌리기 어려운 것(79개 재작성)과 되돌리기 쉬운 것(스캐폴드 코드)이 한 계획에 섞여 있다.",
      "evidence": "spec 87-99행·101-115행·117-124행·171-186행",
      "why_it_matters": "한 계획으로 실행하면 사람이 하는 재작성이 끝날 때까지 기계 쪽 계약을 검증할 수 없고, 중간에 멈추면 색인 재생성과 옛 형식 로그가 섞인 상태로 배포된다. 쪼개면 스캐폴드와 형식 규칙을 먼저 잠그고 이관을 뒤에 둘 수 있다."
    },
    {
      "kind": "scope",
      "where": "「안 하는 것」 — \"**선행연구 회피목록은 별도 작업이다.** … 이 문서의 범위 밖이므로 잊지 않도록 여기 적어 둔다.\"",
      "problem": "정본은 미해결을 넷 중 하나로 처분하라고 정했고(지금 한다·메모리에 적고 알린다·🔴로 즉시 알린다·한 번 알리고 놓아준다), 설계 문서에 적어 두는 것은 그 넷에 없다. 게다가 설계 문서는 배포된 뒤 지우고 git 이력이 갈음하는 타입이라, 잊지 않으려 적은 자리가 배포 후 사라지는 자리다.",
      "evidence": "D:\\projects\\disciplined-coder\\agent-principles.md:123-129 / skills\\domain-docs\\SKILL.md:81(설계 타입의 수명) / spec 145-150행",
      "why_it_matters": "잊지 않으려던 것이 spec과 함께 사라지거나, 반대로 spec이 손유지 백로그 노릇을 하며 남는다. 둘 다 이 레포가 없애려던 실패 모드다."
    }
  ],
  "principles_applied": ["SSOT", "IDEMPOTENT", "FAIL-LOUD", "EXPLICIT", "MEASURE-FIRST", "NAME-ITEMS", "SIMPLE", "REVERSIBLE", "CLEAR-COMM", "READ-FLOW", "ASK-FORK", "TDD", "NO-PRIORITY", "SURGICAL"],
  "notes": "대조해 확인한 것 — spec이 '이미 반영한 것'으로 적은 둘은 실제 레포 상태와 맞는다. 머리말 문구 완화는 scripts/_scaffold_common.sh:25에 '안 쓰이는 항목도 지우지 않는다 — 사용자가 직접 지시할 때만 손댄다'로 들어가 있고(작업 트리 수정본, 아직 커밋 전), `/show-solved`는 `git status`에서 `D commands/show-solved.md`로 스테이지되어 있으며 commands/에는 setup-discipline.md와 show-principles.md만 남고 README 커맨드 절에도 안 남아 있다. 다만 두 변경 모두 아직 커밋되지 않았으므로 배포 상태로 단정하지 말 것. spec의 실측 수치도 재현해 확인했다 — agent-principles.md 14,571자, domains-index.md 605자, 이 레포 docs/solved_problems.md 9,816자에 항목 27개로 spec 값과 정확히 일치한다.\n\n확인하지 못한 것과 그 이유 — 첫째, 전역 오답노트(52개·20,580자·증상 줄 합계 6,622자)는 이 환경에서 `~/.claude/disciplined-coder/`를 읽지 못해 대조하지 못했다. 절감폭 추정과 '45개가 한 달 구간에 뭉친다'는 근거가 그 파일에 걸려 있으므로, 사용자 쪽에서 그 로그의 항목 수와 자 수를 한 번 세어 주면 이관 계획의 전제를 확정할 수 있다. 둘째, 색인 재생성과 머리말 갱신 중 무엇이 먼저 도는지에 따라 다섯째 발견(옛 형식 로그에 새 규칙이 갈아끼워지는 문제)의 크기가 달라지는데, spec에 순서가 없어 코드로도 확인할 수 없었다 — 설계자에게 그 순서를 물어야 한다. 셋째, `_managed_block.sh`의 잠금이 색인 재생성에도 재사용 가능한지는 그 파일을 읽지 않아 모른다(이 렌즈의 읽기 범위는 spec이 이름을 부른 파일까지다)."
}
```
