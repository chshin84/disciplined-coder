```json
{
  "lens": "adversarial",
  "read": [
    "docs/superpowers/specs/2026-08-17-purpose-first-reorientation-design.md",
    "agent-principles.md",
    "skills/reviewer-adversarial/SKILL.md",
    "skills/meta-aggregate/SKILL.md",
    "skills/domain-docs/SKILL.md",
    "scripts/test_docs_drift.sh",
    "scripts/test_scaffold.sh",
    "hooks/doc_review_posttooluse.sh",
    "README.md",
    "docs/DESIGN-NOTES.md"
  ],
  "issues": [
    {
      "where": "위험과 대가 — '압축하다 규칙이 사라질 수 있다' 및 됐다는 것을 어떻게 아나 — '앞은 스크립트가 세고 뒤는 사람이 대조한다'",
      "type": "failure-mode",
      "claim": "설계가 스스로 가장 큰 위험이라고 적은 규칙 소실에 대해, 완화책이 '고칠 때마다 사람이 원문과 새 문장을 나란히 놓고 확인한다' 하나뿐이다. 기계가 붉어지는 자리가 없다.",
      "consequence": "상시 로드되는 agent-principles.md에서 규칙 한 줄이 압축 중에 빠져도 아무 검사도 실패하지 않는다. 그 정본은 ~/.claude/CLAUDE.md의 @import로 모든 프로젝트에 매 세션 실리므로, 빠진 규칙은 즉시 전체 환경에서 사라지고 문서를 다시 통째로 읽는 사람이 나올 때까지 발견되지 않는다. 문서 자신이 '아무도 눈치채지 못한 채 오래 간다'고 적은 바로 그 상태가 완화 후에도 그대로 남는다.",
      "evidence": "agent-principles.md의 FAIL-LOUD는 '구조 — 불변식, 생성된 설정, 명확한 계약 — 가 실수를 막거나 드러내게 만들어, \"작성자가 정확히 기억하기\"에 의존하지 않는다'고 적는다. 같은 파일의 문서·상태 위생 절도 '상태와 문서는 기억해서 갱신으로 유지되지 않는다 — 그게 깨지는 방식이다'라고 적는다. 이 설계의 완화책은 정확히 그 방식이다."
    },
    {
      "where": "위험과 대가 — '테스트 앵커가 붉어진다 … 앵커를 새 문구로 맞추면 되지만 건수가 많다'",
      "type": "failure-mode",
      "claim": "앵커 수리를 분량 문제로만 적었으나, 실제로는 앵커를 새 문구에 맞추는 행위가 테스트의 판별력을 떨어뜨린다. 특히 줄 단위·절 단위로 범위를 좁혀 놓은 앵커는 불릿을 문단으로 푸는 순간 내용이 멀쩡해도 깨지고, 자연스러운 수리는 범위를 넓히는 쪽이다.",
      "consequence": "CLEAR-COMM 안에 그 규칙이 있다는 불변식이 '이 문자열이 파일 어딘가에 있다'로 내려앉는다. 그러면 압축 과정에서 규칙이 다른 절로 흘러가거나 맥락을 잃어도 초록으로 남는다. 게다가 앵커를 고치는 주체와 문서를 압축하는 주체가 같은 세션의 같은 사람이라, 오라클과 검사 대상이 한 손에서 동시에 움직인다. 그 결과 168군데를 다 초록으로 되돌린 뒤에도 '무엇이 빠졌는가'에 대해 테스트가 아무 말도 하지 않는다.",
      "evidence": "scripts/test_scaffold.sh:314-320은 CC_LINE=\"$(grep -F '**`CLEAR-COMM`' \"$CANON\")\"로 한 줄을 뽑아 그 줄 안에서 '질문 도구로 선택지를 띄운다'와 '답할 자리인지'를 단언한다. 이 설계는 그 22문장 불릿을 문단으로 되돌리겠다고 적었으므로 두 단언은 내용 손실 없이도 실패한다. 그리고 같은 파일 :290-291이 '파일 전역 grep이 아니라 절만 뽑아 그 안에서 본다 — 각각 어딘가에 남아 있으면 통과해 버리는 항진을 막는다'고 이미 경고한다."
    },
    {
      "where": "자 — '파일 하나를 지우면 되돌아가므로 되돌리기 쉬운 결정이다(REVERSIBLE)' 대 위험과 대가 절 전체",
      "type": "irreversible",
      "claim": "가역성을 논한 자리가 파이썬 스크립트 한 곳뿐이다. 세 부분 가운데 되돌리기 어려운 것은 스크립트가 아니라 문서 열여섯 개의 압축이며, 문서가 그 대가를 가역성의 언어로 적지 않았다.",
      "consequence": "스크립트는 정말로 파일 하나를 지우면 끝나지만, 압축된 정본은 지운다고 돌아오지 않는다. 원문 문구가 사라지고 그것을 가리키던 앵커까지 새 문구로 갈아 놓으면, 나중에 '이 규칙이 원래 있었는가'를 판단할 기준점이 레포 안에 하나도 남지 않고 git 이력을 뒤지는 수밖에 없다.",
      "evidence": "agent-principles.md의 REVERSIBLE은 '되돌리기 어려운 결정(단방향 문)은 신중히 하고 그 근거를 남긴다'고 요구한다. 설계 문서에서 REVERSIBLE이 인용된 곳은 스크립트 적재 한 줄뿐이고, 열여섯 문서 압축과 168군데 앵커 갱신은 '건수가 많다'·'설계된 대가'로만 적혀 있다."
    },
    {
      "where": "계획을 어떻게 나누나 — '자를 먼저 들이고 … 재조율 순서를 정하고, 렌즈는 재조율된 문서를 첫 시험대로 쓴다'",
      "type": "failure-mode",
      "claim": "가장 위험한 작업인 정본 압축이 렌즈가 존재하기 전에 끝난다. 그런데 문서 스스로 자만으로는 판정이 안 된다고 적었고 임계값도 두지 않기로 했으므로, 재조율 단계에는 해석자가 없다.",
      "consequence": "재조율은 해석 기준이 없는 숫자와 사람의 감만으로 진행되고, 렌즈가 나중에 완성돼 무언가를 잡아내도 그때는 이미 원문이 압축된 뒤라 고칠 대상이 남아 있지 않다. 렌즈의 첫 시험대가 '이미 이 방식으로 고쳐 놓은 문서'라는 점도 문제다. 렌즈가 그 문서에서 지적을 못 내는 것이 렌즈가 무딘 탓인지 문서가 좋아진 덕인지 구별되지 않는다.",
      "evidence": "세 부분 절은 '눈이 없으면 자가 센 숫자를 아무도 해석하지 못한다'고 적는다. 자 절은 '자는 판정하지 않는다', 안 하는 것 절은 '임계값을 정하지 않는다. 판정은 목적이 한다'고 적는다. 목적에 비추는 일은 렌즈의 첫 단계인데 그 렌즈가 재조율보다 뒤에 온다."
    },
    {
      "where": "자 — '여섯 다 실제로 우리 문서의 병을 하나씩 잡아냈다' 및 됐다는 것을 어떻게 아나 — '이미 네 축에서 독립 재현되었다'",
      "type": "over-engineering",
      "claim": "여섯 축 가운데 근거가 실제로 붙은 것은 일부다. 재현 대조에 나온 넷 중 '번호 붙은 제목'은 여섯 축 목록에 없는 항목이고, 여섯 축 중 목록 항목 문장 수·괄호 밀도·문장 길이 퍼짐은 재현 목록에 없다. 문장 길이 퍼짐은 문서 어디에도 그것이 잡아낸 병의 사례가 없다.",
      "consequence": "'여섯 다 병을 하나씩 잡아냈다'가 검증 절과 어긋나므로, 축을 줄일 근거를 검토자가 판단할 수 없다. 특히 문장 길이 퍼짐은 임계값을 두지 않기로 한 설계에서 해석 근거가 전무한 숫자로 남아, 절마다 붙어 나오면서 렌즈가 봐야 할 신호를 희석한다.",
      "evidence": "자 절이 세는 여섯은 통굵 비율, 블록 교대, 문단 이음, 목록 항목 문장 수, 괄호 밀도, 문장 길이 퍼짐이다. 시제품 재현으로 대조했다고 적은 넷은 굵은 조각 204/205, 블록 134/134, 이음말 문단 2/2, 번호 붙은 제목 35/35이다. 마지막 항목은 여섯 축에 없다."
    },
    {
      "where": "렌즈 — '구조를 본다' 단계 및 고쳐 주는 범위 — '구조는 제안까지만 하고'",
      "type": "over-engineering",
      "claim": "네 단계 중 구조 단계가 지금 값을 하는지 문서 자체로 확인되지 않는다. 산출은 제안뿐이고, 반영에는 사람 승인과 메인 세션의 원문 대조가 다시 필요하며, 성공 기준에는 구조와 관련된 항목이 하나도 없다.",
      "consequence": "문서 한 건을 검진할 때마다 목차와 절별 첫 문단을 실어 콜을 한 번 더 태우는데, 그 산출이 통과와 실패를 가르지 않는다. 검증되지 않는 단계가 늘 돌면 비용은 확정이고 효과는 미확정이다. 게다가 이 단계의 산출을 반영하는 경로가 설계의 최대 위험(압축 중 내용 소실)과 정확히 같은 경로여서, 값을 못 하는 단계가 가장 위험한 작업을 새로 만들어 낸다.",
      "evidence": "됐다는 것을 어떻게 아나 절이 렌즈에 요구하는 것은 셋뿐이고 전부 축 수치다. agent-principles.md의 SIMPLE은 '단일 호출로 충분한 일을 굳이 에이전트 시스템으로 키우지 않는다 — 지연과 비용이라는 대가를 따져본다'고 적는다."
    },
    {
      "where": "고쳐 주는 범위 — '어색한 문장은 사람이 읽으면 바로 판정된다' 대 손대지 않는 것 — '숫자는 그대로라 다른 가드를 통과한다'",
      "type": "failure-mode",
      "claim": "절 안 자동 수정을 허용하는 근거가 바로 다음 절의 실측 서술에 반박된다. 완화 표현 삭제는 읽어서는 안 걸리는 종류의 오염인데, 문서는 '읽으면 바로 판정된다'를 자동 수정 허용의 근거로 삼는다.",
      "consequence": "렌즈가 '~할 수 있다'를 지운 문장은 매끄럽게 읽히므로 사람 검토를 통과하고, 숫자와 고유명사가 그대로라 다른 가드도 통과한다. 그 결과 원문보다 단정적인 주장이 문서에 남는다. 절 안 수정에는 구조 변경과 달리 사람 승인도 원문 대조도 요구하지 않으므로, 이 오염을 걸러 낼 지점이 설계 안에 없다.",
      "evidence": "손대지 않는 것 절이 '이 위험은 가정이 아니라 실측이다'라며 im-not-ai 규칙집의 다섯 규칙 중 하나가 시험한 인수인계 문서에서 이미 발동 문턱을 넘었다고 적는다. 같은 문서의 고쳐 주는 범위 절은 그 위험을 인지하기 직전 문단에서 '사람이 읽으면 바로 판정된다'를 근거로 자동 수정을 연다."
    },
    {
      "where": "손대지 않는 것 — 넷을 막는다 및 렌즈 — 절별 수정",
      "type": "risk",
      "claim": "네 가지 금지를 강제하는 것이 렌즈 프롬프트의 문장뿐이고, 렌즈가 그 금지를 어겼는지 확인하는 주체가 설계에 지정되어 있지 않다.",
      "consequence": "금지를 어긴 수정이 그대로 반영된다. 특히 '숫자·부호·고유명사·식별자를 한 글자도 바꾸지 않는다'는 기계로 대조 가능한 성질인데도 확인 주체가 없어, 렌즈가 자기 금지를 지켰다고 보고하면 그대로 믿는 구조가 된다. 자기가 고치고 자기가 지켰다고 말하는 자리를 남기면, 그 렌즈는 단독 LLM 출력으로 끝나는 것과 같아진다.",
      "evidence": "agent-principles.md의 검증 레이어 절은 'LLM이 만든 결과는 단독 결론으로 마치지 말고 리스크에 비례해 리뷰를 더한다'고 적는다. 이 설계에서 리뷰가 붙는 것은 구조 제안뿐이고, 절 안 수정과 네 금지의 준수 여부에는 아무 검증 단계도 붙지 않는다."
    },
    {
      "where": "언제 부르나 — '문서 검진에서 자동으로 부르고 … 애매하면 건다' 및 위험과 대가 — '검진이 여러 번 돈다'",
      "type": "over-engineering",
      "claim": "위험 절이 재조율 기간의 일회성 비용(열여섯 번)만 세고, 이 렌즈가 영구적으로 더하는 상시 비용은 세지 않는다.",
      "consequence": "문서 검진의 렌즈가 둘에서 셋으로 늘고, 새 렌즈만은 한 번의 콜이 아니라 목적·자·구조·절별로 여러 콜이며, 적용 대상은 '애매하면 건다'이고 훅은 spec·plan과 리뷰 기록을 뺀 모든 .md에서 뜬다. 그래서 문서 한 건을 고칠 때마다 드는 콜 수가 지금의 몇 배가 되는데, 그 증가분이 설계 어디에도 적혀 있지 않아 검토자가 대가를 저울에 올릴 수 없다.",
      "evidence": "skills/domain-docs/SKILL.md:134-141이 지금의 문서 검진을 reviewer-grounding과 reviewer-fit 두 렌즈, 각 한 번으로 정한다. hooks/doc_review_posttooluse.sh:13-17은 .md 전부에서 뜨고 spec/plan과 리뷰 기록만 뺀다."
    },
    {
      "where": "언제 부르나 — '발표자료 문안이 여기 든다', '붙여넣은 글에도 쓰인다'",
      "type": "over-engineering",
      "claim": "밖에서 받은 글과 발표자료 문안 경로가 지금 필요하지 않은 채로 열려 있다. 이 경로들에는 검증할 성공 기준이 하나도 붙어 있지 않다.",
      "consequence": "붙여넣은 남의 글에는 렌즈가 목적을 뽑아 올 첫머리도 없고 사람이 목적을 대 준다는 보장도 없는데, 문서 스스로 '짐작한 목적으로 판정하면 그때부터 오염이다'라고 적었다. 그리고 숫자·고유명사 보존 위험은 우리가 쓰지 않은 문서에서 더 크다. 검증되지 않은 경로를 v1에 함께 열면, 그 경로에서 처음 사고가 났을 때 렌즈 전체의 신뢰가 같이 깎인다.",
      "evidence": "됐다는 것을 어떻게 아나 절의 렌즈 기준은 우리 인수인계 문서 하나에서 잡아야 할 세 가지뿐이다. 밖에서 받은 글이나 발표자료 문안에 대해 무엇이 '됐다'인지는 문서 어디에도 없다."
    },
    {
      "where": "됐다는 것을 어떻게 아나 — '그리고 넘긴 것도 함께 센다'",
      "type": "failure-mode",
      "claim": "넘긴 것을 세는 방법이 없어 이 문장이 선언에 머문다. 넘긴 것을 세려면 그 문서에 있는 방해 요소의 전체 목록이 먼저 있어야 하는데, 그 목록을 만드는 것도 같은 자와 같은 눈이다.",
      "consequence": "렌즈가 문서에 이미 이름이 적힌 세 가지만 재현하면 성공 기준이 통과한다. 그래서 이 기준은 통과만 낼 수 있고 실패를 낼 수 없으며, 문서가 스스로 경계한 상태를 그대로 유지한다. 더구나 시험 대상인 인수인계 문서와 판정하는 렌즈가 같은 계열의 모델에서 나오는데, 그 편향을 끊는 장치는 설계에 없다.",
      "evidence": "성공 기준이 잡아야 한다고 든 셋은 전부 설계 문서 본문에 수치까지 적혀 있다. agent-principles.md의 TDD는 '무엇이 됨인지 검증 가능한 성공 기준을 미리 정한다'고 요구하는데, 넘긴 것을 세는 절차가 없어 이 항목은 검증 가능하지 않다."
    },
    {
      "where": "렌즈 — 이름 근거 및 안 하는 것 — 훅을 새로 만들지 않는다",
      "type": "failure-mode",
      "claim": "skills/reviewer-readability/가 생기는 순간 이름 규칙에 묶인 세 가지 열거 불변식이 깨진다. 설계는 앵커가 붉어지는 이유를 '산문 문구를 앵커로 잡아서'로만 적었고, 디렉터리 집합을 진실로 삼는 이 검사들과 meta-aggregate를 한 번도 언급하지 않는다.",
      "consequence": "test_docs_drift.sh가 README 트리 주석과 meta-aggregate의 source 열거를 렌즈 디렉터리 집합과 대조하므로 둘 다 실패한다. 그 실패를 없애려면 meta-aggregate의 source에 readability를 넣어야 하는데, 그러면 고친 문장과 구조 제안을 돌려주는 렌즈가 그것을 담을 필드가 없는 공통 계약에 등재된다. 집계 단계가 이 렌즈의 실제 산출을 받아 낼 수 없는 상태로 이름만 올라간다.",
      "evidence": "scripts/test_docs_drift.sh:22-23이 skills/reviewer-*/에서 이름을 도출하고 :49가 README 트리 주석과, :58이 meta-aggregate의 source 열거와 일치를 단언한다. 현재 값은 README.md:76의 (grounding/fit/consistency/adversarial/prior-art)와 skills/meta-aggregate/SKILL.md:23이다."
    }
  ],
  "principles_applied": ["SIMPLE", "REVERSIBLE", "FAIL-LOUD", "TDD", "SSOT", "MEASURE-FIRST"],
  "notes": "기능 추가는 제안하지 않았다 — 발견은 전부 단순화 근거이거나 이대로 두면 터지는 위험이다. 세 부분을 계획으로 쪼개는 문제는 문서가 이미 따로 쓰겠다고 답했으므로 올리지 않았고, 대신 그 순서가 만들어 내는 실패를 '렌즈 없이 정본을 압축한다'로 올렸다."
}
```
