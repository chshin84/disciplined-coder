```json
{
  "lens": "adversarial",
  "read": [
    "D:/projects/disciplined-coder/skills/reviewer-adversarial/SKILL.md",
    "D:/projects/disciplined-coder/agent-principles.md",
    "D:/projects/disciplined-coder/skills/meta-aggregate/SKILL.md",
    "D:/projects/disciplined-coder/docs/superpowers/specs/2026-08-17-purpose-first-reorientation-design.md",
    "D:/projects/disciplined-coder/skills/domain-docs/SKILL.md",
    "D:/projects/disciplined-coder/skills/domain-spec-review/SKILL.md",
    "D:/projects/disciplined-coder/scripts/test_docs_drift.sh",
    "D:/projects/disciplined-coder/hooks/doc_review_posttooluse.sh",
    "D:/projects/disciplined-coder/docs/DESIGN-NOTES.md",
    "D:/projects/disciplined-coder/README.md"
  ],
  "issues": [
    {
      "where": "'렌즈 — 목적에 비추어 보는 눈'의 목적 정하기 단계(100~101줄)와 '언제 부르나'의 자동 호출(159줄)",
      "type": "failure-mode",
      "claim": "목적을 정하는 두 경로 가운데 자동 호출 경로에는 사람이 없는데, 그때 목적을 어디서 얻을지가 정해져 있지 않다. 설계 전체가 '판정은 목적이 한다'에 걸려 있으므로 이 공백은 판정 근거 자체의 공백이다.",
      "consequence": "문서 검진 훅이 부르는 경로에서는 사람이 한 줄을 적어 줄 자리가 없어 '문서 첫머리에서 뽑는다'만 남는데, 목적을 첫머리에 적지 않는 문서(대부분의 README와 인수인계 문서)에서는 뽑을 것이 없다. 그러면 렌즈는 멈추거나 스스로 짐작하는데, 설계는 짐작을 금지만 하고 짐작이 일어났는지 관측할 수단을 두지 않았다. 결과적으로 짐작한 목적으로 문장까지 고쳐 온 결과물이 오염 표시 없이 들어온다.",
      "evidence": "본문 100~101줄이 '사람이 정하거나 문서 첫머리에서 뽑는다. 렌즈가 짐작하게 두지 않는다 — 짐작한 목적으로 판정하면 그때부터 오염이다'라고 적고, 159줄이 '문서 검진에서 자동으로 부르고'라고 적는다. 그 검진은 hooks/doc_review_posttooluse.sh:30이 비블로킹 넛지로 띄우는 자동 경로여서 사용자에게 목적을 묻는 단계가 없다. 두 문장 사이를 잇는 규정이 본문에 없다."
    },
    {
      "where": "'파이썬 의존이 새로 생긴다'(89~91줄, 225~226줄)와 렌즈 두 번째 단계(103~104줄)",
      "type": "failure-mode",
      "claim": "파이썬이 없을 때의 대안이 '수치 없이 렌즈만 돌린다'인데, 이 설계는 바로 그 조합을 실패라고 이미 선언해 두었다.",
      "consequence": "수치가 없으면 어느 절을 볼지 정하는 기준이 사라지므로 렌즈는 감으로 절을 고른다. 그런데 보고에 적히는 것은 '수치가 없었다'는 사실뿐이라, 판정 방식이 설계가 배제한 방식으로 바뀌었다는 더 중요한 사실은 드러나지 않는다. 파이썬이 없는 환경에서는 렌즈가 계속 돌면서 검진을 받았다는 기록만 쌓인다.",
      "evidence": "본문 65~66줄이 '자가 없으면 눈이 감으로 판정하고'라고 적어 셋이 서로를 필요로 하는 이유로 든다. 그런데 90줄은 파이썬 부재 시 '수치 없이 렌즈만 돌린 뒤 그 사실을 보고에 적는다'로 처리한다. 103~104줄은 '어느 절을 볼지는 여기서 정해진다', 106줄과 111줄은 구조와 절 단계에 '수치를 함께 넘긴다'를 전제한다."
    },
    {
      "where": "'재조율'의 성공 기준(236~237줄)과 '자는 판정하지 않는다'(77~79줄)",
      "type": "risk",
      "claim": "자는 판정하지 않는다고 선언해 놓고 재조율의 성공 기준으로 '자로 잰 값이 내려가야 한다'를 쓴다. 그 순간 자는 관찰값이 아니라 목표가 된다.",
      "consequence": "여섯 축은 모두 무언가를 지우면 기계적으로 내려간다. 굵은 글씨를 지우고 괄호를 지우고 표를 없애면 값은 확실히 내려가지만 강조와 부연이 함께 사라진다. 자는 그 손실을 못 재므로, 전달이 나빠진 편집도 성공으로 기록된다. 특히 괄호 밀도 축은 원칙이 요구하는 용어 부연과 정면으로 맞선다 — 부연을 괄호로 다는 관례가 이 레포 문서 전반의 형태다.",
      "evidence": "본문 77줄 '자는 판정하지 않는다', 202~203줄 '임계값을 정하지 않는다 … 판정은 목적이 한다'와 236~237줄 '자로 잰 값이 내려가야 하고'가 같은 문서 안에 있다. 한편 agent-principles.md의 `CLEAR-COMM`은 '전문영역 용어는 나올 때마다 짧게 부연한다'와 '화살표·슬래시를 문장 안 보조로 쓰는 것은 괜찮다'를 요구하고, 이 설계 자신도 149~150줄에서 '처음 나올 때 한 줄 풀어 준다. 빼는 것이 아니라 더하는 쪽이다'라고 적는다."
    },
    {
      "where": "'테스트 앵커가 붉어진다'(214~216줄)와 '압축하다 규칙이 사라질 수 있다'(220~223줄)",
      "type": "risk",
      "claim": "규칙 소실을 막는 장치가 사람의 대조 한 가지인데, 같은 재조율이 그 소실을 잡아낼 수 있었던 유일한 기계 신호를 사람이 손으로 끄게 만든다.",
      "consequence": "앵커가 붉어졌을 때 '새 문구로 맞추면 된다'로 처리하면, 실수로 빠진 문장과 의도적으로 다시 쓴 문장이 똑같은 모양의 작업(앵커 갱신)으로 처리된다. 168번의 판단 가운데 한 번만 착오가 나도 상시 로드 원칙에서 규칙 하나가 조용히 지워지고, 그 뒤에는 그것을 드러낼 신호가 남지 않는다. 설계가 스스로 '아무도 눈치채지 못한 채 오래 간다'고 적은 위험이 바로 이 경로로 실현된다.",
      "evidence": "본문 215~216줄 '앵커를 새 문구로 맞추면 되지만 건수가 많다', 221~223줄 '원칙 문서는 상시 로드라 빠진 것을 아무도 눈치채지 못한 채 오래 간다 … 나란히 놓고 무엇이 빠졌는지 확인하고'. scripts/test_docs_drift.sh 머리말 11~12줄도 '앵커가 산문 문구라 표현을 고치면 내용이 멀쩡해도 붉어진다'를 인지한 대가로 적는다. agent-principles.md의 `FAIL-LOUD`는 '구조 … 가 실수를 막거나 드러내게 만들어, 작성자가 정확히 기억하기에 의존하지 않는다'를 요구한다."
    },
    {
      "where": "'렌즈'의 이름 선택 근거(95~97줄)와 '고쳐 주는 범위'(124~128줄)",
      "type": "irreversible",
      "claim": "`reviewer-`라는 이름을 고른 이유로 '상시 허가를 받으니 따로 승인받지 않아도 된다'를 명시했는데, 그 허가는 고치지 않고 지적만 하는 읽기 전용 렌즈를 전제로 주어진 것이다.",
      "consequence": "사용자가 승인한 적 없는 종류의 작업(문체 재작성)이 이름 규칙만으로 상시 허가 집합에 들어가고, 문서 검진마다 자동으로 돈다. 더 나쁜 것은 경계가 이름으로 정해진다는 사실 자체다 — 다음에 무엇을 만들든 `reviewer-`를 붙이면 같은 허가를 얻으므로, 허가의 뜻이 '검토만 한다'에서 '이름이 그러하다'로 미끄러진다. 이름은 나중에 바꿀 수 있지만 README 트리 주석과 테스트 앵커, 드리프트 계약이 그 이름을 물기 시작하면 되돌리는 비용이 계속 커진다.",
      "evidence": "본문 95~97줄 '이름을 `reviewer-`로 시작하는 데는 실용적 이유가 있다. 원칙 문서가 `reviewer-*` 서브에이전트 호출을 상시 허가하므로 부를 때마다 따로 승인받지 않아도 된다'. agent-principles.md 51~58줄은 그 허가를 주면서 '`reviewer-prior-art`도 이 허가에서 뺀다'며 성격이 다른 렌즈를 명시적으로 도려낸다. skills/domain-spec-review/SKILL.md:40은 읽기 전용 서브에이전트를 쓰는 이유를 '구조적 거짓 방지'로 적는다."
    },
    {
      "where": "'자 — 방해를 세는 스크립트'의 축 선정 근거(73~75줄)",
      "type": "risk",
      "claim": "여섯 축의 정당화가 '여섯 다 실제로 우리 문서의 병을 하나씩 잡아냈다'인데, 축은 우리 문서를 보고 고른 것이므로 그 대조는 새 정보가 아니다. 그리고 같은 논법이 일곱 번째 축도 정당화한다.",
      "consequence": "다음 문서 종류에서 여섯 축에 안 걸리는 병을 만나면 '이 축이 실제로 병을 잡아냈다'는 같은 근거로 축이 늘어난다. 축 증식을 멈출 기준이 설계에 없고, 임계값도 두지 않으므로 '이 축은 쓸모없다'를 보일 방법도 없다. 이것은 이 설계가 첫 절에서 비판한 실패 모드와 같은 모양이다 — 규칙을 못 줄이는 대신 축을 늘리게 된다.",
      "evidence": "본문 74~75줄 '여섯 다 정규식으로 세어지고, 여섯 다 실제로 우리 문서의 병을 하나씩 잡아냈다'. 19~20줄은 '문서 종류가 하나 늘 때마다 규칙이 늘고, 목록에 없는 형태를 만나면 그냥 통과한다'를 규칙 증식의 병으로 적는다. 202~203줄은 임계값을 두지 않는다고 못박아 축의 유효성을 사후에 반증할 수치 기준을 남기지 않는다."
    },
    {
      "where": "'남의 것을 다시 짓지 않는다'(164~166줄)",
      "type": "risk",
      "claim": "외부 목록을 링크로만 맡기면서 요약본을 남기지 않고, 렌즈가 실행 시점에 그 링크를 어떻게 읽는지도 정하지 않았다.",
      "consequence": "두 가지가 동시에 걸린다. 첫째, 링크한 저장소가 사라지거나 방향을 바꾸면 우리 쪽에는 요약도 사본도 없어 무엇을 잃었는지조차 알 수 없다. 둘째, 그 이전에 이미 링크가 판정에 닿지 않을 공산이 크다 — 읽기 전용 렌즈가 웹 도구를 갖는지 이 설계는 다루지 않았고, 이 레포는 웹에 나가는 렌즈를 하나로 한정해 왔다. 그러면 '남의 것을 재사용했다'고 기록되지만 실제 판정에는 그 목록이 한 번도 쓰이지 않는다.",
      "evidence": "본문 164~166줄 '그 목록을 우리 레포에 베끼면 두 벌이 되고 … 베끼지 않고 출처로 링크한다(`SSOT`)'. 그러나 skills/domain-docs/SKILL.md:16은 같은 `SSOT` 규칙을 '본문을 베끼지 말고 distill(핵심만 요약)한 뒤 출처를 링크한다'로 적어 요약 단계를 요구한다. skills/domain-spec-review/SKILL.md:49와 86~87줄은 웹에 나가는 렌즈를 `reviewer-prior-art` 하나로 한정하고 '웹 도구가 없으면 돌릴 수 없다고 알린다'를 규정한다. 이 설계에는 새 렌즈의 웹 접근에 대한 규정이 없다."
    },
    {
      "where": "'절을 하나씩 본다'(111~118줄)와 '렌즈는 한 번만 띄운다'(160줄)",
      "type": "over-engineering",
      "claim": "호출 비용을 '한 번만 띄운다'로 적었지만 실제 비용은 절 수에 비례한다. 이 레포가 문서 검진에서 표집을 1회로 묶은 근거는 바로 그 빈도 대비 비용이었는데, 그 계산이 이 렌즈에는 적용되지 않았다.",
      "consequence": "절이 스무 개인 README 한 건을 검진하는 데 구조 단계 한 번과 절 단계 스무 번이 들어, 기존 두 렌즈를 합친 것보다 큰 비용이 문서를 저장할 때마다 발생한다. 그 비용이 '한 번만 띄운다'는 문장에 가려 계상되지 않으므로, 나중에 비싸다는 것이 드러났을 때 무엇을 줄여야 하는지 판단할 기준선도 남지 않는다.",
      "evidence": "본문 111~113줄 '아픈 절부터 하나씩 넘기고 절마다 핵심 지적 소수와 고친 문장을 받는다. 절을 넘길 때 앞뒤 절의 제목과 첫 문단을 함께 준다'와 160줄 '렌즈는 한 번만 띄운다'. skills/domain-docs/SKILL.md:140~141은 '일반 문서는 검진 빈도가 훨씬 높아 배로 늘리는 대가가 다르다'를 이유로 2회 표집을 거부한다."
    },
    {
      "where": "'됐다는 것을 어떻게 아나'의 마지막 줄(239줄)과 '렌즈'를 새로 만든다는 결정(95줄)",
      "type": "failure-mode",
      "claim": "렌즈 디렉터리 하나를 늘리는 것만으로 기존 드리프트 테스트가 붉어지는데, 그 대가가 '위험과 대가' 절에 없다.",
      "consequence": "`skills/reviewer-readability/`를 만드는 순간 README 트리 주석의 렌즈 나열과 `meta-aggregate`의 `source` 열거가 실제 디렉터리 집합과 어긋나 테스트가 실패한다. 그러면 '기존 테스트가 전부 통과해야 한다'는 성공 기준이 첫 단계에서 깨지고, 그것을 통과시키려면 이 설계가 다루지 않은 두 곳(README의 열거, 집계 계약의 렌즈 목록과 '다섯 렌즈'라는 문구)을 함께 고쳐야 한다. 더 근본적으로는 새 렌즈의 산출물 계약이 정해지지 않았다 — 집계 계약은 지적 목록만 담는 모양이라 이 렌즈가 돌려주는 '고친 문장'과 '구조 제안'을 담을 자리가 없다.",
      "evidence": "scripts/test_docs_drift.sh:23이 디렉터리에서 렌즈 집합을 도출하고 49줄이 'README 트리 주석이 렌즈 전부를 적는다', 58줄이 'meta-aggregate가 렌즈 전부를 태깅한다'를 단언한다. README.md:76의 나열은 '(grounding/fit/consistency/adversarial/prior-art)'이고 skills/meta-aggregate/SKILL.md:20은 '다섯 렌즈는 이 스키마로 돌려준다', 23~28줄의 스키마는 `issues` 배열만 정의한다. 이 설계 본문 239줄은 '기존 테스트가 전부 통과해야 한다'를 성공 기준으로 적고, 212~226줄의 '위험과 대가'에는 이 항목이 없다."
    },
    {
      "where": "'스크립트를 스킬 자산으로 싣는 것은 이 레포의 첫 사례다'(88~91줄)",
      "type": "irreversible",
      "claim": "되돌리기 쉽다는 판정의 대상을 파일 하나로 좁혀, 실제로 되돌리기 어려운 것을 가렸다.",
      "consequence": "지우면 되돌아오는 것은 스크립트뿐이다. 그 자가 재조율 착수 순서를 정하고, 렌즈가 볼 절을 고르고, 재조율의 성공 판정에도 들어가므로, 자를 지울 무렵이면 열여섯 문서는 이미 그 값에 맞춰 다시 쓰인 뒤다. 자가 틀렸다는 것이 나중에 드러나도 문서 재작성은 되돌아오지 않고, 무엇이 자 때문에 바뀐 것인지 구별할 기록도 남지 않는다.",
      "evidence": "본문 91줄 '파일 하나를 지우면 되돌아가므로 되돌리기 쉬운 결정이다(`REVERSIBLE`)'. 그러나 191~192줄은 '순서는 자가 정한다 … 심해서 먼저 간다', 103~104줄은 '어느 절을 볼지는 여기서 정해진다', 236~237줄은 '자로 잰 값이 내려가야 하고'로 자를 세 자리에 묶는다."
    },
    {
      "where": "'절 안 수정'과 '구조 변경'을 가르는 근거(124~132줄)",
      "type": "failure-mode",
      "claim": "절 안 수정에 승인 단계를 두지 않는 근거가 '어색한 문장은 사람이 읽으면 바로 판정된다'인데, 목적이 틀렸을 때 생기는 오류는 어색함으로 나타나지 않는다.",
      "consequence": "목적 한 줄이 어긋난 회차에서는 문장이 매끄럽게 다듬어진 채로 강조와 서술 방향만 목적과 반대로 정리된다. 사람이 읽어서 걸러 낸다는 안전장치는 문체를 보는 장치라 이 오류를 못 잡고, 절 안 수정에는 승인 단계도 없어 그대로 반영된다. 설계가 스스로 짐작한 목적을 '오염'이라 불렀으면서, 오염이 실제로 일어났을 때 그것을 걸러 낼 자리는 구조 변경 쪽에만 두었다.",
      "evidence": "본문 126~128줄 '문체는 다르다. 고치는 데 필요한 것이 문서 안에 다 있고, 어색한 문장은 사람이 읽으면 바로 판정되므로 리뷰어가 고쳐 와도 검증이 사라지지 않는다'와 130~132줄 '구조는 제안까지만 하고, 사람이 승인한 뒤 … 반영한다'. 101줄은 '짐작한 목적으로 판정하면 그때부터 오염이다'라고 적는다. 30~32줄은 같은 형태가 목적에 따라 도움도 되고 방해도 된다는 것을 이 설계의 전제로 삼는다."
    },
    {
      "where": "'렌즈'의 네 단계 순서(103~104줄)와 '됐다는 것을 어떻게 아나'의 렌즈 검증(232~234줄)",
      "type": "risk",
      "claim": "후보 절을 고르는 것은 목적이 아니라 자인데, 성공 기준은 자가 고른 후보 안에서만 잡음과 놓침을 센다. 그래서 '무엇이 방해인가'를 목적이 정한다는 주장이 실제로 시험되지 않는다.",
      "consequence": "여섯 축에 안 잡히는 방해 — 결론이 뒤에 있는 순서, 부연 없는 용어, 독자가 섞인 절 — 는 절 선택 단계에서 후보가 되지 않으므로 구조 단계까지 가지 못한다. 그런데 '넘긴 것도 함께 센다'는 검증은 후보에 오른 것만 세므로, 후보에 아예 못 오른 누락은 잡힌 것으로도 넘긴 것으로도 세어지지 않는다. 검증이 실패를 드러낼 수 없다는 바로 그 문제가 한 층 위에서 그대로 남는다.",
      "evidence": "본문 103~104줄 '자로 잰다 … 어느 절을 볼지는 여기서 정해진다. 축이 겹쳐 나오는 절이 답이므로 개수를 미리 박을 필요가 없다'와 232~234줄 '넘긴 것도 함께 센다. 잡은 것만 세면 검증이 실패를 드러낼 수 없다'. 30~32줄은 '무엇이 방해인지를 문서 밖에 고정해 두면 반드시 어긋난다'고 적지만, 여섯 축은 목적과 무관하게 문서 밖에 고정되어 있다(73~75줄)."
    }
  ],
  "principles_applied": [
    "SIMPLE", "REVERSIBLE", "FAIL-LOUD", "SSOT", "MEASURE-FIRST",
    "EXPLICIT", "CLEAR-COMM", "TDD", "FOCUSED", "NO-PRIORITY"
  ],
  "notes": "읽기 범위 규율에 따라 문서가 가리키는 파일만 확인했고 그 밖으로 탐색하지 않았다. '테스트가 산문 문구를 168군데에서 앵커로 잡는다'는 주장은 scripts/test_*.sh의 grep -qF 건수 합(47+94+21+6=168)과 맞아 반박 대상으로 올리지 않았다. 문서가 인용한 외부 저장소(stop-slop-ko, korean-skills, im-not-ai, korean-report-style)는 웹에 나가지 않는 렌즈라 확인하지 않았고, 그 확인은 다른 렌즈의 몫으로 남긴다. 가드에 따라 기능을 더하자는 제안은 하나도 넣지 않았고, 결과를 구체적으로 못 적는 의심은 올리지 않았다."
}
```
