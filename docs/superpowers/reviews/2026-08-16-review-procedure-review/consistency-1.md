# reviewer-consistency 1회차 원본 (2026-08-16, 1회차 리뷰)

리뷰어가 돌려준 것을 그대로 옮긴다. 고치지 않는다.

```json
{"lens":"consistency","issues":[
{"where":"처분 절 — 재리뷰 판단","type":"contradiction","claim":"재리뷰를 허용하는 문장이 domain-spec-review가 두 곳에서 못 박은 '리뷰는 한 번만 돈다'와 정면으로 부딪치는데, 제안은 그 문장을 고치거나 지우라고 말하지 않는다. frontmatter에도 같은 문구가 있어 고쳐야 할 자리가 둘이다.","consequence":"처분 절만 고치면 같은 파일 안에 상반된 두 규칙이 공존해, 실행하는 세션이 어느 쪽을 따를지 파일에서 도출하지 못한다. 검증 목록에도 그 문구 제거를 확인하는 항목이 없어 모순이 남아도 테스트가 조용히 통과한다.","evidence":"제안 78~81 대 skills/domain-spec-review/SKILL.md:3,84"},
{"where":"검증 절 — 기본값이 고치기가 남아 있지 않다","type":"contradiction","claim":"기존 테스트는 같은 문구가 반드시 있어야 통과하도록 단언하는데, 제안은 없어야 통과하는 단언을 더한다고만 적고 기존 단언을 지우라는 말을 하지 않는다.","consequence":"두 단언이 동시에 참일 수 없어 스크립트는 반영하는 순간 FAIL로 떨어지고, 계약이 FAIL=0이므로 비정상 종료한다.","evidence":"제안 100,105 대 scripts/test_docs_drift.sh:100,115"},
{"where":"처분 절 — 재리뷰 판단과 마커 기록 순서","type":"contradiction","claim":"재리뷰는 개선 뒤에 도는데, 작업 순서 절은 처분 직후 개선보다 먼저 마커를 남기라고 정한다. 두 절의 순서를 어떻게 맞출지 적지 않았다.","consequence":"1차에서 passed가 붙은 문서가 2차에서 🔴를 받으면 마커는 이미 terminal이라 게이트가 거짓을 말하는 상태로 남는다. 훅은 마커 존재만 보므로 탐지되지 않는다.","evidence":"제안 78~81 대 skills/domain-spec-review/SKILL.md:20~23,99~106"},
{"where":"처분 절 — 기각에 근거를 요구하지 않는다","type":"contradiction","claim":"발견을 넘길 때 근거도 기록도 요구하지 않는 규칙이 CLEAR-COMM, 건너뜀을 조용히 두지 않는다는 기존 규범, 마이너를 한 번 surface하라는 처분 규정과 모두 어긋난다.","consequence":"기각한 발견이 어떤 산출물에도 남지 않으므로, 표집을 두 배로 늘려 더 건진 것이 조용히 사라져도 관측되지 않는다. 예고한 기준선 대조도 성립하지 않는다.","evidence":"제안 75~76,88 대 agent-principles.md:11,64~67 및 SKILL.md:73~75"},
{"where":"렌즈 셋에 추출 한 줄 — fit과 prior-art를 손대지 않는 근거","type":"gap","claim":"fit을 빼는 근거를 제품 런타임으로 들었으나 fit은 domain-docs의 문서 검진에서도 읽기 전용 서브에이전트로 돈다. 반대로 손대기로 한 grounding도 같은 검진에서 도는데 그 영향을 다루지 않았다.","consequence":"grounding의 system 문장을 고치면 PREP이 없는 문서 검진까지 지시가 흘러가, 맥락 보강 없이 지시 밀도만 오른 조합이 된다. 제안이 스스로 기각한 방향이 그 호출자에서 실현된다.","evidence":"제안 42,51~52 대 skills/domain-docs/SKILL.md:126~129"},
{"where":"검증 절 — 렌즈별 단언","type":"contradiction","claim":"개수를 박지 않고 도출한다고 선언해 놓고, 바로 다음 줄부터 세 렌즈에는 있고 두 렌즈에는 없다는 이름 목록을 손으로 적는다.","consequence":"기존 루프는 모든 렌즈에 같은 계약을 균일하게 적용하는데 비대칭 계약은 그 루프로 표현되지 않아 이름 목록을 따로 유지해야 한다. 여섯 번째 렌즈가 생기면 어느 쪽에도 안 걸리고 조용히 통과한다.","evidence":"제안 100~103 대 scripts/test_docs_drift.sh:88~95, 2"},
{"where":"PREP 네 칸 표","type":"drift","claim":"같은 대상을 두 이름으로 부른다. '확인할 사실'은 기존 '검증할 구체 사실'과 같고, 표 넷 전체가 기존 두 항목을 대체하는지 그 아래로 들어가는지 밝히지 않는다. reviewer-grounding은 자기 출처를 옛 이름으로 부른다.","consequence":"옛 이름과 새 이름이 함께 남으면 같은 칸을 두 번 채우거나 한쪽만 채운다. grounding은 자기 출처를 어디서 받는지 파일에서 도출하지 못한다.","evidence":"제안 29~34 대 SKILL.md:30~32 및 reviewer-grounding:12"},
{"where":"PREP 네 칸의 배치와 추출 한 줄의 배치","type":"contradiction","claim":"PREP을 프롬프트 맨 앞에 붙인다고 적고 추출 한 줄도 system 맨 앞에 넣는다고 적는다. 두 내용이 같은 자리를 주장하는데 어느 쪽이 앞인지, PREP이 system인지 user인지 정하지 않는다.","consequence":"구현하는 사람마다 배치가 달라져, 앞쪽 지시가 더 지켜진다는 관측에 기대는 이 설계의 근거가 실행에서 재현되지 않는다.","evidence":"제안 27,36,42 대 skills/reviewer-consistency/SKILL.md:23~24"},
{"where":"2회 표집 병합 — 겹침 표시","type":"gap","claim":"이 표시를 담을 자리가 산출물 계약 어디에도 없다. meta-aggregate의 집계 항목은 닫힌 필드 목록이고 source는 렌즈 이름 다섯 중 하나인데, 제안은 그 파일을 고치지 않겠다고 명시한다.","consequence":"표시가 계약에 없으므로 집계 단계에서 버려지거나, 즉석으로 source를 늘리게 된다. 후자면 집계 태깅 집합이 렌즈 디렉터리와 달라져 단언이 깨진다.","evidence":"제안 64~65,87 대 skills/meta-aggregate/SKILL.md:15,49"},
{"where":"2회 표집 — prior-art 예외","type":"drift","claim":"호출자는 디스패치 목록을 셋으로 정의하고 선행연구는 그 목록에 없다고 명시한다. '디스패치되는 렌즈'에는 prior-art가 애초에 없는데 예외를 따로 적어 목록 안에 있는 것처럼 읽히게 만든다.","consequence":"반영자가 예외 문장을 근거로 prior-art를 디스패치 목록에 적으면, 테스트가 도출하는 집합이 넓어져 README·DESIGN-NOTES와 어긋나고 단언이 실패한다.","evidence":"제안 58,61 대 SKILL.md:49~52 및 scripts/test_docs_drift.sh:25~26,66~77"},
{"where":"prior-art의 가드 다섯은 하나도 지우지 않는다","type":"drift","claim":"보존 대상을 개수로 부르는데 그 개수가 원본과 맞지 않는다. 가드 절의 인용 블록은 여섯이고 description은 넷으로 열거한다.","consequence":"보존 계약이 개수로만 적혀 있어 어느 집합을 지키라는 것인지 도출되지 않는다. 하나가 삭제되어도 남은 다섯을 세면 여전히 참으로 읽힌다.","evidence":"제안 54 대 skills/reviewer-prior-art/SKILL.md:3,34,37,40,44,48,51"},
{"where":"제목과 안 하는 것 목록 대 렌즈 셋에 더하는 추출 한 줄","type":"contradiction","claim":"문서가 세운 규칙은 깊이를 지시가 아닌 맥락·표집·처분으로 올린다는 것과 렌즈 파일에 절차를 나열하지 않는다는 것인데, 실제로 바꾸는 것 하나가 렌즈 파일 세 곳의 system에 순서를 가진 지시를 더하는 것이다.","consequence":"자기 규칙과 본문이 어긋나 '안 하는 것' 목록을 나중에 경계로 쓸 수 없다. 한 줄은 되고 두 줄은 안 되는 경계가 없으므로 다음 편집자가 같은 논리로 줄을 더 붙여도 위반한 규칙이 없다.","evidence":"제안 1,85 대 42~46"},
{"where":"인지한 대가와 한계 — 문서 밖을 보는 렌즈가 grounding 하나뿐","type":"contradiction","claim":"이 진술이 렌즈 정의 및 제안 자신의 PREP 표와 부딪친다. consistency는 짝 문서와 이름 불린 파일까지 읽고 adversarial도 문서가 가리키는 파일을 확인한다.","consequence":"호출자가 이 문장을 믿으면 grounding 이외의 렌즈에는 '열어 볼 것'을 비워 채우고, 맥락 증강을 핵심 이득원으로 삼은 설계가 세 렌즈 중 하나에서만 작동한다.","evidence":"제안 31,96 대 reviewer-consistency:18~20 및 reviewer-adversarial:20~22"},
{"where":"PREP 네 칸 표 — 핵심 결정 칸","type":"contradiction","claim":"핵심 결정을 하나에서 셋까지 적으라고 개수 범위를 강제하면서, 같은 문서의 처분 절에서는 열거를 강제하면 후보가 팽창한다는 이유로 기본값 고치기를 뺀다.","consequence":"떠받치는 결정이 하나뿐인 문서에서도 칸을 채우려고 부차적 결정을 끌어올리게 되고, 리뷰어는 그것을 기둥으로 알고 대조한다. 없애려 한 팽창이 PREP 단계에서 다시 들어온다.","evidence":"제안 32 대 73"},
{"where":"도달 문제 진단과 검증 절의 단언","type":"gap","claim":"앞선 안을 기각한 근거는 도달 경로가 없다는 것이었는데, 이번 안의 검증은 렌즈 SKILL.md에 문구가 있는지만 확인한다. 디스패치 절은 프롬프트를 렌즈 파일에서 가져오라고 지시하지 않는다.","consequence":"렌즈 파일에 한 줄이 있어도 실제 프롬프트에 실린다는 보장이 없는데 단언은 파일 텍스트만 보므로 전부 통과한다. 기각하게 만든 실패 모드가 검증으로 걸러지지 않는다.","evidence":"제안 17~18,100~104 대 skills/domain-spec-review/SKILL.md:34~38"}
],"notes":"먼저 이 문서가 스스로 세운 규칙을 뽑아 목록으로 만든 뒤 본문과 대조했다. 뽑은 규칙 가운데 지시 금지, 렌즈 파일에 절차 금지, 도출 방식 준수 셋에서 본문이 자기 규칙을 어겼다."}
```
