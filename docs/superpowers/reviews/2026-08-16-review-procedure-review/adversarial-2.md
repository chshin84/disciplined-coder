# reviewer-adversarial 2회차 원본 (2026-08-16, 1회차 리뷰)

리뷰어가 돌려준 것을 그대로 옮긴다. 고치지 않는다.

```json
{"lens":"adversarial","issues":[
{"where":"렌즈 셋에 추출 한 줄 — adversarial 항목","type":"failure-mode","claim":"세 지시 가운데 adversarial의 것만 추출이 아니라 생성이다. 검증 가능한 주장과 문서가 세운 규칙은 표면에 실제로 적혀 있지만, 설계가 깔고 있는 전제는 정의상 문서에 적혀 있지 않아 모델이 지어내야 한다.","consequence":"오판율을 올리지 않는다는 근거가 이 렌즈에서만 성립하지 않는다. 작성자가 세운 적 없는 전제를 만들어 놓고 공격하는 발견이 늘고, 그 발견은 문서 인용으로 반박하기 어려워 처분 단계에서 판별 비용이 가장 비싸다. 이 리뷰 자체가 사례다.","evidence":"제안 46행이 세 지시를 나란히 두고 48-49행이 셋을 한꺼번에 방어한다. 그러나 46행의 대상만 문서에 실재하지 않는다."},
{"where":"추출 줄을 system 맨 앞에 넣는 배치","type":"failure-mode","claim":"추출 줄을 맨 앞에 밀어 넣으면 그 자리에 있던 역할 선언과 가드가 한 칸씩 뒤로 밀린다. 앞쪽 지시 편향은 새 줄에만 유리하게 작동하지 않고 밀려난 줄에는 불리하게 작동한다.","consequence":"adversarial에서 가장 먼저 약해지는 것이 '기능 추가는 제안하지 말 것' 가드다. 이 가드가 느슨해지면 YAGNI 렌즈가 기능 증식의 출처로 뒤집히고, 손실이 그 렌즈를 통과한 모든 설계 문서로 번진다.","evidence":"제안 10-11,36행; reviewer-adversarial/SKILL.md 25행"},
{"where":"reviewer-fit을 면제하는 문단","type":"risk","claim":"fit을 면제한 근거가 손대기로 한 grounding에도 그대로 있다. grounding은 런타임 리스크 표에서 2–3점과 4–5점 양쪽에 배정된 렌즈다.","consequence":"spec 리뷰의 깊이를 위해 만든 한 줄이 제품 런타임의 모든 grounding 리뷰 콜에 실린다. 런타임은 요청마다 도는 것이라 비교가 안 되는 빈도로 비용이 붙고, 면제 규칙이 성질이 아니라 이름으로 그어져 다음 사람이 도출할 수 없다.","evidence":"제안 51-52행; domain-llm-runtime/SKILL.md 18-19행"},
{"where":"이 안은 렌즈를 거의 건드리지 않는다(4행)와 새 지시를 만들지 않는다(36행)","type":"failure-mode","claim":"지시 총량이 줄지 않았고 실행 주체만 바뀌었다. 네 칸 채우기, 범위 밖 도출, 못 채운 칸 보고, 2회 디스패치, 병합 시 표시, 재리뷰 여부 판단이 모두 호출자에게 새로 얹힌 지시다.","consequence":"지시 밀도가 준수율을 떨어뜨린다는 근거가 참이라면, 그 저하는 이제 훅 계약·마커 순서·게이팅·처분 분류를 이미 지고 있는 메인 세션에서 일어난다. 그런데 메인 세션은 JSON을 돌려주지 않아 준수 여부를 관측할 산출물이 없다. 실패 지점을 관측 가능한 자리에서 관측 불가능한 자리로 옮겼다.","evidence":"제안 27-38,58-65,78-81행; meta-aggregate 19-29행"},
{"where":"처분에서 기본값 고치기를 뺀다 절 전체","type":"failure-mode","claim":"처분하는 호출자가 곧 그 문서의 작성자다. 기본값 고치기를 없애고 기각 근거 요구도 없애면, 작성자가 발견을 전부 넘겨도 산출물에 아무 흔적이 남지 않는다.","consequence":"발견이 0건이었던 리뷰와 전량 드롭한 리뷰가 산출물상 완전히 같아진다. 남는 흔적은 마커뿐인데 마커는 🔴 유무만 인코딩하므로 둘 다 passed다. 스킬이 자기 존재 이유로 적은 확증 편향이 가장 세게 작동하는 단계에서 다시 무방비가 된다.","evidence":"domain-spec-review 12-14,20-23,106행; 제안 75-76,88행"},
{"where":"받아들여 고치거나 🔴로 올리거나 넘긴다","type":"failure-mode","claim":"세 갈래를 제시하면서 어느 것이 기본인지 정하지 않는다. 기존 설계는 🔴 기준에 걸리지 않으면 전부 고친다는 규칙으로 빈자리를 메우고 있었는데 그 규칙만 빼고 대체를 두지 않았다.","consequence":"편향 교정이 과잉으로 넘어간다. '전부 고친다'에서 '아무것도 고치지 않아도 된다'로 건너뛰므로 잡음을 흡수한다던 단계가 신호까지 함께 흡수한다. 과잉 수정은 diff를 남기지만 과소 수정은 흔적이 없어 더 발견하기 어렵다.","evidence":"domain-spec-review 92-94행; 제안 70-76,94행"},
{"where":"2회 표집과 재리뷰 판단의 결합","type":"failure-mode","claim":"두 변경이 서로를 먹인다. 표집을 두 배로 하면 반영할 발견이 늘고, 반영이 늘면 개선폭이 컸다는 판정이 쉬워지며, 재리뷰는 다시 2회 표집으로 돌아 또 새 발견을 낸다. 상한은 바로 이 시점에 삭제된다.","consequence":"없앴다던 왕복이 재생되는 경로가 실재한다. 한 바퀴 비용은 예전보다 비싸다. '무한히 도는 것을 관측한 적이 없다'는 관측이 아니라, 지금까지 '한 번만 돈다'는 규칙이 그 사건을 불가능하게 만들어 온 결과다.","evidence":"제안 58-59,78-81행; domain-spec-review 84-85행; meta-aggregate 58행"},
{"where":"재리뷰 문단","type":"failure-mode","claim":"재리뷰를 돌 때 PREP을 다시 채우는지 아무 말이 없다. PREP은 디스패치 전 준비로 정의돼 1라운드 문서를 기준으로 만들어진다.","consequence":"2라운드에서 렌즈가 받는 맥락이 고쳐지기 전 문서를 가리킨다. 이 설계가 깊이의 유일한 원천으로 지목한 입력이 재리뷰에서 가장 먼저 낡으므로, 재리뷰는 비용만 온전히 내고 깊이는 1라운드보다 낮은 상태로 돈다.","evidence":"domain-spec-review 28-32행; 제안 78-81행"},
{"where":"재리뷰 문단과 작업 순서 절의 결합","type":"failure-mode","claim":"마커는 개선보다 먼저 쓰이고 그 값은 1라운드 결과로 결정된다. 재리뷰를 붙이면 2라운드 시작 시 이미 최종 마커가 박혀 있는데 2라운드 결과로 마커를 고치라는 규정이 없다.","consequence":"2라운드에서 처음 🔴가 나오면 문서는 passed 마커를 단 채 미해결 결정을 안고 남는다. 훅은 조용하고 Stop도 통과시켜, 게이트 토큰이 실제 상태와 반대를 말하는데 드러내는 장치가 없다.","evidence":"domain-spec-review 20-23,99-106행; 제안 78-81,83-89행"},
{"where":"둘 다에서 나온 것과 한쪽에서만 나온 것을 표시한다","type":"failure-mode","claim":"이 표시는 관측 사실이지만 소비되는 자리에서는 신뢰도 등급으로 작동한다. 기각에 근거가 필요 없어진 처분 단계에서 '한쪽에서만'은 발견을 떨어뜨릴 가장 값싼 명분이 된다.","consequence":"걷어냈던 라벨이 이름만 바꿔 돌아온다. 더 나쁜 것은 신호가 목적과 반대로 작동한다는 점이다. 두 표집이 일치하는 발견은 1회로도 잡혔을 것이고 표집을 늘려 얻는 순이득은 정의상 한쪽에서만 나온 발견인데, 표시는 바로 그 몫을 체계적으로 깎는다.","evidence":"제안 64-65,75-76행; meta-aggregate 31행"},
{"where":"범위 밖 칸과 손으로 창작하지 않는다","type":"failure-mode","claim":"도출되는 것은 함께 도는 렌즈의 이름뿐이고, 그 렌즈가 무엇을 보는지에 해당하는 문장은 여전히 호출자가 쓴다. 창작 금지는 목록에만 걸리고 내용에는 걸리지 않는다.","consequence":"각 렌즈에게 '이건 다른 렌즈가 본다'고 적어 준 영역이 실제로는 아무도 보지 않는 사각지대가 될 수 있다. 그 배제문은 준수율이 가장 높다고 주장한 자리에 놓이므로 배제는 확실히 지켜지고 누락은 어디에도 기록되지 않는다.","evidence":"제안 34,36,37,95행"},
{"where":"2회 표집과 재리뷰 판단 — 변경 넷 가운데 둘","type":"over-engineering","claim":"인용한 측정은 표면성의 원인을 맥락 부족으로 지목한다. 그 진단에서 도출되는 처방은 PREP 하나이고, 2회 표집과 재리뷰는 놓침이라는 다른 문제를 겨냥하는데 그 놓침의 크기를 재 놓은 값이 없다.","consequence":"크기를 모르는 문제를 막으려고 리뷰마다 토큰을 영구히 두 배로 쓰고 종료 조건이 사람 판단인 루프를 얹는다. 이득이 0으로 밝혀져도 그때는 정본 다섯 곳과 테스트가 이 배치를 전제하고 있어 되돌리는 비용이 넣을 때보다 크다. 단순화는 PREP만 남기고 표집과 재리뷰를 넣지 않는 것이다.","evidence":"제안 14-15,67-68,93행; agent-principles SIMPLE·MEASURE-FIRST"},
{"where":"검증 절 — 더할 단언 넷","type":"failure-mode","claim":"네 단언은 모두 문구가 특정 파일에 있는지 없는지만 본다. 그런데 이 제안의 실패 위험은 전부 행위 쪽에 있다.","consequence":"테스트가 초록인데 설계는 하나도 작동하지 않는 상태가 정상적으로 존재할 수 있고, 초록이 그 상태를 확인해 준 것처럼 읽힌다. TDD가 요구하는 성공 기준이 '문장이 존재함'으로 치환된다.","evidence":"제안 100-105,95행; scripts/test_docs_drift.sh:2,23,26"},
{"where":"prior-art를 1회로 두는 근거","type":"risk","claim":"근거 둘 가운데 '발견이 어차피 전부 🔴로 가므로 이득이 처분 단계로 흘러들지 않는다'는 성립하지 않는다. 처분 단계가 달라지지 않는 것과 이득이 없는 것은 다른 말이다.","consequence":"이 논리를 규칙으로 재사용하면 표집 정책이 뒤집힌다 — 놓침의 대가가 가장 큰 렌즈일수록 표집을 줄이라는 결론이 나온다. 정당한 근거인 비용이 부당한 근거와 묶여 있어 나중에 어느 축에서 다시 볼지 판단할 수 없다.","evidence":"제안 61-62행; domain-spec-review 96-97행"}
],"notes":"이 리뷰는 지시대로 전제 목록을 먼저 만들고 각 전제를 공격하는 순서로 수행했다. 그 과정에서 목록의 일부가 문서에 적혀 있지 않은 내가 만든 전제였고, 그 관찰 자체를 첫 번째 발견의 근거로 썼다. 가드에 따라 기능을 더하자는 제안은 하지 않았으며 제시한 대안은 둘 다 변경을 넣지 않는 쪽의 단순화다."}
```
