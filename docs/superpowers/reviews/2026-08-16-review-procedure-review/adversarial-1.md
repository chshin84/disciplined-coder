# reviewer-adversarial 1회차 원본 (2026-08-16, 1회차 리뷰)

리뷰어가 돌려준 것을 그대로 옮긴다. 고치지 않는다.

```json
{"lens":"adversarial","issues":[
{"where":"인지한 대가와 한계 — 오탐은 처분 단계가 흡수한다","type":"failure-mode","claim":"처분 단계는 오탐을 흡수하는 자리가 아니다. 처분의 세 기준은 발견이 참인지를 묻지 않고 사용자에게 올릴지만 가르며, 셋에 걸리지 않으면 기본값이 고치기다. 되돌릴 수 있고 가치판단이 아니며 방향이 하나인 오탐은 정의상 세 기준을 모두 비껴간다.","consequence":"표집을 두 배로 하면 자동 반영되는 오탐도 두 배가 되어 리뷰어가 잘못 읽은 내용이 문서를 바꾼다. 더 나쁜 것은 그 수정이 동작을 건드리면 재리뷰 표시가 붙어 한 바퀴를 더 돌게 만든다는 점이다. 파이프라인 어디에도 참거짓을 판정하는 단계가 없다.","evidence":"SKILL.md 처분 절, meta-aggregate의 판단 재귀 회피, 검토 문서 111행"},
{"where":"안 하는 것 — 재리뷰에 상한을 두지 않는다","type":"failure-mode","claim":"이 루프는 표시 수가 단조 감소한다는 전제 위에 서 있는데 그것을 보장하는 장치가 없다. 매 회차 문서가 바뀌므로 새 표집은 새 문서를 처음 보는 것과 같다.","consequence":"라운드당 서브에이전트 여섯과 PREP 여섯 벌이 든다. 실제로는 사람이 중간에 그만두는 형태로 끝나는데, 그때 문서는 반영이 절반이고 마커도 기록도 없는 상태로 남는다. Stop 루프가드는 이미 소진되었고 PostToolUse는 차단하지 않으므로 미완결 문서가 저항 없이 통과한다.","evidence":"검토 문서 105,114-115행, hooks/spec_review_stop.sh:12"},
{"where":"표시가 남아 있으면 마커를 남기지 않는다","type":"failure-mode","claim":"마커 미루기는 미마커 창을 한 번의 편집 묶음에서 여러 턴에 걸친 루프 전체로 늘린다. 재리뷰 루프는 🔴 때문에 사용자 개입을 포함하므로 한 턴에 끝나지 않는다.","consequence":"기존 설계에서 마커 없이 지나갈 수 있는 창은 한 번이었는데 이 설계에서는 라운드 수만큼 열린다. 막으려던 것과 크기가 비슷하거나 더 큰 위험을 새로 만든다.","evidence":"검토 문서 78-83행, hooks/spec_review_stop.sh:12"},
{"where":"마커를 미루는 동안의 개선 편집","type":"risk","claim":"마커가 없는 동안 편집할 때마다 PostToolUse가 '마커를 먼저 남기고 그다음 개선하라'를 주입한다. 이 문구는 지금 수행 중인 절차와 정확히 반대다.","consequence":"지시에 따라 마커를 먼저 달아 미루기 장치가 무력화되거나, 지시를 습관적으로 무시하게 되어 진짜로 필요한 상황에서도 무시된다. 후자는 훅 하나의 신호 가치를 통째로 떨어뜨린다.","evidence":"hooks/spec_review_posttooluse.sh:22, SKILL.md 135, 검토 문서 82-83"},
{"where":"재리뷰 표시","type":"failure-mode","claim":"표시가 어디에 사는지 정해져 있지 않다. 문서에 적으면 '문서에 상태 금지'와 부딪치고, 세션 컨텍스트에만 두면 휘발한다.","consequence":"표시가 휘발하면 종료 조건이 사라진다. 다음 턴은 남은 표시를 모르므로 루프가 끝났다고 판단하고 마커를 확정한다. 이 설계가 마커를 미루면서까지 막으려던 바로 그 상태다.","evidence":"검토 문서 78-80, SKILL.md 133-134, agent-principles 문서·상태 위생"},
{"where":"합산 기록","type":"failure-mode","claim":"합산 기록에는 아무 게이트가 없다. 훅은 마커만 보고, 검증 단언은 문서에 규정이 적혀 있는지만 확인할 뿐 기록 파일이 생겼는지는 보지 않는다.","consequence":"기록을 빠뜨린 리뷰와 남긴 리뷰가 게이트 관점에서 구별되지 않는다. 구별 불가능성이 발견에서 기록으로 한 칸 옮겨갔을 뿐 없어지지 않았고, 이 층에는 마커 같은 계약 토큰조차 없어 더 나쁘다.","evidence":"검토 문서 65-67,119-125"},
{"where":"파일명과 한 번 쓰고 고치지 않는다","type":"irreversible","claim":"파일명에 회차를 가르는 요소가 날짜와 주제뿐인데 재리뷰는 같은 날 같은 주제로 여러 라운드를 돈다.","consequence":"덮어쓰면 1회차 발견 원본이 사라진다. 사라지는 것이 하필 기준선이고, 1회차는 문서가 바뀌기 전 상태에 대한 유일한 관측이라 재생성이 불가능하다.","evidence":"검토 문서 59,69,67,79"},
{"where":"합산 기록의 세 항목과 작업 순서","type":"risk","claim":"합산 기록은 처분보다 먼저 쓰이고 그 안에 겹침 여부가 각 발견 옆에 놓인다. 그다음 같은 사람이 그것을 보며 처분을 가른다.","consequence":"겹침 표시를 눈앞에 두고 처분하면 한쪽에서만 나온 발견을 덜 무겁게 다루게 된다. 명시적으로 금지한 사용이 배치에 의해 되살아난다.","evidence":"검토 문서 48-49,63, SKILL.md 125"},
{"where":"PREP에 범위 밖 칸을 더한다","type":"over-engineering","claim":"범위 밖 칸은 결함 밀도가 가장 높은 구역의 이중 관측을 손으로 제거한다. 그런데 같은 문서가 다른 자리에서는 중복이 해롭지 않다고 말한다.","consequence":"배정이 어긋나면 두 렌즈가 모두 남의 몫이라고 여기는 구역이 생기고, 그 구역은 표집을 두 배로 해도 0회 관측된다. 놓침을 줄이려고 시작한 변경이 특정 구역의 놓침을 확실하게 만든다.","evidence":"검토 문서 24-29,48-49, meta-aggregate의 기계적 집계"},
{"where":"범위 밖 칸을 호출자가 채운다","type":"risk","claim":"범위 밖 배정은 리뷰 대상 문서의 작성자가 쓰고, 그 결과는 합산 기록의 세 항목 어디에도 들어가지 않는다.","consequence":"작성자가 자신 없는 구역을 어느 렌즈의 몫에서도 뺄 수 있고 그 사실이 어떤 산출물에도 남지 않는다. 나중에 놓친 결함이 드러나도 렌즈의 실패인지 배정의 실패인지 판별할 자료가 없다.","evidence":"검토 문서 24-25,63, SKILL.md 38-42"},
{"where":"일반 문서 검진과 제품 런타임도 합산해 남긴다","type":"over-engineering","claim":"2회 표집은 호출 빈도를 이유로 옮기지 않으면서 기록 의무는 같은 논증을 적용하지 않고 그대로 옮긴다. 게다가 겹침 여부는 1회로 도는 자리에서 언제나 아니오라 정보가 0이다.","consequence":"빈도가 높은 경로에 값이 0인 필드를 포함한 기록 의무를 지운다. '완결된 기록이라 썩지 않는다'는 정당화는 한 번 쓰고 마는 파일에 대한 것이지 계속 자라는 로그에 대한 것이 아니다.","evidence":"검토 문서 53-55,71,63,69,95-96"},
{"where":"리뷰 기록을 별도 디렉터리에 두는 이유","type":"failure-mode","claim":"spec 훅은 피했지만 문서 훅은 피하지 못한다. reviews/*.md는 문서 작성 훅의 조건에 정확히 들어맞아 기록을 쓰는 순간 검진 넛지가 뜬다.","consequence":"넛지를 무시하는 것이 유일하게 실행 가능한 선택이 되고, 그 무시가 관행이 되면 진짜 문서에 대한 넛지도 함께 흘려보낸다. 리뷰 층을 강화하려다 인접한 검진 층의 신호를 갉아먹는다.","evidence":"hooks/doc_review_posttooluse.sh:13-14, 검토 문서 59-61,71"},
{"where":"남아 있으면 그 부분을 다시 리뷰한다","type":"failure-mode","claim":"재리뷰의 범위가 표시가 붙은 부분으로 좁혀지는데 마커는 문서 전체에 대해 사이클이 끝났다고 선언한다.","consequence":"마지막 라운드에서 바꾼 내용이 손대지 않은 절과 어긋나도 아무도 보지 않은 채 마커가 확정된다. 라운드가 늘수록 검토되지 않은 채 확정되는 면적이 커진다.","evidence":"검토 문서 79-80, SKILL.md 133-137"}
],"notes":"되돌리기 비용 자체는 낮다 — 전부 산문 편집이라 데이터나 배포가 걸리지 않는다. 다만 검증 계획이 '다섯 렌즈 모두 절을 갖는다'를 테스트로 굳히므로 나중에 걷어내려면 테스트도 함께 고쳐야 한다. 가드에 따라 새 필드·새 렌즈·새 관측 장치 같은 기능 추가는 제안하지 않았고, 위 발견은 전부 축소하거나 위치를 되돌리는 방향이다."}
```
