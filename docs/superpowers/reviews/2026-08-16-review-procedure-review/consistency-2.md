# reviewer-consistency 2회차 원본 (2026-08-16, 1회차 리뷰)

리뷰어가 돌려준 것을 그대로 옮긴다. 고치지 않는다.

```json
{"lens":"consistency","issues":[
{"where":"검증 절 — 기본값이 고치기가 남아 있지 않다","type":"contradiction","claim":"검증에 단언을 더한다고만 적으면서, 기존 스크립트가 이미 반대 방향으로 단언하는 문구의 부재를 새 단언으로 요구한다. 기존 단언을 지우라는 지시가 없다.","consequence":"두 단언이 같은 파일에서 동시에 참일 수 없어 스크립트가 어떻게 고치든 FAIL을 낸다. FAIL=0 계약이 구조적으로 달성 불가가 되고, 구현자는 제안에 없는 삭제를 임의로 판단해야 한다.","evidence":"제안 100,105 대 scripts/test_docs_drift.sh:100"},
{"where":"처분 절 — 재리뷰 판단","type":"gap","claim":"'리뷰는 한 번만 돈다'가 호출자 frontmatter, 처분 절, README 세 곳에 있는데 제안은 그중 어느 것도 고치라고 적지 않는다.","consequence":"재리뷰를 넣으면 호출자 파일 하나가 상반된 두 규칙을 동시에 규정하고, README 캐시는 스킬과 어긋난 채 남는다.","evidence":"제안 78-81 대 SKILL.md:3,84-85 와 README.md:25"},
{"where":"처분 절 — 재리뷰와 마커 선기록의 순서","type":"contradiction","claim":"호출자는 처분 직후 마커를 먼저 남기고 그다음 고치도록 순서를 못박고 마커의 뜻을 '리뷰가 한 번 돌았다'로 정하는데, 제안은 반영이 끝난 뒤 한 번 더 돌 수 있게 하면서 마커를 어떻게 할지 적지 않는다.","consequence":"2차에서 처음 🔴가 나오면 문서는 passed 마커를 단 채 미해결 결정을 안고 남는다. 마커가 이미 마지막 줄에 있으므로 훅은 조용하고 Stop도 통과시켜, 게이트 토큰이 실제 상태와 반대를 말한다.","evidence":"제안 78-81 대 SKILL.md:99-106"},
{"where":"처분 절 — 받아들여 고치거나 🔴로 올리거나 넘긴다","type":"gap","claim":"세 갈래와 기존 🔴 진입 기준 셋의 관계를 적지 않았다. 또한 '넘긴다'를 보고에 남기라는 요구가 없다.","consequence":"되돌리기 어려운 결정도 넘긴다로 처리할 수 있게 되어 🔴 강제가 재량으로 바뀌고, 마커는 passed로 남는다. 넘긴 사실이 어떤 산출물에도 남지 않아 빠뜨림이 관측되지 않는다.","evidence":"제안 75-76,88 대 SKILL.md:87-94,73-75"},
{"where":"렌즈 셋에 추출 한 줄 / 검증 절","type":"gap","claim":"앞선 안을 기각한 근거는 도달 경로가 없다는 것인데, 이 안도 렌즈 SKILL.md만 고친다. 호출자의 디스패치 절은 렌즈의 system을 그대로 실으라고 적지 않고 검증도 텍스트 존재만 본다.","consequence":"파일에 한 줄이 있는지만 확인하는 단언은 통과하는데 실제 프롬프트에는 실리지 않을 수 있다. 앞선 안을 죽인 문제가 그대로 남은 채 테스트가 초록으로 덮는다.","evidence":"제안 17-18,42-46,102-103 대 SKILL.md:34-38"},
{"where":"fit과 prior-art를 뺀 근거","type":"contradiction","claim":"fit을 뺀 근거로 제품 런타임을 들지만 grounding도 런타임 검증표와 문서 검진에서 쓰인다. 같은 기준이 두 렌즈를 가르지 못한다.","consequence":"비대칭의 근거가 성립하지 않아 다음 변경 때 재현되지 않는다. 게다가 설계 문서를 전제한 문장이 제품 런타임 grounding 콜에도 실려, 제품 출력을 보는 자리에 spec 리뷰용 지시가 섞인다.","evidence":"제안 51-52 대 skills/domain-llm-runtime/SKILL.md:18-21, domain-docs:126"},
{"where":"2회 표집 — 디스패치되는 렌즈","type":"drift","claim":"'디스패치되는 렌즈를 2회씩' 다음에 'prior-art는 1회'라고 적어 prior-art를 디스패치되는 렌즈로 취급하지만, 호출자는 그 목록에 없다고 못박고 테스트는 그 불릿을 권위 있는 출처로 읽는다.","consequence":"이 문장대로 호출자를 고쳐 prior-art를 불릿에 넣으면 README·DESIGN-NOTES와 어긋나 단언이 실패한다. 넣지 않으면 예외 문장이 헛돈다.","evidence":"제안 58,61 대 SKILL.md:52 와 scripts/test_docs_drift.sh:25-26,66-77"},
{"where":"2회 표집 — 겹침 표시","type":"gap","claim":"표시를 담을 자리가 산출물 계약에 없다. 집계 항목은 닫힌 필드 목록이고 source는 렌즈 이름 열거이며, 제안은 meta-aggregate를 건드리지 않겠다고 선언한다.","consequence":"표시가 병합 단계에서 조용히 버려지거나, 선언과 달리 SSOT 스키마를 예고 없이 고치게 된다. 후자면 source 열거를 렌즈 디렉터리와 대조하는 단언도 함께 검토해야 하는데 검증 목록에 없다.","evidence":"제안 64-65,87 대 skills/meta-aggregate/SKILL.md:47-51"},
{"where":"검증 절 — 네 칸·다중 표집·재검토 판단이 적혀 있다","type":"gap","claim":"단언이 쓸 앵커 문구가 본문 어디에도 정해져 있지 않다. 본문은 '2회씩', '개선폭이 컸는지'로 쓰는데 검증 절은 '다중 표집', '재검토 판단'이라는 다른 말로 부른다.","consequence":"구현자가 본문 문구와 테스트 앵커를 각각 창작하게 된다. 그러면 단언은 자기가 방금 심은 문구만 확인하고 규율이 실제로 적혔는지는 검사하지 못한다.","evidence":"제안 104 대 58,78 와 scripts/test_docs_drift.sh:10-12"},
{"where":"검증 절 — 렌즈별 비대칭 단언","type":"drift","claim":"개수를 박지 않고 디렉터리에서 도출한다고 적었지만, 어느 렌즈가 어느 편인지는 이름 다섯을 손으로 나눠 적어야만 표현된다.","consequence":"렌즈가 하나 늘면 사람이 이름 목록을 손으로 고쳐야 하고, 안 고치면 새 렌즈는 어느 단언에도 걸리지 않는다. 없애려던 손유지 열거가 되살아난다.","evidence":"제안 100,102-103 대 scripts/test_docs_drift.sh:22-26,1-9 와 agent-principles.md:31"},
{"where":"검증 절 마지막 문단 — 기준선 대조","type":"gap","claim":"기준선은 렌즈 넷을 1회씩 돌려 얻은 값인데, 2회 표집은 prior-art를 뺀 셋에만 적용된다. 두 조건의 렌즈 구성이 다르다.","consequence":"'같은 조건'이라고 적었으나 조건이 같지 않아, 증가분이 두 번째 표집 덕인지 렌즈 구성 차이 탓인지 가를 수 없다. 3회로 올릴지를 이 대조로 정하겠다는 계획이 근거를 내놓지 못한다.","evidence":"제안 107-108 대 58,61,67-68"},
{"where":"인지한 대가와 한계 절","type":"gap","claim":"대가를 '디스패치되는 렌즈의 토큰이 두 배'로 적지만, 같은 문서가 상한 없는 재리뷰를 도입했다. 재리뷰 비용이 대가 목록에 없다.","consequence":"이 절을 읽고 비용을 승인하는 사람이 실제 상한 없는 지출을 두 배로 오해한다. '상한은 두지 않는다'와 대가 절의 확정적 상한 표현이 같은 문서에서 어긋난다.","evidence":"제안 93 대 80-81,89"},
{"where":"PREP 네 칸","type":"drift","claim":"네 칸이 기존 두 항목과 겹치는데 대체인지 추가인지 적지 않는다. 특히 '검증할 구체 사실'과 '관련 파일 경로'를 다른 이름으로 다시 부른다.","consequence":"호출자 PREP 절에 준비 목록이 두 벌 남아 무엇을 채워야 하는지가 갈린다. grounding이 자기 출처를 옛 이름으로 정의하고 있어, 이름을 바꾸면 그 참조가 가리키는 대상이 사라진다.","evidence":"제안 29-34 대 SKILL.md:28-32 와 reviewer-grounding:11-13"},
{"where":"PREP 네 칸 표의 핵심 결정 행","type":"contradiction","claim":"표 셀을 서술형 종결 없이 명사로 끊는다. 정본은 표 같은 구조화된 산출물에서도 완결된 문어체를 요구하며 그 자리에서 가장 자주 무너진다고 지목한다.","consequence":"이 표는 그대로 호출자가 매 리뷰마다 채우는 서식이 된다. 서식 자체가 조각을 본보기로 보여주므로 프롬프트 맨 앞 문구가 회차마다 조각으로 채워진다.","evidence":"제안 32 대 agent-principles.md:11"}
],"notes":"먼저 이 문서가 스스로 세운 규칙을 뽑아 본문과 대조했다. 외부 사실·인용의 참거짓과 실패 모드·과설계 판단은 범위 밖으로 두었다."}
```
