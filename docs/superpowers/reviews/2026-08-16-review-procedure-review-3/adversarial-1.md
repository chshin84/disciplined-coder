# reviewer-adversarial 1회차 원본 (2026-08-16, 3회차 리뷰)

리뷰어가 돌려준 것을 그대로 옮긴다. 고치지 않는다.

```json
{"lens":"adversarial","read":["docs/superpowers/specs/2026-08-16-review-procedure-design.md","docs/superpowers/reviews/2026-08-16-review-procedure-review.md","docs/superpowers/reviews/2026-08-16-review-procedure-review-2.md","skills/domain-spec-review/SKILL.md","skills/meta-aggregate/SKILL.md","skills/reviewer-adversarial/SKILL.md","hooks/spec_review_stop.sh","hooks/doc_review_posttooluse.sh","docs/solved_problems.md (git show ca87020)","git status --porcelain -- docs/superpowers/, ls docs/superpowers/reviews/"],
"issues":[
{"where":"마커는 미루지 않는다(121-125행)","type":"failure-mode","claim":"마커를 개선보다 먼저 남기면 이후의 모든 편집 구간에서 PostToolUse 넛지와 Stop 게이트가 둘 다 침묵한다. 이 상태는 이 레포가 이미 실패로 기록해 둔 상태다.","consequence":"🔴 반영을 포함한 가장 큰 기능적 변화가 어떤 훅에도 걸리지 않는 구간에 들어간다. 그 구간에서 세션이 끝나면 재리뷰가 안 돈 사실이 어디에도 남지 않고 통과한다. 남는 방어는 훅이 강제하지 않는 자동 1회 재리뷰 관례뿐인데, 그 관례를 지켰는지는 마커로도 기록으로도 관측되지 않는다.","evidence":"docs/solved_problems.md 최신 항목(커밋 ca87020). domain-spec-review/SKILL.md 178행은 같은 효과를 이점으로 적는다. 1회차 리뷰 87행이 지적한 것은 미마커 구간의 길이였지 이 반대편 대가가 아니었다."},
{"where":"다시 리뷰하면 그 회차의 마커를 새로 남긴다(122행)","type":"failure-mode","claim":"2회차 이후의 마커에는 어떤 강제도 없다. Stop 하드게이트는 신규 파일에만 걸린다.","consequence":"첫 리뷰 이후 spec이 커밋되면 재리뷰 마커를 안 남겨도 게이트가 안 잡는다. 회차별 마커 규칙이 가장 필요한 시점에 관측 장치가 없는 순수 자율 규범이다.","evidence":"hooks/spec_review_stop.sh 26-27,33-44행. domain-spec-review/SKILL.md 230-231행."},
{"where":"몇 번 돌았는지는 기록 파일을 세어 안다(138-140행) × 같은 이름의 폴더를 만든다(99행)","type":"failure-mode","claim":"두 처방이 같은 디렉터리에서 충돌한다. 원본 보관 폴더의 이름이 기록 파일의 이름 그루터기와 같아, 같은 스템으로 세면 개수가 2배가 된다. 무엇을 세는지는 정의되지 않았다.","consequence":"1회차만 끝난 시점에 항목이 파일 1 + 폴더 1 = 2가 되어 '둘 이상이면 묻는다'에 걸린다. 자동 재리뷰가 한 번도 발동하지 못하거나, 세는 방식을 구현자가 매번 임의로 정해 결과가 갈린다.","evidence":"ls docs/superpowers/reviews/ 결과에 파일 둘과 디렉터리 둘이 같은 층에 있다. spec 138-139행은 대상·글롭을 정하지 않는다."},
{"where":"기록 파일명 규약(91행)과 세는 규칙","type":"failure-mode","claim":"세는 키(주제 슬러그·날짜 접두사)를 도출하는 규칙이 없다. 실제 파일명도 spec 파일명에서 기계적으로 나오지 않는다.","consequence":"세션이 바뀌어 규칙을 기억하는 주체가 사라진 상태에서 슬러그를 다르게 잡으면 개수가 0으로 읽혀 자동 재리뷰 예산이 조용히 리셋된다. 리셋은 아무 신호도 남기지 않는다.","evidence":"검토 대상은 …-design.md인데 기록은 …-review.md다. -design이 떨어졌고 그 변환 규칙이 없다. 이 사이클 자체가 08-15에서 08-16으로 날짜 경계를 넘었다."},
{"where":"하나면 한 번 더 자동, 둘 이상이면 묻는다(139행)","type":"failure-mode","claim":"개수는 영구 누적되며 리셋 지점이 없다. 파일은 한 번 쓰고 고치지 않는다고 못 박혀 계속 쌓인다.","consequence":"한 번 2회차까지 간 문서는 몇 달 뒤 새 리뷰 사이클을 시작해도 개수가 이미 2 이상이라 자동 재리뷰가 영구히 불가능해진다. 새 사이클의 1회차가 3회차로 취급된다.","evidence":"spec 92,113행과 139-140행. domain-spec-review/SKILL.md 134행."},
{"where":"세션이 바뀌면 기억은 사라지지만 파일은 남는다(140행)","type":"risk","claim":"회차 상태의 유일한 저장소가 git이 추적하지 않는 디렉터리다. 그 디렉터리는 훅의 대상에서도 명시적으로 빠져 있다.","consequence":"git clean -fd·새 클론·워크트리·사람의 삭제 어느 쪽으로도 개수가 0이 되고 그 리셋이 관측되지 않는다. 사라지는 것은 재리뷰 상한만이 아니라 기준선 전체다. 상태를 문서에 안 적는 대신 고른 매체가 문서보다 덜 견고하다.","evidence":"git status --porcelain -- docs/superpowers/ → ?? docs/superpowers/reviews/. hooks/doc_review_posttooluse.sh 17행. spec은 커밋하라는 말을 어디에도 적지 않는다."},
{"where":"각 지적을 어떻게 처리했는지는 담지 않는다(103-104행)","type":"irreversible","claim":"기록에서 처분을 빼면, 2회 표집을 정당화할 때 쓴 종류의 근거를 다음 회차가 재생산할 수 없다.","consequence":"기준선이 지적 건수만 담아 신호와 잡음을 구분하지 못한다. 74행의 '새로 잡은 것이 잡음이 아니었다'와 176행의 '두 번 돌리면 틀린 지적도 는다'는 둘 다 처분을 알아야 나오는 주장이다. 결과적으로 2회를 1회로 되돌리자는 판단만 근거를 잃는다 — 줄이는 쪽 경로가 닫힌다.","evidence":"spec 110-111행이 이 파일을 기준선으로 지정하는데 103-104행이 처분을 뺀다. 2회차 기록에도 처분 절이 없다."},
{"where":"처분의 대체 경로 — domain-spec-review/SKILL.md 148행 '커밋과 보고에 남는다'","type":"risk","claim":"대체 경로로 지목된 두 매체가 회차를 구별하지 못하고, 하나는 세션과 함께 사라진다.","consequence":"합칠 때 틀린 지적을 걸렀는지, 몇 건을 걸렀는지가 어디에도 남지 않는다. 오탐률이 관측되지 않으므로 인정한 대가는 영원히 측정 불가로 남는다.","evidence":"spec 140행이 세션 휘발을 스스로 적는데 보고는 세션 산출물이다. git status에 두 회차 결과가 한 워킹트리 변경으로 섞여 있다."},
{"where":"안 볼 것을 프롬프트에 직접 적어 준다(31-47행)","type":"failure-mode","claim":"배정을 프롬프트에만 적기로 하면서, 무엇을 안 보게 했는지가 산출물 어디에도 남지 않게 됐다.","consequence":"배정을 빠뜨린 회차와 제대로 적은 회차의 산출물이 동일하다. 1회차가 경고한 '0회 관측 구역이 빈 issues와 구별되지 않는다'가 처방 뒤에 오히려 더 안 보이게 됐다 — PREP 칸이면 준비 산출물로 적히지만 프롬프트는 휘발한다.","evidence":"spec 33-34,103행, 1회차 리뷰 35-37행, meta-aggregate 19-29행."},
{"where":"배정 고정(41-43행) × 표집 실측이 한 문서 한 회차(178행)","type":"risk","claim":"고정 배정이 전제 검증을 두 렌즈로 쪼개, 어느 쪽도 온전히 수행할 수 없게 만든다.","consequence":"'이 수치가 다른 문서로 안 옮겨지면 2회 규칙 전체가 무너진다'는 판단은 수치 대조(grounding 몫)와 실패 모드 추론(adversarial 몫)을 동시에 해야 나온다. 설계 전체가 n=1 측정 위에 서 있는데 그 의존성을 짚을 담당 렌즈가 구조적으로 없다. 아무도 안 짚으면 겹침이 0이라 통계로도 안 드러난다.","evidence":"spec 41-42,178행. 핵심 수치는 전부 실측 주장이고 62·85-87행이 그 수치로 적용 범위를 가른다."},
{"where":"병렬이라 벽시계 시간은 거의 그대로다(62행), 인지한 대가(173-174행)","type":"over-engineering","claim":"비용 산정이 합치기 단계를 빼놓았다. 그 단계는 병렬화되지 않는 메인 세션의 직렬 작업이고 지적 수에 비례한다.","consequence":"표집을 2배로 늘리면 직렬 검증도 2배가 되고 재리뷰까지 돌면 4배가 된다. 가장 큰 비용을 대가 절에 안 적었기 때문에 '싸다'는 전제로 2회가 정당화됐고, 실제로는 그 직렬 부담 때문에 검사가 형식화될 위험이 주된 실패 경로가 된다.","evidence":"domain-spec-review/SKILL.md 119-123행. 1회차 서로 다른 발견은 12+23+18=53건이다. spec 173-174행은 서브에이전트 수만 세고 합치기 비용을 세지 않는다."},
{"where":"검증(182-200행)","type":"over-engineering","claim":"산문에 고정 문자열 앵커를 약 열여덟 개 새로 건다. 같은 레포에 그 방식이 문체 편집만으로 붉어진 기록이 있다.","consequence":"규정 문장을 다듬을 때마다 의미 없는 실패가 나고, 그것을 피하려 산문이 얼어붙거나 단언을 습관적으로 고쳐 맞추게 되어 검사가 형해화된다. 사본이 여러 파일에 흩어져 갈라질 수 있는 규칙만 남기고, 한 파일에만 있는 규정의 단언은 뺄 수 있다.","evidence":"spec 184-185,189-197행. docs/solved_problems.md의 grep -F 계약 항목."}
],
"notes":"지시대로 바깥 사실과의 대조와 문서 안의 앞뒤 모순·이름 드리프트는 보지 않았다. 뽑은 전제는 여섯이다 — (1) 마커가 있으면 훅이 침묵해도 안전하다, (2) 회차 수는 기록 파일 개수와 같다, (3) 기록 파일은 세션을 넘어 남는다, (4) 처분은 커밋과 보고가 대신 담는다, (5) 훅 안내문은 고칠 수 없는 제약이다, (6) 2회 표집의 추가 비용은 토큰뿐이다. (1)(2)(3)(4)(6)이 거짓이면 무엇이 무너지는지를 위에 적었다. (5)는 spec 202-204행이 doc_review_posttooluse.sh는 고치면서 안내문 출처인 spec_review_stop.sh는 빼고 있어, 같은 레포의 수정 가능한 파일을 불변 제약으로 다루고 있다."}
```
