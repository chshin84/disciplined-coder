# reviewer-adversarial 2회차 원본 (2026-08-16, 3회차 리뷰)

리뷰어가 돌려준 것을 그대로 옮긴다. 고치지 않는다.

```json
{"lens":"adversarial","issues":[
{"where":"회차를 기록 파일로 센다 × 원본을 같은 이름의 폴더에 넣는다","type":"failure-mode","claim":"회차를 세는 대상과 원본 보관 폴더가 같은 이름을 쓴다. 한 회차가 돌면 `<주제>-review.md` 파일 하나와 `<주제>-review/` 폴더 하나가 같이 생기므로, 그 이름으로 세면 1회차 직후에 이미 2가 된다.","consequence":"'하나면 한 번 더 자동, 둘 이상이면 묻는다'의 자동 1회가 실행되지 않는다. 2회차 처방이 새로 만든 카운터가 상한을 1회가 아니라 0회로 조인다. 반대로 디렉터리를 빼도록 구현하면 그 규칙이 어디에도 적혀 있지 않아 호출자마다 갈린다.","evidence":"reviews/ 실제 목록에 파일 둘과 디렉터리 둘이 함께 있다. spec 99행은 같은 이름의 폴더를 만들라 하고, 138-139행은 '몇 개인지 보면 된다'고만 적어 무엇을 세는지 정하지 않았다."},
{"where":"기록 파일을 세어 안다 — 세는 키가 되는 주제","type":"failure-mode","claim":"파일명의 주제를 무엇에서 도출하는지 정하지 않았다. 호출자가 회차마다 자유롭게 짓는 문자열이 상한의 유일한 키다.","consequence":"재리뷰에서 슬러그를 조금만 다르게 지으면 카운트가 0으로 리셋되어 자동 재리뷰가 무한히 다시 열린다. 반대로 같은 날 주제어가 겹치는 다른 문서가 있으면 남의 기록까지 세어 아직 자동 회차가 남은 문서를 묻는 쪽으로 밀어낸다.","evidence":"spec 91행이 파일명 형식만 정하고 주제 도출 규칙은 없다. specs/에 같은 날짜의 두 문서가 공존하며 둘 다 주제어에 review를 포함한다."},
{"where":"카운터의 내구성","type":"failure-mode","claim":"카운터가 호출자가 자발적으로 쓰는 파일에 달려 있는데, 이 spec 자신이 1회차에서 그 파일을 안 쓰고 넘어간 전례가 있다. 게다가 기록이 0개일 때 어느 분기로 가는지가 정의되어 있지 않다.","consequence":"기록을 빠뜨린 회차는 카운트에 안 잡히므로 자동 1회 상한이 실효를 잃고, 반복 리뷰가 사람 개입 없이 계속 돌 수 있다.","evidence":"1회차 기록에 '제도를 만들면서 그 제도의 첫 적용 대상을 빠뜨렸다'가 관측 사실로 적혀 있다. spec 139행의 분기는 하나면과 둘 이상이면 둘뿐이다."},
{"where":"카운터와 검증 절의 관계","type":"failure-mode","claim":"검증 절이 더하는 단언은 전부 문구 대조다. 기록 파일의 존재나 개수를 확인하는 단언은 하나도 없다. 그런데 자동 재리뷰 상한의 유일한 상태 저장소가 그 파일들이다.","consequence":"사람이 낡은 기록을 지우거나 옮기면 상한이 조용히 리셋되고, 그 리셋을 관측할 수단이 없다. '파일은 남는다'를 근거로 파일을 택했는데 파일이 남는다는 것 자체는 아무도 보증하지 않는다. 게다가 '낡는 것이 정상'이 낡은 파일을 정리하고 싶은 유인을 스스로 만든다.","evidence":"spec 189-197행의 단언 목록, 140행, 106-107행"},
{"where":"처리 결과를 담지 않는다 × 틀린 지적은 합칠 때 거른다","type":"failure-mode","claim":"오탐은 기록보다 앞선 합치기에서 걸러지므로 애초에 기록에 안 들어가고, 반영 여부는 기록에서 빠졌다. 판단이 실제로 일어나는 두 자리가 모두 무흔적이다.","consequence":"기록의 존재 이유로 든 조용한 소멸이 리뷰 단계에서 필터 단계로 자리만 옮긴다. 지적 스무 건을 전량 버린 회차와 원래 두 건뿐이던 회차가 기록상 구별되지 않는다. 이 필터는 spec이 '뒤에는 거를 자리가 없다'고 못 박은 되돌릴 수 없는 유일한 관문이다.","evidence":"spec 79-81,91,103-104,109-111행"},
{"where":"기록에 담는 것의 구성","type":"risk","claim":"기록은 쓰지 말라고 규정한 겹침 여부는 담고, 측정에 필요한 반영 여부는 뺐다. 담을 것의 선택이 기록의 두 목적과 반대 방향이다.","consequence":"기준선이 지적의 부피만 재고 값어치는 못 잰다. 인정한 대가(두 번 돌리면 틀린 지적도 는다)는 영원히 관측되지 않고, 나중에 2회를 3회로 올릴지 1회로 되돌릴지를 이 기록으로는 판단할 수 없다.","evidence":"spec 76-77,103,175,110-111행. 1회차 기록 82-83행에도 같은 구조가 지적돼 있다."},
{"where":"마커는 미루지 않는다 × 무엇이 남았는지는 문서에 적지 않는다","type":"failure-mode","claim":"마커는 개선보다 앞서 파일에 박히고, 재리뷰가 필요하다는 사실은 호출자의 기억에만 남는다. 게이트는 마커 유무만 본다.","consequence":"반영 뒤 재리뷰 전에 세션이 끝나면 파일 상태만으로는 재리뷰가 밀린 문서와 끝난 문서를 구별할 수 없다. 다음 세션은 '한 번 더 돌려도 된다'는 것만 알 뿐 '돌려야 한다'는 것은 모른다 — 의무가 권한으로 바뀐다. 카운터는 기억이 휘발한다는 이유로 파일로 옮겨 놓고, 재리뷰 의무는 같은 이유가 적용되는데도 기억에 남겼다.","evidence":"spec 121-122,131-132,140행. hooks/spec_review_stop.sh:30. 1회차 기록 73행이 같은 구멍을 이미 지적했다."},
{"where":"미루지 않는 이유가 둘이다 — 둘째 근거","type":"risk","claim":"'미루는 동안 마커 없이 지나가는 구간이 늘어나 게이트가 붙잡을 확률이 오히려 떨어진다'는 훅의 동작 방향과 반대다. 훅은 마커가 없을 때 차단하므로 미마커 구간이 길수록 붙잡을 확률은 올라간다.","consequence":"되돌린 결정이 사실상 근거 하나에만 서 있는데 문서는 둘이라고 적고 있다. 남은 근거는 훅 안내문 문구를 고치면 사라지는 종류라, 이 결정은 지금 보이는 것보다 약한 토대 위에 있다.","evidence":"hooks/spec_review_stop.sh 30,43,46-51행. 확률이 떨어진다는 서술이 성립하려면 루프가드 소진 논증이 필요한데 spec에 없다."},
{"where":"안 볼 것 배정표와 그 근거","type":"over-engineering","claim":"배정된 문장들이 각 렌즈의 체크리스트에 원래 없는 항목을 빼라고 지시한다. grounding은 외부 사실 근거만, consistency는 내부 모순·커버리지·드리프트만 보므로 '실패 모드와 과설계와 비가역은 안 봐도 된다'는 무효 지시다. adversarial도 이미 문서 밖 탐색이 금지돼 있어 '바깥 사실과의 대조는 안 봐도 된다' 역시 무효다.","consequence":"이 장치에 귀속시킨 효과를 뒷받침하는 기전이 없다. 같은 회차에 문서 자체가 크게 바뀌었으므로 관측된 감소는 교란된 값이다. 매 디스패치마다 무효 지시가 붙고 단언 둘이 이를 고정한다. 줄이는 제안 — 배정표를 통째로 뺀다. 겹침은 spec 스스로 '값싼 낭비'라 했으므로 기전 없는 지시와 단언을 얹는 것은 순손실이다.","evidence":"domain-spec-review 84-86행, reviewer-adversarial 20-22행, spec 53-55행, 1회차 기록 26,105-106행"},
{"where":"프롬프트에 직접 적는다 × 검증 절","type":"risk","claim":"안 볼 것이 PREP 칸에서 프롬프트 자유문으로 옮겨 가면서 관측점이 사라졌다. 검증은 문구 유무만 보고, 기록에는 이번에 무엇을 빼 줬는지 담지 않는다. 프롬프트 안 위치도 정하지 않았다.","consequence":"어떤 디스패치가 배정을 빠뜨렸는지 사후에 확인할 방법이 없어 수치를 재현하거나 귀인할 수 없다. 위치를 안 정한 것도 위험이다 — 앞에 놓이면 렌즈 체크리스트를 눌러 시야를 좁히고 뒤에 놓이면 무시된다. spec은 그 근거로 다른 안을 기각해 놓고 자기가 더하는 지시에는 같은 잣대를 대지 않았다.","evidence":"spec 12-14,27,31-34,103,189-191,177행"},
{"where":"기능적 변화가 있으면 한 번까지 자동으로 다시 리뷰한다 절 전체","type":"over-engineering","claim":"자동 재리뷰 1회를 위해 회차 카운팅 규칙, 파일명 규약, 마커 시점 규정, 기억에만 사는 재리뷰 의무가 함께 들어왔고, 그 넷이 위 실패 모드 대부분의 출처다. 남는 이득은 사용자에게 한 번 묻는 것을 아끼는 것뿐이다.","consequence":"자동 1회를 빼고 '기능적 변화가 있으면 다시 돌릴지 사용자에게 묻는다'로 줄이면 카운팅 규칙과 그에 딸린 실패 모드가 통째로 사라지고 서브에이전트 최대치도 열둘에서 여섯으로 준다. 잃는 것은 대답 한 번이다. 이 저울질은 이 레포가 prior-art에서 이미 채택한 것과 같다.","evidence":"spec 173,179-180,138-140행. domain-spec-review 100-102행"}
],"notes":"이번 회차 지적 열하나 가운데 여덟이 2회차 처방에서 직접 파생한 것이다. 제안은 모두 빼는 쪽이다 — 배정표 제거, 자동 재리뷰 제거. 두 절이 사라지면 카운팅·마커 시점·재리뷰 의무 관련 지적 여섯이 함께 없어지고 남는 것은 2회 표집과 기록 두 가지인데, 그 둘은 실측과 관측된 사고를 각각 근거로 갖는다."}
```
