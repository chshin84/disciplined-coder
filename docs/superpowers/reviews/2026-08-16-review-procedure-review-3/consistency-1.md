# reviewer-consistency 1회차 원본 (2026-08-16, 3회차 리뷰)

리뷰어가 돌려준 것을 그대로 옮긴다. 고치지 않는다.

```json
{"lens":"consistency","read":["docs/superpowers/specs/2026-08-16-review-procedure-design.md","docs/superpowers/reviews/2026-08-16-review-procedure-review.md","docs/superpowers/reviews/2026-08-16-review-procedure-review-2.md","skills/domain-spec-review/SKILL.md","skills/domain-docs/SKILL.md","skills/domain-llm-runtime/SKILL.md","skills/nested-orchestration/SKILL.md","agent-principles.md","scripts/test_docs_drift.sh","scripts/test_hooks.sh","README.md","hooks/spec_review_posttooluse.sh","hooks/doc_review_posttooluse.sh"],
"issues":[
{"where":"안 하는 것 절","type":"contradiction","claim":"본문은 'PREP에 칸을 만들지 않는다'고 못박았는데, 안 하는 것 절은 \"PREP에 '안 볼 것' 말고 다른 칸을 만들지 않는다\"고 적어 안 볼 것이 이미 PREP의 한 칸인 것을 전제한다.","consequence":"구현하는 쪽이 안 볼 것을 PREP 칸으로 만들면 렌즈 파일에 그 값을 받을 자리가 없어 리뷰어에게 닿지 않고, 이 문서가 기각한 두 번째 안으로 그대로 되돌아간다.","evidence":"spec:166 (대조: 같은 파일 33-34, 57 / domain-spec-review/SKILL.md:35)"},
{"where":"README의 메타 산출물 리뷰 줄","type":"drift","claim":"README가 아직 '동작이나 계약이 달라지는 수정에는 재리뷰 표시를 붙이고'라고 적는다. 재리뷰 표시라는 장치는 없어졌고 짝 스킬에는 그 말이 없다. spec 본문은 README를 왜 고치는지 한 줄도 적지 않고, 검증 절에도 README를 보는 단언이 없다.","consequence":"진입점 문서가 폐기된 표시 절차를 계속 지시하고, 그 표시는 문서에 진행 상태를 남기는 것이라 '무엇이 남았는지는 문서에 적지 않는다'와 정면으로 부딪친다. 검증이 보지 않으므로 조용히 남는다.","evidence":"README.md:25 (대조: spec:121-125,131,189-197,203)"},
{"where":"검증 절 — 마커를 미룬다는 규정이 없다는 단언","type":"gap","claim":"단언이 '마커를 남기지 않는다'라는 문자열의 부재로 구현됐는데, 그 문구는 그 파일에 존재한 적이 없다(git log -S가 아무것도 돌려주지 않는다).","consequence":"이 단언은 무조건 통과하므로 마커를 미루는 서술이 되살아나도 잡지 못한다. 1회차가 같은 모양의 헛단언을 지적했는데 고치는 과정에서 같은 것이 새로 하나 생겼다.","evidence":"test_docs_drift.sh:131 (약속한 곳: spec:192)"},
{"where":"두 번씩 띄우는 근거의 실측 표","type":"contradiction","claim":"spec이 '오늘 잰 값'으로 적은 표(14/12/7, 15/14/10, 10/15/7)가 레포의 두 기록 파일 어느 쪽과도 맞지 않는다. 반면 같은 문서의 겹침 수치(12건 중 3건, 23건 중 3건)는 1회차 기록의 표와 정확히 일치한다.","consequence":"두 실측이 서로 다른 회차의 것이 되어 '한 문서 한 회차에서 얻은 값'이라는 서술이 사실과 어긋나고, 기준선이 기록 파일에 남는다는 핵심 근거가 첫 사례부터 성립하지 않는다.","evidence":"spec:64-72,53-55,110-111,178 (대조: review.md:8-12 / review-2.md:18-22)"},
{"where":"리뷰가 도는 자리 표의 마지막 행과 그 뒤 문단","type":"contradiction","claim":"여러 에이전트를 한꺼번에 돌리는 자리를 표는 '정하지 않았다'로, 본문은 '아직 빈자리다'로, 안 하는 것 절은 '이 규율을 적용하지 않는다'고 결정형으로 적는다. 표의 '몇 번 도나' 칸도 횟수 대신 규율 배제를 적어 그 열의 질문에 답하지 않는다.","consequence":"같은 자리가 미결과 결정으로 동시에 적혀 읽는 쪽이 갈린다. '아직 빈자리'는 진행 중 상태를 문서에 담은 것이라 spec 자신의 규정과 부딪친다.","evidence":"spec:154,159-161,169 (대조: 같은 파일 131)"},
{"where":"기록에 담을 것 — 문서 검진 쪽","type":"contradiction","claim":"spec은 기록에 '각 지적을 어떻게 처리했는지는 담지 않는다'고 적고 같은 절 끝에서 '일반 문서 검진도 파일로 남긴다'고 범위를 넓히는데, domain-docs는 '무엇을 고쳤고 무엇을 넘겼는지 함께 적는다'고 정반대를 규정한다.","consequence":"문서 검진 기록을 쓰는 쪽이 어느 규칙을 따를지 갈린다. 두 규정이 각각 별도 단언으로 고정돼 있어 어느 쪽을 고쳐도 검사가 붉어진다.","evidence":"spec:103-104,115 (대조: domain-docs/SKILL.md:138 / test_docs_drift.sh:123,149)"},
{"where":"문서 검진 기록의 자리","type":"gap","claim":"spec·plan 기록의 경로는 못박으면서 일반 문서 검진은 '파일로 남긴다'고만 적고 어디에 남기는지 정하지 않는다. domain-docs에도 경로가 없다.","consequence":"기록이 docs/superpowers/reviews/ 밖에 쓰이면 훅의 제외 규칙에 걸리지 않아 검진 넛지가 다시 뜨고, 막으려던 순환이 그 경로에서 되살아난다.","evidence":"spec:91,115,152 (대조: domain-docs/SKILL.md:138 / hooks/doc_review_posttooluse.sh:17)"},
{"where":"다시 리뷰를 부르는 이름","type":"drift","claim":"같은 절차를 본문과 표에서는 '다시 리뷰'로, 인지한 대가와 한계 절에서는 '재리뷰'로 부른다. 짝 스킬의 절 제목과 README도 '재리뷰' 쪽이다.","consequence":"검증이 문구 대조라고 스스로 정했는데 같은 개념이 두 문구로 갈리면 어느 쪽을 앵커로 잡을지 흔들린다.","evidence":"spec:119,134,138,142,144,148 대 179 (대조: domain-spec-review/SKILL.md:173 / README.md:25)"},
{"where":"기록 파일을 부르는 이름","type":"drift","claim":"같은 산출물을 '리뷰 결과를 파일로 남긴다', '기록 파일', '합산 기록'으로 부른다. 짝 스킬도 프런트매터는 '합산 기록', 본문 제목은 '리뷰 기록'으로 갈라 적는다. 2회차가 이름이 셋이라고 지적한 것이 그대로 남았다.","consequence":"훅 제외 규칙과 앵커가 어느 이름을 가리키는지 흐려지고, 읽는 쪽이 별개 산출물로 오해해 두 벌을 쓰게 된다.","evidence":"spec:89,138,177 (대조: domain-spec-review/SKILL.md:3 대 131 / review-2.md:68)"},
{"where":"리뷰가 도는 자리 표의 '몇 번 도나' 칸","type":"drift","claim":"표 셀이 '2회', '1회'처럼 명사 조각으로 끝난다. 정본은 표 셀도 예외 없이 완결된 문장이나 구로 채우라고 명시한다. 1·2회차가 모두 지적했는데 이 칸이 남았다.","consequence":"정본을 강제하는 문서가 정본의 문체 규칙을 어기면, 이 문서를 본보기로 삼는 다음 spec들이 같은 조각 표기를 복제한다.","evidence":"spec:151-153 (규칙: agent-principles.md:11 / 앞선 지적: review.md:65, review-2.md:70)"},
{"where":"grounding이 도는 자리에 대한 요약 문장","type":"contradiction","claim":"본문은 'reviewer-grounding은 위험도가 낮은 런타임 호출을 빼면 나머지 자리에서 모두 돈다'고 단정하는데, 바로 위 표의 마지막 행은 그 자리를 '그 작업이 렌즈를 직접 부른다'로만 적어 정하지 않았다. 실제 워크플로 스킬은 grounding을 조건부로 적는다.","consequence":"미정이라고 선언한 자리를 본문이 정해진 것처럼 세어 흔들리는 경로 수를 과대평가한다. 1회차가 같은 모양의 과장을 지적했는데 표현만 바뀐 채 남았다.","evidence":"spec:156 (대조: 같은 파일 154 / nested-orchestration/SKILL.md:44 / review.md:53-54)"},
{"where":"검증 절의 단언 목록","type":"gap","claim":"'더하는 단언은 이렇다'는 닫힌 목록으로 적혔는데, 실제 스크립트에는 목록에 없는 단언이 여럿 있다 — 두 렌즈가 서로 빼지 않는다는 것과 그 이유, 합칠 때 세 기준으로 거른다는 것, 재리뷰 1회 자동, 회차를 파일로 센다는 것, 사용자 결정 반영도 대상이라는 것, 기능적 변화를 본다는 것.","consequence":"검증 절이 실제 방어선을 좁게 서술하므로, 나중에 스크립트를 정리하는 쪽이 목록에 없는 단언을 지워도 spec 대조로는 걸리지 않는다.","evidence":"spec:187-197 (대조: test_docs_drift.sh:130,135-138,141,143-145)"}
],
"notes":"이번 회차 지적 열둘 가운데 넷은 앞선 회차가 이미 짚었다가 고치는 과정에서 형태만 바뀌어 남은 것이다(README의 재리뷰 표시, 헛단언, 멀티에이전트 행의 결정형과 미결형 혼재, 표 셀 명사 조각). 반대로 마커 미루기 폐기, 회차별 파일명, 받는 즉시 저장, 훅 안내문과의 충돌, 문서 검진 훅의 reviews 제외는 spec·짝 스킬·스크립트·훅에 일관되게 반영된 것을 확인했다. 실패 모드와 과설계와 되돌리기 어려운 결정은 지시대로 보지 않았다."}
```
