# 리뷰 기록 — 업데이트 뒤 자기감사 (2026-09-01)

대상은 커밋 `201407a` 시점의 레포 전체이고, 실행체는 `.claude/workflows/self-audit.js`다. 렌즈 여덟을
한 회차에 병렬로 띄웠고 발견마다 사실성·실질성 두 관점의 반박 검증을 붙였다. 원본은 같은 이름의
폴더에 넣었다.

**이 기록은 메인 세션이 직접 썼다.** 실행체가 기록 걸음을 배선하지 않아 자동으로 남지 않는다. 그
사실 자체가 이 회차의 확정 발견 하나다.

## 실행 전에 고친 것

실행체가 파싱 단계에서 막혀 있었다. `.claude/workflows/self-audit.js:84`의 `lens-adversarial`
프롬프트가 템플릿 문자열 안에 백틱을 그대로 담아 문자열이 거기서 끊겼다. 커밋 `405f235`가 그 문장을
넣은 시점부터 이 워크플로는 돌 수 없었고, 그 뒤의 두 회차(`2f64d74`·`e6d9893`)는 자기감사 없이
지나갔다. 백틱 둘을 이스케이프해 `node --check`가 통과하게 고친 뒤 돌렸다.

## 돌린 렌즈와 그 결과

| 렌즈 | 무엇을 봤는가 |
|---|---|
| `lens-grounding` | README·CLAUDE.md·정본·커맨드 문서가 코드에 근거하는지 |
| `lens-consistency` | 정본과 스킬 열넷 사이의 모순과 이름 드리프트 |
| `lens-adversarial` | 정본의 절 전부와 훅·스크립트·스킬 설계의 실패 모드 |
| `ssot-audit` | 정본과 스킬과 스크립트와 훅과 문서 사이의 손 동기화 쌍 |
| `shell-audit` | 스크립트와 훅의 오류 삼킴·멱등성·명시성 |
| `clear-comm-audit` | `PROSE-FORM` 자기준수 |
| `plugin-compliance` | `domain-plugin` 처방의 자기준수 |
| `docs-compliance` | `domain-docs` 처방의 자기준수 |

`lens-fit`과 `lens-readability`는 이 실행체의 배정에 없어 돌지 않았다. `lens-prior-art`는 정본이
상시 허용에서 뺀 렌즈라 공백으로 세지 않는다.

## 판정 개수

| 항목 | 값 |
|---|---|
| 확정 | 37건이다 |
| 기각 | 23건이다 |
| 미판정 | 0건이다 |
| 응답하지 않은 렌즈 | 0개다 |
| 결정론 검사가 실어 온 발견 | 7건이고 그 가운데 6건이 확정 목록과 겹치지 않는다 |
| 에이전트 | 131개다 |

결정론 검사 결과는 `allPassed:false`로 왔으나 실행 계약이 깨진 것이 아니다. `scripts/test_*.sh` 다섯이
합계 PASS=630 FAIL=0으로 끝났고 `claude plugin validate ./`도 `version` 경고 하나만 냈다. false를
만든 것은 같은 결과 배열에 실려 온 발견 일곱이다.

## 둘 이상의 렌즈가 함께 잡은 것

| 발견 | 잡은 렌즈 |
|---|---|
| 전역 `settings.json`을 묻지 않고 고쳐 자동 갱신을 켠다 | grounding, adversarial |
| Codex 매니페스트가 Claude 쪽 문안을 복제하는데 대조 검사가 그 파일을 안 훑는다 | ssot-audit, plugin-compliance |

## 상충 — 처분이 갈리는 짝

- **죽은 것의 처분이 넷으로 갈린다.** 배포된 spec은 지우라 하고, 오답노트 설계 문서 셋은 지우지 말고
  superseded를 붙이라 하며, DESIGN-NOTES를 가리키는 spec은 살아 있는 문서로 전제해 참조만 고치라 하고,
  죽은 셸 함수는 삭제하지 말고 표시하라 한다.
- **게이트 해제 스위치의 범위.** README에 `DISCIPLINED_CODER_REVIEW_GATE`를 spec 게이트 해제 스위치로
  적으라는 확정 발견과, 그 변수가 문서 훅까지 끈다는 결정론 발견이 같은 문자열에 서로 다른 범위를
  요구한다. 앞을 먼저 반영하면 새 오해를 사용자 문서에 심는다.
- **문안 수정과 기계 앵커.** 괄호 소유권 제목 여섯을 명사구로 줄이라는 발견의 대상 둘을
  `scripts/test_docs_drift.sh:118,179`가 grep 앵커로 붙들고 있다. 문안만 고치면 계약 검사가 실패한다.
- **렌즈 배정의 문서와 실행체.** `lens-adversarial`의 호출자 주장을 빼면, 그 렌즈를 실제로 띄우는
  `self-audit.js`가 배정 표 밖의 렌즈를 띄우는 상태로 남는다.
- **README 압축 방향.** README에 문장을 더하라는 확정 발견 여섯이 그 문서를 12,710바이트에서
  2,555바이트로 줄인 직전 회차의 결정과 부딪친다.

## 커버리지 공백

- `lens-fit`과 `lens-readability`가 이 실행체의 배정에 없어 목적에 비춘 전달 방해 판정이 이 회차에 없다.
- 결정론 검사가 실어 온 발견 일곱은 반박 검증을 거치지 않아 확신도가 확정 37건과 같지 않다.
- `.github/workflows/ci.yml`은 어느 렌즈 배정에도 없고 결정론 검사가 우연히 잡았다. 같은 종류의 파일이
  더 있는지는 확인되지 않았다.
- 중복 제거 손실을 감지하는 장치가 없어, 확정 목록이 원시 발견 전부를 덮는지 이 회차로는 확인할 수 없다.
- 맥과 리눅스에서의 스위트 실행, Codex 등록 절차, 홈 리다이렉트 PC의 동작은 코드 읽기로만 다뤘다.

## 처분

확정 37건과 결정론 발견 여섯의 처분은 이 파일이 정하지 않는다. 사용자 결정이 필요한 것 넷을 즉시
올렸고, 무엇을 고쳤는지는 그 뒤의 커밋이 적는다. 이 파일은 이 회차를 찍은 기록이라 뒤에 고치지 않는다.
