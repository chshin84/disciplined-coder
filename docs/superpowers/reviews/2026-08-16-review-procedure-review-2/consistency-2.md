# reviewer-consistency 2회차 원본 (2026-08-16, 2회차 리뷰)

리뷰어가 돌려준 것을 그대로 옮긴다. 고치지 않는다.

```json
{"lens":"consistency","issues":[
{"where":"리뷰 결과 절과 본문 전체","type":"contradiction","claim":"마지막 절은 🔴 넷이 결정을 기다린다고 적었는데, 본문에는 그 🔴가 묻는 항목의 답이 이미 규정으로 들어가 있다. 재리뷰 상한은 한 번까지 자동으로, 런타임 기록은 남기지 않는다로 정해 놓았다.","consequence":"어떤 규정이 승인을 받은 것이고 어떤 것이 대기 중인지 가릴 수 없다. 마커가 escalated인 채로 결정이 반영돼 있어, 사용자가 다른 답을 고르면 이미 짝 문서까지 퍼진 규정을 되감아야 한다.","evidence":"specs/...design.md:187 대 112·101-103; reviews/...review.md:30,39-44"},
{"where":"세 호출자 절","type":"contradiction","claim":"절 제목은 호출자가 셋이라 적었는데 바로 아래 표에는 다섯 행이 있다.","consequence":"1회차 지적을 행 추가로 고치면서 제목을 그대로 두어, 제목과 표가 서로 다른 개수를 말한다. 제목만 읽는 독자는 런타임 저위험 티어와 멀티에이전트 자리를 계속 못 본다.","evidence":"specs/...design.md:128 대 132-136"},
{"where":"멀티에이전트 자리의 처분","type":"contradiction","claim":"표의 마지막 행과 안 하는 것 절은 적용하지 않는다고 결정형으로 적었는데, 표 아래 본문은 정하지 않았고 이번 범위에서 뺐다고 미결형으로 적는다.","consequence":"한쪽은 결정, 다른 쪽은 보류라 나중에 그 자리를 다룰 때 이 spec이 이미 답했는지 다시 물어야 하는지 판단할 근거가 없다.","evidence":"specs/...design.md:136,151 대 141-143"},
{"where":"표 뒤 grounding 서술","type":"contradiction","claim":"grounding이 나머지 자리에서 모두 돈다고 적었으나, 같은 표의 멀티에이전트 행은 어느 렌즈가 도는지를 정하지 않았다고 선언한다.","consequence":"1회차 지적을 티어 추가로만 고치고 문장은 남겨, 렌즈 파일을 건드릴 때 흔들리는 경로의 범위를 여전히 부풀려 말한다.","evidence":"specs/...design.md:138 대 136,142-143"},
{"where":"마커 미루기 규정과 훅 안내문·드리프트 가드","type":"contradiction","claim":"본문은 표시가 남으면 마커를 남기지 않는다고 정했는데, 집행하는 훅 안내문은 조건 없이 마커를 먼저 남기라고 지시한다. 고친 파일 여섯에 두 훅이 없고, 드리프트 스크립트는 훅의 마커 선기록 지시와 호출자의 마커 미루기를 동시에 단언한다.","consequence":"훅을 따르면 새 규정을 어기고, 새 규정을 따르면 훅을 어긴다. 드리프트 테스트가 두 규정을 함께 통과시키므로 모순이 가드에 잡히지 않고 고정된다.","evidence":"specs/...design.md:118,178-180; hooks/spec_review_posttooluse.sh:22; scripts/test_docs_drift.sh:101,112,113,130"},
{"where":"재리뷰 자동 상한과 호출자 스킬의 반복 규정","type":"contradiction","claim":"spec은 한 번까지 자동이라 적었는데 짝 스킬은 같은 절에서 표시가 없어질 때까지 반복하고 마커는 마지막 회차에서 확정한다고 적는다.","consequence":"두 문장이 나란히 있어 무한 반복 금지와 표시 소멸까지 반복이 충돌한다. 자동 1회를 다 쓴 뒤 표시가 남으면 마커를 낼 수도 없고 더 돌 수도 없어 끝낼 경로가 정의되지 않는다.","evidence":"specs/...design.md:112,116-118,132; skills/domain-spec-review/SKILL.md:165-176"},
{"where":"합산 기록 절","type":"contradiction","claim":"기록은 합치기가 끝나면 쓴다고 시점을 못 박았고 담을 것에 처분을 넣었으며 한 번 쓰고 고치지 않는다고 선언했는데, 처분은 기록을 쓴 뒤에 오는 단계다. 회차별 별개 파일 규정이 더해져 나중에 채워 넣을 경로도 막혔다.","consequence":"규정대로 하면 아직 없는 처분을 담아야 하고, 나중에 채우면 고치지 않는다는 규정을 어긴다. 회차별 별개 파일을 넣으면서 이 충돌이 더 굳어졌다.","evidence":"specs/...design.md:83-85,91,99; skills/domain-spec-review/SKILL.md:160"},
{"where":"범위 밖 칸과 안 하는 것 절","type":"gap","claim":"리뷰어를 띄울 때 안 봐도 되는 것을 적어 준다고 정했으면서, 안 하는 것 절에서는 렌즈 파일도 프롬프트도 그대로라고 선언했다. user 템플릿에는 그 칸이 들어갈 자리가 없다.","consequence":"첫째 결정이 렌즈에 도달하는 경로가 문서상 존재하지 않는다. 앞선 두 안을 기각한 이유가 자기 안에 그대로 남는다.","evidence":"specs/...design.md:32,147; skills/reviewer-consistency/SKILL.md:24; skills/domain-spec-review/SKILL.md:30-33"},
{"where":"재리뷰 표시 절","type":"gap","claim":"표시를 언제 떼는지는 정했으나 어디에 두는지는 여전히 정하지 않았다. 마커를 낼지가 표시 유무에 걸려 있고, 검증도 서술 존재만 확인한다.","consequence":"세션 메모리에만 있으면 여러 턴 루프나 다른 세션에서 휘발하고, 마커를 미룰지 확정할지 판단할 근거가 사라진다. 표시 없음과 표시 휘발이 산출물상 구별되지 않는다.","evidence":"specs/...design.md:105-118,168; reviews/...review.md:76"},
{"where":"일반 문서 검진의 기록","type":"drift","claim":"spec은 파일로 남긴다고 적었는데 소유 문서인 domain-docs에는 파일이라는 말도 경로도 없다. 검증도 결과를 남긴다는 규정이 있는지만 확인한다.","consequence":"어디에 어떤 형태로 남기는지가 정해지지 않아 호출자마다 갈린다. 기록을 근거로 삼은 조용한 소멸 방지가 그 자리에서는 성립하지 않는다.","evidence":"specs/...design.md:101,134,171-172; skills/domain-docs/SKILL.md:138-139"},
{"where":"기록을 부르는 이름","type":"drift","claim":"같은 산출물을 spec은 합산 기록, 호출자 스킬은 리뷰 기록, 표는 기록, domain-docs는 결과를 합산해 남긴다로 부른다. 실제 파일 제목도 리뷰 기록이다.","consequence":"드리프트 가드가 문자열 앵커로 도는 구조라 이름이 갈리면 어느 표현을 검사할지 정해지지 않고, 한 이름만 고쳐도 다른 곳이 조용히 남는다.","evidence":"specs/...design.md:81,130,159,168,184; skills/domain-spec-review/SKILL.md:125; skills/domain-docs/SKILL.md:138"},
{"where":"안 하는 것 절의 PREP 칸 이름","type":"drift","claim":"안 하는 것 절은 '안 볼 것' 말고 다른 칸을 만들지 않는다고 적었는데, 그 칸의 실제 이름은 본문에서도 스킬에서도 '범위 밖'이다.","consequence":"실제 산출물에 없는 이름으로 금지 규정을 걸어 두어, PREP 항목을 손댈 때 이 금지가 어느 칸을 가리키는지 대조되지 않는다.","evidence":"specs/...design.md:148 대 30-32; skills/domain-spec-review/SKILL.md:33"},
{"where":"검증 절의 도출 주장","type":"drift","claim":"개수를 박지 않고 기존 도출 방식을 따른다고 적었으나 실제로 더해진 단언은 전부 고정 문자열 grep이다.","consequence":"1회차에서 같은 지적을 받은 문장이 그대로 남아 있어 서술과 스크립트가 계속 어긋난다. 문장을 다듬기만 해도 단언이 깨지는데 문서에는 도출이라 적혀 있어 실패 원인을 오해하게 만든다.","evidence":"specs/...design.md:166 대 scripts/test_docs_drift.sh:115-144"},
{"where":"검증 절이 약속한 단언과 스크립트","type":"gap","claim":"훅의 reviews 제외를 단언한다고 적었으나 드리프트 스크립트에 그 훅을 검사하는 줄이 없다. 반대로 스크립트가 거는 단언 가운데 배정 고정, 서로 빼지 않음, 합치기 세 기준, 원본 담기, 회차 파일명은 검증 절 목록에 없다.","consequence":"검증 절과 스크립트가 서로의 부분집합도 아니어서 어느 쪽이 약속의 정본인지 정해지지 않는다. 훅의 제외 처리는 가드가 없어 되돌아가도 아무도 알아채지 못한다.","evidence":"specs/...design.md:166-176; scripts/test_docs_drift.sh:115-144; hooks/doc_review_posttooluse.sh:17"},
{"where":"검증 절의 '리뷰는 한 번만 돈다' 단언","type":"gap","claim":"두 파일에는 원래 그 문구가 없었고, 실제로 살아 있는 곳은 짝 계획서다. 스크립트에도 이 단언이 없다.","consequence":"1회차에 헛단언으로 지목된 항목이 그대로 남아, 폐기된 규칙이 살아 있는 파일은 계속 검사 밖에 있다. 계획서를 실행하는 워커가 뒤집힌 규칙을 읽을 수 있다.","evidence":"specs/...design.md:170; plans/2026-08-16-review-layer-redesign.md:234"},
{"where":"표집 실측 표와 리뷰 기록의 표","type":"drift","claim":"spec은 29%에서 53%까지의 표를 오늘 잰 값이라 소개하는데, 같은 날짜의 리뷰 기록은 같은 범위를 앞선 회차(다른 문서)에서 잰 것이라 적고 오늘 이 spec을 두고 잰 값으로는 다른 숫자의 표를 싣는다.","consequence":"같은 날 두 문서가 같은 실측을 서로 다른 회차에 귀속시켜, 어느 값이 어느 문서의 기준선인지 가릴 수 없다.","evidence":"specs/...design.md:56-64,78 대 reviews/...review.md:8-18"},
{"where":"두 표의 열 이름과 셀","type":"drift","claim":"실측 표의 열 이름과 셀, 호출자 표의 일부 셀이 명사 조각이다.","consequence":"정본이 표와 스펙에서도 완결된 문어체를 요구하는데 1회차 지적 뒤에도 형식이 그대로다. '없다'가 무엇이 없다는 뜻인지 열 이름과 합쳐야만 읽힌다.","evidence":"specs/...design.md:60-64,130-136; agent-principles.md:11"},
{"where":"절 제목과 열거 문장","type":"drift","claim":"제목이 개수를 박고 있고 본문도 담는 것은 넷, 이유는 둘, 고친 파일은 여섯으로 개수를 문장에 심었다.","consequence":"항목을 개수로 부르면 목록이 바뀌는 순간 조용히 어긋난다. 실제로 호출자 자리가 셋에서 다섯으로 늘었는데 제목이 셋으로 남은 사례가 같은 문서 안에 있다.","evidence":"specs/...design.md:28,87,91,178 및 128; agent-principles.md:11"}
],"notes":"실패 모드·과설계·되돌리기 어려움은 배정대로 보지 않았다. 바깥 사실 대조는 인용 논문 본문까지는 열지 않고 레포 안 파일과 훅·스크립트 실물만 대조했다."}
```
