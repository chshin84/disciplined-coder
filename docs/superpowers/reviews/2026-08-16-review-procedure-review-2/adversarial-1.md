# reviewer-adversarial 1회차 원본 (2026-08-16, 2회차 리뷰)

리뷰어가 돌려준 것을 그대로 옮긴다. 고치지 않는다.

```json
{"lens":"adversarial","issues":[
{"where":"다시 리뷰 한 번 자동(112–114행)과 표시가 남으면 마커를 안 남긴다(118행)의 결합","type":"failure-mode","claim":"자동 재리뷰 한 번을 소진한 뒤에도 표시가 남는 상태가 정상 경로로 도달 가능한데, 그 상태에서는 마커를 남길 수도 없고 자동으로 더 돌 수도 없어 문서가 무마커인 채로 사용자 질문을 기다리게 된다.","consequence":"재리뷰 1회가 만들어 낸 수정이 다시 기능적 변화이면 표시가 남고, 마커는 금지되고, 자동 회차는 소진된 상태가 된다. 사용자에게 물으려면 턴을 끝내야 하는데 Stop 게이트는 미리뷰 문서가 남은 턴 종료를 막는다. 결국 이 상태를 빠져나가는 유일한 경로가 stop_hook_active 루프가드로 게이트가 한 턴에 한 번만 걸리는 알려진 결함이 된다. 설계의 종료가 감수한 대가로 적어 둔 결함에 의존한다.","evidence":"specs/...design.md:112-114,118; skills/domain-spec-review/SKILL.md:19-23, 202-207"},
{"where":"돌리는 횟수를 3회 이상으로 올리지 않는다(150행)와 재리뷰 자동 1회(112행)","type":"over-engineering","claim":"선언된 상한은 회차당 2회인데 자동 재리뷰가 붙으면 한 문서에 대해 렌즈당 실제로 네 번 도는 것이 기본 경로가 되어, 상한이 실효를 갖지 못한다.","consequence":"2회까지만 쟀다는 근거로 3회 이상을 금지해 놓고, 근거가 없는 3회·4회 구간을 재리뷰라는 다른 이름으로 통과시킨다. 늘어난 두 회차의 이득은 측정된 적이 없고 비용은 확정적으로 두 배가 된다.","evidence":"specs/...design.md:150,112,155"},
{"where":"🔴를 반영한 경우도 대상이다(108–110행)와 리뷰 결과(187–189행)","type":"failure-mode","claim":"🔴가 하나라도 있으면 마커는 escalated로 확정되는데, 🔴 반영은 그 마커 뒤에 오므로 마커 미루기 규정이 가장 큰 기능적 변화에는 구조적으로 적용되지 않는다.","consequence":"1회차가 지적한 문제가 처방된 뒤에도 형태만 바꿔 그대로 남는다. escalated가 찍힌 문서 위에 사용자 결정에 따른 미검토 편집이 얹혀도 훅은 드러내지 못한다. 이 문서 자신이 그 상태에 있다.","evidence":"specs/...design.md:187,189,108-110,118"},
{"where":"표시가 어디에 놓이는지가 정해지지 않은 채 마커가 표시에 종속된 것(116–121행)","type":"failure-mode","claim":"마커의 정확성을 '표시'라는 대상에 종속시켰는데, 그 표시를 둘 수 있는 자리가 양쪽 다 막혀 있다.","consequence":"문서에 적으면 '문서에 상태 금지'와 부딪치고, 세션 맥락에 두면 압축이나 재개에서 휘발한다. 휘발하면 '표시가 하나도 없으면' 분기로 읽혀 마커가 곧바로 확정되므로, 거짓 마커를 막으려고 만든 규정이 정확히 그 거짓 마커를 조용히 생산한다.","evidence":"specs/...design.md:116-121; skills/domain-spec-review/SKILL.md:20-23"},
{"where":"PREP 고정 배정(32–36행)과 재리뷰의 입력을 다시 준비한다(123–124행)","type":"risk","claim":"배정을 문서에 고정한 결과, 배정이 틀렸을 때 생기는 사각이 문서마다·회차마다 동일해져 표집을 늘려도 메워지지 않는다.","consequence":"어떤 결함 종류가 아무에게도 배정되지 않았거나 잘못 배정되면, 2회 표집도 재리뷰도 같은 배정표로 도는 이상 그 구역의 관측은 네 번 모두 0이다. 0회 관측 구역의 산출은 빈 issues와 구별되지 않으므로 잘못된 배정 자체가 리뷰로는 절대 발견되지 않는다.","evidence":"specs/...design.md:32,42-44; skills/domain-spec-review/SKILL.md:35,181-182"},
{"where":"틀린 지적은 합칠 때 거른다(71–73행)와 기록에 담는 것 넷(91행)","type":"failure-mode","claim":"오탐을 거르는 자리를 새로 만들면서 그 자리를 문서 작성자 본인에게 주었고, 무엇을 걸렀는지는 기록에 담기지 않는다.","consequence":"확증 편향 주체에게 지적을 무근거로 판정할 권한을 새로 준 셈이다. 필터가 표집 증분을 얼마나 되돌렸는지는 어떤 산출물로도 관측되지 않는다. 특히 '이미 다른 데서 처리된 것은 아닌가'는 불편한 지적을 흔적 없이 떨어뜨리는 가장 값싼 경로다.","evidence":"specs/...design.md:71-73,91; skills/domain-spec-review/SKILL.md:12-14,113-123"},
{"where":"두 번 다 나왔다는 것을 신뢰도로 쓰지 않는다(68–69행)와 기록에 담는 겹침 여부(91행)","type":"risk","claim":"겹침 여부를 기록 항목으로 고정하면서 그것을 신뢰도로 쓰지 말라고 하는데, 겹침을 눈앞에 둔 채 기각을 판단하는 사람이 같은 사람이다.","consequence":"한 번만 나온 지적을 떨어뜨리는 쪽이 기록상 덜 눈에 띈다. 우선적으로 걸러지는 것이 정확히 2회 표집으로 건지려던 몫이 되어, 표집 증분이 형식적으로만 남고 실질은 1회 리뷰로 회귀할 수 있다.","evidence":"specs/...design.md:68-69,91"},
{"where":"회차별 별개 파일(83–85행)과 한 번 쓰고 고치지 않는다(99행)","type":"irreversible","claim":"덮어쓰기를 막으려고 회차 파일을 불변으로 못 박은 결과, 앞 회차가 남긴 미결 항목의 결말을 적을 자리가 어디에도 없어졌다.","consequence":"🔴에 대한 사용자 결정은 1회차 파일에 쓸 수 없고 2회차 파일의 담는 것 넷에도 자리가 없다. 리뷰가 만들어 낸 가장 중요한 산출물인 사용자 결정이 파일 기록에서 사라진다.","evidence":"specs/...design.md:83-85,99,91,187,96-97"},
{"where":"리뷰어가 돌려준 원본(91–93행)","type":"risk","claim":"기록을 정당화하는 핵심 논거인 원본 보존이 제도의 첫 적용에서 이미 지켜지지 않았고, 대신 곧 사라질 임시 경로가 커밋됐다.","consequence":"'나중에 대조'라는 논거가 대조 대상 없이 남는다. 첫 원자료로 지목된 파일이 죽은 포인터가 되어 기준선 목적이 첫 회차부터 달성되지 않는다. 강제 수단이 없는 규정을 남기면 관측이 생겼다고 착각하게 만드는 쪽이 더 나쁘다.","evidence":"reviews/...review.md:94-106,90; specs/...design.md:159"},
{"where":"일반 문서 검진도 파일로 남긴다(101행)와 2회 표집을 한정한 근거(77–79행)","type":"over-engineering","claim":"호출 빈도를 근거로 표집은 좁혔으면서 기록 의무만 문서 검진까지 폈는데, 같은 근거를 적용하면 기록 의무야말로 좁혀야 한다.","consequence":"빈도가 높은 경로에 가장 무거운 산출물 의무가 붙어, README 한 줄 수정마다 기록 파일이 하나씩 늘어난다. 그 디렉터리는 검진 훅에서 빠져 있어 늘어난 산출물은 아무 점검도 받지 않는다.","evidence":"specs/...design.md:77-79,101,87-89"},
{"where":"인지한 대가와 한계(155–162행)와 합칠 때 거른다의 첫 질문(71–72행)","type":"over-engineering","claim":"대가를 토큰과 벽시계 시간으로만 셈했는데, 실제로 늘어난 것은 병렬이 아닌 직렬 구간, 즉 메인 세션의 합치기 비용이다.","consequence":"필터의 첫 질문은 지적마다 근거 파일을 열어 확인하라는 것이라 발견 수에 비례해 컨텍스트를 직접 소모한다. 병목이 리뷰어에서 합치기로 옮겨 갔는데 그 비용이 대가 목록에 없으므로, 컨텍스트가 모자랄 때 필터가 형식적으로 수행되는 쪽으로 무너진다.","evidence":"specs/...design.md:155-157,71-72,184"}
],"notes":"이번 회차에서 새로 들어온 위험은 대부분 처방들이 서로 맞물리는 자리에 있다. 자동 재리뷰 한 번과 마커 미루기가 만나면 무마커 교착이 생기고, 고정 배정과 2회 표집이 만나면 사각이 회차 간에 상관되며, 작성자가 수행하는 오탐 필터와 겹침 표시가 만나면 표집 증분이 우선적으로 깎인다. 줄이는 방향의 처방은 셋이다. 자동 재리뷰 회차를 없애고 재리뷰 여부를 항상 사용자에게 넘기면 교착과 실효 4회 표집이 함께 사라진다. 기록 의무를 2회 표집과 같은 경계로 되돌리면 근거가 한 기준으로 통일된다. 강제 수단이 없는 원본 전문 보존 규정은 지킬 수 있는 형태로 줄이거나 빼는 편이, 지켜지지 않은 채 관측이 있다고 믿게 하는 것보다 낫다."}
```
